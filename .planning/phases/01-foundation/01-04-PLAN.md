---
phase: "01"
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - apps/web-storefront/package.json
  - apps/web-storefront/tsconfig.json
  - apps/web-storefront/vite.config.ts
  - apps/web-storefront/index.html
  - apps/web-storefront/src/main.tsx
  - apps/web-storefront/src/App.tsx
  - apps/web-storefront/src/app.css
  - apps/web-storefront/.env.example
  - apps/web-admin/package.json
  - apps/web-admin/tsconfig.json
  - apps/web-admin/vite.config.ts
  - apps/web-admin/index.html
  - apps/web-admin/src/main.tsx
  - apps/web-admin/src/App.tsx
  - apps/web-admin/src/app.css
  - apps/web-admin/.env.example
  - apps/web-vendor/package.json
  - apps/web-vendor/tsconfig.json
  - apps/web-vendor/vite.config.ts
  - apps/web-vendor/index.html
  - apps/web-vendor/src/main.tsx
  - apps/web-vendor/src/App.tsx
  - apps/web-vendor/src/app.css
  - apps/web-vendor/.env.example
autonomous: true
requirements:
  - FND-04
  - FND-07

must_haves:
  truths:
    - "All three web apps (storefront, admin, vendor) start with pnpm dev and display a health screen in the browser"
    - "All three apps import and render a design token (e.g., primary color) from @grovio/ui/tokens — changing the token propagates visually"
    - "Tailwind CSS v4 is configured via @tailwindcss/vite plugin (NOT PostCSS) in all three apps"
    - "All three apps import types from @grovio/contracts without TypeScript errors"
    - "pnpm typecheck passes for all three web apps"
  artifacts:
    - path: "apps/web-storefront/vite.config.ts"
      provides: "Vite 8 config with @tailwindcss/vite plugin"
      contains: "@tailwindcss/vite"
    - path: "apps/web-admin/vite.config.ts"
      provides: "Vite 8 config with @tailwindcss/vite plugin"
      contains: "@tailwindcss/vite"
    - path: "apps/web-vendor/vite.config.ts"
      provides: "Vite 8 config with @tailwindcss/vite plugin"
      contains: "@tailwindcss/vite"
    - path: "apps/web-storefront/src/App.tsx"
      provides: "Storefront health screen showing app name and API_URL env var"
    - path: "apps/web-admin/src/App.tsx"
      provides: "Admin health screen showing app name"
    - path: "apps/web-vendor/src/App.tsx"
      provides: "Vendor health screen showing app name"
  key_links:
    - from: "apps/web-storefront/src/app.css"
      to: "packages/ui/src/tokens/tokens.css"
      via: "@import"
      pattern: "@import.*@grovio/ui"
    - from: "apps/web-storefront/src/App.tsx"
      to: "@grovio/contracts"
      via: "HealthCheckResponse import"
      pattern: "from '@grovio/contracts'"
---

<objective>
Create the three web app shells: apps/web-storefront, apps/web-admin, and apps/web-vendor. Each is a Vite 8 + React 19 + TypeScript + Tailwind CSS v4 application with a minimal health screen, @tailwindcss/vite plugin (not PostCSS), and imports from @grovio/contracts.

Purpose: The three web app shells are the scaffolding for Phases 4 and 6. Getting Tailwind v4 wired via @tailwindcss/vite (not PostCSS — a hard constraint from CLAUDE.md), React 19 with correct imports, and @grovio/contracts resolution in place now prevents framework-level rework later. The health screens also satisfy FND-04 (all apps boot with health response visible).

Output: Three runnable Vite apps, each displaying an app-specific health screen, each importing design tokens from @grovio/ui (plan 01-07 will populate the token file; these apps reference it now to prove the import chain works once tokens exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@CLAUDE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Web app package scaffolds — package.json, tsconfig, vite.config for all three apps</name>
  <files>
    apps/web-storefront/package.json,
    apps/web-storefront/tsconfig.json,
    apps/web-storefront/vite.config.ts,
    apps/web-admin/package.json,
    apps/web-admin/tsconfig.json,
    apps/web-admin/vite.config.ts,
    apps/web-vendor/package.json,
    apps/web-vendor/tsconfig.json,
    apps/web-vendor/vite.config.ts
  </files>
  <read_first>
    CLAUDE.md (Vite 8.0.x with Rolldown; Tailwind CSS 4.3.x with @tailwindcss/vite — NOT PostCSS; React 19.2.x >=19.2.3 for CVE-2025-55182; framer-motion 12.x import from "motion/react"; @tanstack/react-query 5.100.x; Zustand 5.0.x; TypeScript 5.8.x; Node 20.19+/22.12+ for Vite 8)
    .planning/phases/01-foundation/01-CONTEXT.md (workspace naming under Claude's Discretion: apps/web-storefront, apps/web-admin, apps/web-vendor)
  </read_first>
  <action>
    Create package.json for each of the three apps following this pattern (adjust name per app):

    apps/web-storefront/package.json: name "@grovio/web-storefront", version "0.1.0", private true, type "module".
    apps/web-admin/package.json: name "@grovio/web-admin", version "0.1.0", private true, type "module".
    apps/web-vendor/package.json: name "@grovio/web-vendor", version "0.1.0", private true, type "module".

    All three share the same dependency set:
    - react@19.2.x (>=19.2.3 per CVE-2025-55182), react-dom@19.2.x
    - framer-motion@12.x (import path is "motion/react" in code)
    - @tanstack/react-query@5.100.x
    - zustand@5.0.x
    - @grovio/contracts: workspace:*
    - @grovio/ui: workspace:* (design tokens — package created in plan 01-07)

    DevDependencies for all three:
    - vite@8.0.x, @vitejs/plugin-react@^4.x
    - tailwindcss@4.3.x, @tailwindcss/vite@^4.3.x
    - typescript@5.8.x, @types/react@^19, @types/react-dom@^19
    - @grovio/config: workspace:*

    Scripts for all three: "dev": "vite", "build": "tsc -b && vite build", "preview": "vite preview", "typecheck": "tsc --noEmit", "lint": "eslint src".

    Create tsconfig.json for each app extending "@grovio/config/tsconfig/react.json". Set composite false. Include ["src"], references to nothing (no project references needed for app-level).

    Create vite.config.ts for each app:
    Import tailwindcss from "@tailwindcss/vite" and react from "@vitejs/plugin-react". Export defineConfig with: plugins: [tailwindcss(), react()]. Set server.port: 5173 for storefront, 5174 for admin, 5175 for vendor. Set resolve.alias: { "@": "/src" }. Do NOT configure PostCSS at all — Tailwind v4 is handled entirely by the @tailwindcss/vite plugin.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && node -e "['apps/web-storefront','apps/web-admin','apps/web-vendor'].forEach(d=>{const p=require('./'+d+'/package.json'); console.log(p.name, p.dependencies['react']?'react-ok':'no-react')})"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web-storefront/package.json name equals "@grovio/web-storefront"
    - apps/web-admin/package.json name equals "@grovio/web-admin"
    - apps/web-vendor/package.json name equals "@grovio/web-vendor"
    - All three vite.config.ts files contain "@tailwindcss/vite" import (NOT "postcss" or "tailwind.config.js")
    - All three vite.config.ts files do NOT contain "postcss" string
    - All three package.json files have react version >=19.2.3 specified
    - All three tsconfig.json files extend "@grovio/config/tsconfig/react.json"
  </acceptance_criteria>
  <done>All three web apps have complete package.json (with correct dependency versions), tsconfig extending shared React preset, and Vite 8 config using @tailwindcss/vite plugin without PostCSS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Web app entry points — index.html, main.tsx, App.tsx, app.css health screens</name>
  <files>
    apps/web-storefront/index.html,
    apps/web-storefront/src/main.tsx,
    apps/web-storefront/src/App.tsx,
    apps/web-storefront/src/app.css,
    apps/web-storefront/.env.example,
    apps/web-admin/index.html,
    apps/web-admin/src/main.tsx,
    apps/web-admin/src/App.tsx,
    apps/web-admin/src/app.css,
    apps/web-admin/.env.example,
    apps/web-vendor/index.html,
    apps/web-vendor/src/main.tsx,
    apps/web-vendor/src/App.tsx,
    apps/web-vendor/src/app.css,
    apps/web-vendor/.env.example
  </files>
  <read_first>
    CLAUDE.md (React 19.2.x — ref as prop, no forwardRef needed; Framer Motion 12.x "motion/react" import; Tailwind CSS v4 @import "tailwindcss" syntax)
    .planning/phases/01-foundation/01-CONTEXT.md (D-07 per-app .env.example; D-08 vars documented with comments)
    packages/contracts/src/health.ts (HealthCheckResponse type for TypeScript assertion in App.tsx)
  </read_first>
  <action>
    Create index.html for each app with standard Vite HTML template. Set title to "Grovio Storefront" / "Grovio Admin" / "Grovio Vendor" respectively. Include <div id="root"> and <script type="module" src="/src/main.tsx">.

    Create src/main.tsx for each app: import React, ReactDOM from "react" and "react-dom/client"; import "./app.css"; import App from "./App". Call ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><App /></React.StrictMode>).

    Create src/app.css for each app using Tailwind v4 syntax:
    Line 1: @import "tailwindcss";
    Line 2 (placeholder until plan 01-07): @import "@grovio/ui/tokens" (this import will resolve once packages/ui is created in plan 01-07; wrap in a CSS comment noting "design tokens — populated by plan 01-07").

    Create src/App.tsx for each app with a minimal health screen component:
    - Storefront App.tsx: renders a full-screen centered div with Tailwind classes showing "Grovio Storefront", import.meta.env.VITE_API_URL display, and a status indicator. Import HealthCheckResponse from "@grovio/contracts" as a TypeScript type assertion only (const _check: HealthCheckResponse = { status: "ok", version: "0.1.0", timestamp: new Date().toISOString() }). Use motion.div from "motion/react" for a simple fade-in animation (opacity 0 to 1, 0.5s) to prove framer-motion works.
    - Admin App.tsx: same pattern, text "Grovio Admin Panel".
    - Vendor App.tsx: same pattern, text "Grovio Vendor Panel".

    Create .env.example for each app:
    - web-storefront/.env.example: VITE_API_URL (backend API base URL), VITE_PUBLIC_URL (public CDN/media URL), VITE_GOOGLE_MAPS_API_KEY (Google Places — Phase 4+)
    - web-admin/.env.example: VITE_API_URL, VITE_PUBLIC_URL
    - web-vendor/.env.example: VITE_API_URL, VITE_PUBLIC_URL
    Each var must have a # comment line above it explaining purpose and format per D-08.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && grep -l "@tailwindcss" apps/web-storefront/src/app.css apps/web-admin/src/app.css apps/web-vendor/src/app.css | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All three src/app.css files start with "@import \"tailwindcss\";" on line 1
    - All three src/App.tsx files import HealthCheckResponse from "@grovio/contracts"
    - All three src/App.tsx files import from "motion/react" (not "framer-motion" directly)
    - apps/web-storefront/src/App.tsx renders text containing "Grovio Storefront"
    - apps/web-admin/src/App.tsx renders text containing "Grovio Admin Panel"
    - apps/web-vendor/src/App.tsx renders text containing "Grovio Vendor Panel"
    - All three .env.example files contain VITE_API_URL with a comment line above it
    - All three index.html files have <div id="root"> and script type="module" pointing to /src/main.tsx
  </acceptance_criteria>
  <done>All three web apps have complete entry points; health screens render app-specific text with Framer Motion fade-in; Tailwind v4 CSS import is in place; @grovio/contracts type is used in each App.tsx to verify the import chain works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Vite dev server | Development only; no auth needed |
| Web apps → VITE_API_URL | Client-side env vars are public; never put secrets in VITE_* vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | VITE_* env vars are baked into client bundle | accept | VITE_* vars are intentionally public; .env.example comments explicitly state "do not put secrets here" |
| T-04-02 | Tampering | React 19.2.x CVE-2025-55182 | mitigate | All three apps pin react@>=19.2.3; enforced in package.json version ranges |
| T-04-SC | Tampering | npm installs (react, vite, tailwindcss, framer-motion, react-query, zustand) | mitigate | slopcheck + blocking checkpoint for [ASSUMED]/[SUS] packages before install |
</threat_model>

<verification>
After pnpm install from root:
1. `cd apps/web-storefront && pnpm typecheck` — exits 0
2. `cd apps/web-admin && pnpm typecheck` — exits 0
3. `cd apps/web-vendor && pnpm typecheck` — exits 0
4. `grep -r "@tailwindcss/vite" apps/web-storefront/vite.config.ts apps/web-admin/vite.config.ts apps/web-vendor/vite.config.ts` — all three match
5. `grep -r "postcss" apps/web-storefront/ apps/web-admin/ apps/web-vendor/` — zero matches (no PostCSS config)
6. Check each app/src/App.tsx imports from "motion/react" not "framer-motion"
</verification>

<success_criteria>
- All three web apps scaffold with Vite 8 + React 19 + TypeScript + Tailwind CSS v4
- Tailwind configured via @tailwindcss/vite plugin exclusively — no postcss.config.* files
- All three apps typecheck cleanly against @grovio/contracts and @grovio/config
- Each app has a health screen component with app-specific title and Framer Motion fade-in
- .env.example files cover VITE_API_URL with documented comments
- All three apps can run pnpm dev and serve on their designated ports (5173/5174/5175)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
