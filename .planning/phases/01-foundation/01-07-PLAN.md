---
phase: "01"
plan: "07"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-04"
files_modified:
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/src/tokens/tokens.css
  - packages/ui/src/tokens/index.ts
  - packages/ui/src/index.ts
  - apps/web-storefront/src/app.css
  - apps/web-admin/src/app.css
  - apps/web-vendor/src/app.css
autonomous: true
requirements:
  - FND-07

must_haves:
  truths:
    - "Changing --grovio-primary in packages/ui/src/tokens/tokens.css propagates visually to all three web apps after a Vite HMR cycle"
    - "All three web apps import the design token CSS file via @import in their app.css"
    - "Design tokens are defined as CSS custom properties in a Tailwind v4 @theme block"
    - "Token names are semantic (--grovio-primary, --grovio-surface, etc.) not visual (not --color-blue-500)"
    - "TypeScript token constants are exported from @grovio/ui for use in JS/TS code and React Native (Phase 7)"
  artifacts:
    - path: "packages/ui/src/tokens/tokens.css"
      provides: "Tailwind v4 @theme block with semantic CSS custom property tokens"
      contains: "--grovio-primary"
    - path: "packages/ui/src/tokens/index.ts"
      provides: "TypeScript token constants matching CSS custom properties"
      contains: "grovioPrimary"
    - path: "packages/ui/src/index.ts"
      provides: "Barrel export for @grovio/ui package"
  key_links:
    - from: "apps/web-storefront/src/app.css"
      to: "packages/ui/src/tokens/tokens.css"
      via: "@import url"
      pattern: "@import.*@grovio/ui"
    - from: "apps/web-admin/src/app.css"
      to: "packages/ui/src/tokens/tokens.css"
      via: "@import url"
      pattern: "@import.*@grovio/ui"
    - from: "apps/web-vendor/src/app.css"
      to: "packages/ui/src/tokens/tokens.css"
      via: "@import url"
      pattern: "@import.*@grovio/ui"
---

<objective>
Create the packages/ui package with design-token CSS (Tailwind v4 @theme block) and TypeScript token constants; wire the token import into all three web app CSS files so that changing one token file propagates across the entire web surface.

Purpose: FND-07 requires a single-place rebrand. The Tailwind v4 @theme directive in a shared CSS file is the correct mechanism: buyers change tokens in tokens.css and all three web apps pick up the changes via @import. This satisfies the "one-place rebrand" goal without requiring a tailwind.config.js in each app.

Output: packages/ui/src/tokens/tokens.css with Tailwind v4 @theme block containing semantic tokens; TypeScript constants for React Native use; all three web apps import the token file and use Tailwind semantic token classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@CLAUDE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: packages/ui scaffold and design-token definitions</name>
  <files>
    packages/ui/package.json,
    packages/ui/tsconfig.json,
    packages/ui/src/tokens/tokens.css,
    packages/ui/src/tokens/index.ts,
    packages/ui/src/index.ts
  </files>
  <read_first>
    CLAUDE.md (Tailwind CSS 4.3.x — @theme directive, CSS custom properties, no tailwind.config.js needed; @tailwindcss/vite is the Vite plugin; packages/ui/src/tokens/ is the canonical location per CLAUDE.md)
    .planning/phases/01-foundation/01-CONTEXT.md (FND-07: one-place rebrand; Claude's Discretion: Tailwind v4 @theme vs CSS custom properties)
  </read_first>
  <action>
    Create packages/ui/package.json: name "@grovio/ui", version "0.1.0", private false (shared). type "module". Exports: ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }, "./tokens": { "style": "./src/tokens/tokens.css" } — the CSS export uses the "style" condition so bundlers and PostCSS-aware tools can resolve it. devDependencies: typescript@5.8.x, tsup@8.x, @grovio/config@workspace:*. Scripts: "build": "tsup src/index.ts --dts --format esm", "typecheck": "tsc --noEmit".

    Create packages/ui/tsconfig.json extending "@grovio/config/tsconfig/react.json". Include ["src/**/*"]. OutDir "dist".

    Create packages/ui/src/tokens/tokens.css with a Tailwind v4 @theme block. The @theme block declares CSS custom properties that become Tailwind utility classes automatically in v4. Define the following semantic tokens (these are the starter defaults buyers restyle):

    @theme {
      --color-grovio-primary: oklch(55% 0.2 250);       /* Primary brand color — buyer replaces this */
      --color-grovio-primary-hover: oklch(48% 0.2 250);  /* Hover state for primary */
      --color-grovio-secondary: oklch(65% 0.15 190);     /* Secondary/accent color */
      --color-grovio-surface: oklch(98% 0.005 250);      /* Card/panel background */
      --color-grovio-surface-raised: oklch(100% 0 0);    /* Elevated surface (modal, dropdown) */
      --color-grovio-border: oklch(90% 0.01 250);        /* Default border */
      --color-grovio-text: oklch(20% 0.02 250);          /* Primary text */
      --color-grovio-text-muted: oklch(50% 0.02 250);    /* Secondary/muted text */
      --color-grovio-success: oklch(60% 0.15 145);       /* Success state */
      --color-grovio-error: oklch(55% 0.2 30);           /* Error/danger state */
      --color-grovio-warning: oklch(70% 0.15 80);        /* Warning state */
      --font-sans: "Inter", ui-sans-serif, system-ui, sans-serif;
      --radius-sm: 0.375rem;
      --radius-md: 0.5rem;
      --radius-lg: 0.75rem;
      --radius-xl: 1rem;
    }

    Use OKLCH color space — it is perceptually uniform and Tailwind v4's native color format. Add a comment block at the top of tokens.css explaining: "Grovio Design Tokens — buyers change these values to rebrand the entire marketplace. All three web apps import this file. OKLCH values: L=lightness (0-1), C=chroma, H=hue (0-360)."

    Create packages/ui/src/tokens/index.ts: export TypeScript constants matching the CSS tokens for use in React Native (Phase 7) and any JS context. Use camelCase names:
    export const tokens = {
      colors: {
        grovioPrimary: "oklch(55% 0.2 250)",
        grovioSecondary: "oklch(65% 0.15 190)",
        grovioSurface: "oklch(98% 0.005 250)",
        grovioText: "oklch(20% 0.02 250)",
        grovioTextMuted: "oklch(50% 0.02 250)",
        grovioSuccess: "oklch(60% 0.15 145)",
        grovioError: "oklch(55% 0.2 30)",
        grovioWarning: "oklch(70% 0.15 80)",
      },
      fontFamily: { sans: "'Inter', ui-sans-serif, system-ui, sans-serif" },
      borderRadius: { sm: "0.375rem", md: "0.5rem", lg: "0.75rem", xl: "1rem" },
    } as const;
    export type Tokens = typeof tokens;

    Create packages/ui/src/index.ts: export { tokens } from "./tokens/index"; export type { Tokens } from "./tokens/index". Note: No CSS re-export from index.ts — CSS is imported directly via the "./tokens" style export path.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio/packages/ui" && node -e "const p=require('./package.json'); console.log(p.name, p.exports['./tokens'] ? 'tokens-export-ok' : 'missing-tokens-export')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/package.json name equals "@grovio/ui"
    - packages/ui/package.json exports["./tokens"] has "style" field pointing to "./src/tokens/tokens.css"
    - packages/ui/src/tokens/tokens.css contains "@theme {" block
    - packages/ui/src/tokens/tokens.css contains "--color-grovio-primary:" definition using OKLCH
    - packages/ui/src/tokens/tokens.css contains a buyer-facing comment explaining how to rebrand
    - packages/ui/src/tokens/index.ts exports const tokens with colors.grovioPrimary
    - packages/ui/src/index.ts exports tokens
  </acceptance_criteria>
  <done>packages/ui package defined with @theme block CSS tokens in OKLCH color space and TypeScript constants; CSS accessible via "@grovio/ui/tokens" style export path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire token import into all three web apps</name>
  <files>
    apps/web-storefront/src/app.css,
    apps/web-admin/src/app.css,
    apps/web-vendor/src/app.css
  </files>
  <read_first>
    packages/ui/src/tokens/tokens.css (the file being imported — verify its @theme block)
    apps/web-storefront/src/app.css (created in plan 01-04 with placeholder comment — replace comment with real import)
    apps/web-admin/src/app.css (same)
    apps/web-vendor/src/app.css (same)
    CLAUDE.md (Tailwind v4: @import "tailwindcss" MUST come before @import of custom theme files; @tailwindcss/vite handles the resolution)
  </read_first>
  <action>
    Update all three app.css files. Each file must have exactly these two lines in this order:

    Line 1: @import "tailwindcss";
    Line 2: @import "@grovio/ui/tokens";

    The @import "tailwindcss" MUST appear before the token import so Tailwind v4 processes the @theme block from the token file correctly. The @tailwindcss/vite plugin resolves the "tailwindcss" specifier natively; the workspace package reference "@grovio/ui/tokens" is resolved by Vite's resolver which honors pnpm workspace symlinks.

    Remove the placeholder comment that was written in plan 01-04 (the "design tokens — populated by plan 01-07" comment) and replace with the actual imports.

    After the two imports, web apps may add any app-specific CSS custom properties or @layer overrides below the imports. For now, the files contain only the two import lines.

    Also update each App.tsx health screen to demonstrate a token-using Tailwind class: add bg-grovio-primary text-white to one element in each App.tsx health screen. This proves the token resolves to a Tailwind utility class. Tailwind v4's @theme block makes --color-grovio-primary available as the bg-grovio-primary utility automatically.

    Files to update for App.tsx: apps/web-storefront/src/App.tsx, apps/web-admin/src/App.tsx, apps/web-vendor/src/App.tsx (minor update — add className="bg-grovio-primary text-white" to one element).
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && grep -c "@import \"@grovio/ui/tokens\"" apps/web-storefront/src/app.css apps/web-admin/src/app.css apps/web-vendor/src/app.css</automated>
  </verify>
  <acceptance_criteria>
    - apps/web-storefront/src/app.css line 1 is exactly: @import "tailwindcss";
    - apps/web-storefront/src/app.css line 2 is exactly: @import "@grovio/ui/tokens";
    - apps/web-admin/src/app.css has the same two-line pattern
    - apps/web-vendor/src/app.css has the same two-line pattern
    - grep "@import \"@grovio/ui/tokens\"" in all three app.css files returns 3 matches
    - apps/web-storefront/src/App.tsx contains className="bg-grovio-primary (or contains "bg-grovio-primary" as a class)
    - The placeholder comment from plan 01-04 is removed from all three app.css files
  </acceptance_criteria>
  <done>All three web app CSS files import Tailwind v4 then the @grovio/ui token file; changing tokens.css propagates to all three apps via the shared import chain; token-using Tailwind classes render in health screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS import chain → Tailwind v4 compiler | CSS @theme tokens are build-time; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | OKLCH color values visible in source | accept | Design tokens are intentionally buyer-visible; not sensitive |
| T-07-02 | Tampering | @import order affects Tailwind v4 processing | mitigate | Document and enforce: @import "tailwindcss" MUST come before @import "@grovio/ui/tokens"; verified by acceptance criteria |
</threat_model>

<verification>
1. `grep -n "@import" apps/web-storefront/src/app.css` — line 1 is tailwindcss, line 2 is @grovio/ui/tokens
2. `grep -n "@import" apps/web-admin/src/app.css` — same pattern
3. `grep -n "@import" apps/web-vendor/src/app.css` — same pattern
4. `cat packages/ui/src/tokens/tokens.css | grep "@theme"` — confirms @theme block exists
5. `cat packages/ui/src/tokens/tokens.css | grep "grovio-primary"` — confirms semantic token defined
6. Run `pnpm dev` in any web app and visit localhost — bg-grovio-primary class renders with OKLCH primary color
7. Change --color-grovio-primary value in tokens.css → Vite HMR picks up the change instantly in all running dev servers
</verification>

<success_criteria>
- packages/ui/src/tokens/tokens.css has @theme block with semantic OKLCH tokens
- Buyer-facing comment in tokens.css explains how to rebrand
- All three web app CSS files have @import "tailwindcss" then @import "@grovio/ui/tokens"
- Changing tokens.css propagates visually to all three apps via HMR
- TypeScript token constants in packages/ui/src/tokens/index.ts for React Native Phase 7 use
- pnpm typecheck passes for packages/ui and all three web apps
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-07-SUMMARY.md` when done.
</output>
