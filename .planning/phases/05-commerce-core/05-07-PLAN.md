---
phase: 05-commerce-core
plan: 07
type: execute
wave: 5
depends_on: ["05-04", "05-02"]
files_modified:
  - apps/api/src/modules/payments/PaymentProvider.ts
  - apps/api/src/modules/payments/StripeAdapter.ts
  - apps/api/src/modules/payments/RazorpayAdapter.ts
  - apps/api/src/modules/payments/PaymentService.ts
  - apps/api/src/modules/payments/index.ts
  - apps/api/src/modules/payments/__tests__/PaymentService.test.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04]
must_haves:
  truths:
    - "All payment business logic depends only on the PaymentProvider interface — stripe/razorpay SDKs imported ONLY in their adapters (PAY-01)"
    - "PaymentService selects the active provider(s) based on configured env keys (PAY-02, D-09)"
    - "Each adapter creates a provider payment order server-side and parses+verifies its own webhook signature (PAY-03, PAY-04)"
    - "Stripe webhook uses constructEvent (raw body); Razorpay uses HMAC-SHA256 timingSafeEqual"
  artifacts:
    - path: "apps/api/src/modules/payments/PaymentProvider.ts"
      provides: "PaymentProvider interface + canonical WebhookEvent types"
      contains: "interface PaymentProvider"
    - path: "apps/api/src/modules/payments/StripeAdapter.ts"
      provides: "Stripe SDK adapter (only file importing stripe)"
      contains: "import Stripe"
    - path: "apps/api/src/modules/payments/PaymentService.ts"
      provides: "provider selection + abstraction-only orchestration"
      contains: "getEnabledProviders"
  key_links:
    - from: "apps/api/src/modules/payments/PaymentService.ts"
      to: "PaymentProvider interface"
      via: "depends on interface, not SDK"
      pattern: "PaymentProvider"
---

<objective>
Build the PaymentProvider abstraction (PAY-01): the interface + canonical webhook event types, the StripeAdapter and RazorpayAdapter (the ONLY two files allowed to import the SDKs), and PaymentService which selects the enabled provider(s) by config (PAY-02, D-09) and delegates create-order / webhook-parse / refund through the interface.

Purpose: This is the hard architectural boundary from Pitfall 9 — checkout/order code must never import a payment SDK. The adapters encapsulate Stripe PaymentIntent creation (3DS-ready) and Razorpay order creation (Pitfall 4: order_id required), plus each provider's webhook signature verification.
Output: 4 payment module files + index + PaymentService tests. No analog exists (PATTERNS "No Analog Found") — built from RESEARCH Patterns 1, 4, 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-commerce-core/05-CONTEXT.md
@.planning/phases/05-commerce-core/05-PATTERNS.md
@.planning/phases/05-commerce-core/05-RESEARCH.md
@apps/api/src/config/env.ts
@packages/contracts/src/payments/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaymentProvider interface + Stripe & Razorpay adapters (PAY-01)</name>
  <files>apps/api/src/modules/payments/PaymentProvider.ts, apps/api/src/modules/payments/StripeAdapter.ts, apps/api/src/modules/payments/RazorpayAdapter.ts, apps/api/src/modules/payments/index.ts</files>
  <read_first>
    - .planning/phases/05-commerce-core/05-PATTERNS.md (PaymentProvider.ts interface — exact shape; CRITICAL no-SDK-import rule)
    - .planning/phases/05-commerce-core/05-RESEARCH.md (Pattern 1 interface, Pattern 4 Stripe constructEvent + raw body, Pattern 5 Razorpay HMAC, Pitfall 4 order_id required, Pitfall 8 providerOrderId)
    - apps/api/src/config/env.ts (STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, RAZORPAY_KEY_ID/SECRET/WEBHOOK_SECRET)
    - packages/contracts/src/payments/types.ts (ProviderPaymentOrder shape to align with)
  </read_first>
  <action>
    Create `PaymentProvider.ts` with the interface from RESEARCH Pattern 1: `CreatePaymentOrderParams`, `ProviderPaymentOrder`, `WebhookEvent` (canonical type union PAYMENT_CAPTURED/PAYMENT_FAILED/REFUND_SUCCEEDED/REFUND_FAILED), and `interface PaymentProvider { createPaymentOrder, handleWebhook, initiateRefund }`. No SDK import here.
    Create `StripeAdapter.ts`: `import Stripe from "stripe"` (the ONLY backend file besides this allowed to). Constructor takes {secretKey, webhookSecret}. `createPaymentOrder` calls `stripe.paymentIntents.create` (amount, currency, metadata.orderId) and returns clientSecret + providerOrderId (the PaymentIntent id). `handleWebhook(rawBody, signature)` calls `stripe.webhooks.constructEvent(rawBody, signature, webhookSecret)` and maps payment_intent.succeeded→PAYMENT_CAPTURED, payment_intent.payment_failed→PAYMENT_FAILED, charge.refunded→REFUND_SUCCEEDED, extracting orderId from metadata and providerEventId from event.id. `initiateRefund` calls `stripe.refunds.create`.
    Create `RazorpayAdapter.ts`: `import Razorpay from "razorpay"`. Constructor takes {keyId, keySecret, webhookSecret}. `createPaymentOrder` calls `razorpay.orders.create` (amount, currency, notes.orderId) — MUST return order_id as providerOrderRef (Pitfall 4) and keyId as providerKey for the frontend modal. `handleWebhook(rawBody, signature)` verifies via `crypto.createHmac('sha256', webhookSecret).update(rawBody).digest('hex')` compared with `crypto.timingSafeEqual` (Pattern 5), then maps payment.captured→PAYMENT_CAPTURED, payment.failed→PAYMENT_FAILED, refund.processed→REFUND_SUCCEEDED. `initiateRefund` calls `razorpay.payments.refund`.
    Create `index.ts` exporting PaymentProvider types, both adapters, and (after task 2) PaymentService.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PaymentProvider.ts declares `interface PaymentProvider` with `createPaymentOrder`, `handleWebhook`, `initiateRefund` and does NOT import stripe or razorpay
    - StripeAdapter.ts is the only file importing `stripe`; uses `stripe.webhooks.constructEvent` (not manual HMAC) and `stripe.paymentIntents.create`
    - RazorpayAdapter.ts imports `razorpay`, uses `crypto.timingSafeEqual` for signature check, and returns order_id from razorpay.orders.create (Pitfall 4)
    - Both adapters return the canonical `WebhookEvent` union from handleWebhook
    - `pnpm --filter @grovio/api typecheck` exits 0
  </acceptance_criteria>
  <done>PaymentProvider interface + both SDK adapters compile; SDK imports are confined to the two adapter files.</done>
</task>

<task type="auto">
  <name>Task 2: PaymentService provider selection + abstraction-only orchestration (PAY-02, PAY-03)</name>
  <files>apps/api/src/modules/payments/PaymentService.ts, apps/api/src/modules/payments/index.ts, apps/api/src/modules/payments/__tests__/PaymentService.test.ts</files>
  <read_first>
    - apps/api/src/modules/payments/PaymentProvider.ts (interface PaymentService depends on)
    - apps/api/src/config/env.ts (which keys are set determines enabled providers, PAY-02)
    - .planning/phases/05-commerce-core/05-RESEARCH.md (Pattern 4 onConflictDoNothing payment_events upsert — note PaymentService does NOT touch raw body; the route handler does)
    - packages/contracts/src/payments/types.ts (EnabledProviders shape)
  </read_first>
  <action>
    Create `PaymentService.ts`. Deps {env, db, logger}. It lazily constructs adapters from env keys: a StripeAdapter when STRIPE_SECRET_KEY+STRIPE_WEBHOOK_SECRET are set, a RazorpayAdapter when RAZORPAY_KEY_ID+KEY_SECRET+WEBHOOK_SECRET are set. Methods: `getEnabledProviders()` → {stripe: boolean, razorpay: boolean} (D-09, PAY-02); `getProvider(id: 'stripe'|'razorpay')` → the matching PaymentProvider or throws ProviderNotConfiguredError; `createPaymentOrder(provider, params)` → delegates to the adapter; `recordWebhookEvent(provider, event)` → idempotency upsert into payment_events via `onConflictDoNothing({ target: [paymentEvents.provider, paymentEvents.providerEventId] })`, returning whether the event was newly inserted (false = duplicate, PAY-03). PaymentService imports ONLY PaymentProvider + the two adapter classes — never `stripe`/`razorpay` directly. Domain error ProviderNotConfiguredError. Update index.ts to export PaymentService.
    Write `PaymentService.test.ts`: getEnabledProviders reflects which env keys are present (mock env with only Stripe → {stripe:true, razorpay:false}); getProvider throws when a provider's keys are absent; recordWebhookEvent returns true on first insert and false on duplicate (mock db onConflictDoNothing returning [] on conflict). Assert PaymentService.ts source does not import stripe/razorpay.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api test --run src/modules/payments</automated>
  </verify>
  <acceptance_criteria>
    - PaymentService.ts exports `class PaymentService` with `getEnabledProviders`, `getProvider`, `createPaymentOrder`, `recordWebhookEvent`
    - PaymentService.ts does NOT import `stripe` or `razorpay` (only the adapter classes + PaymentProvider interface)
    - recordWebhookEvent test asserts true on first insert, false on duplicate (D-10/PAY-03 idempotency)
    - getEnabledProviders test asserts the result reflects configured env keys (PAY-02)
    - `pnpm --filter @grovio/api test --run src/modules/payments` exits 0
  </acceptance_criteria>
  <done>PaymentService selects enabled providers, delegates through the interface, and performs idempotent webhook-event recording, all tested with no SDK leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment provider → API (webhook) | Untrusted inbound webhook claiming a payment event. |
| API → payment provider | Server-side order creation requires secret keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Spoofing | webhook signature verification | mitigate | StripeAdapter uses constructEvent (HMAC + timestamp tolerance); RazorpayAdapter uses crypto.timingSafeEqual HMAC-SHA256. Verification is performed inside the adapter before any canonical event is emitted. |
| T-05-02 | Tampering | payment_events idempotency | mitigate | PaymentService.recordWebhookEvent uses onConflictDoNothing on (provider, provider_event_id); duplicate delivery returns false → handler exits 200 without re-processing (PAY-03). |
| T-05-PAY | Information Disclosure | provider SDK coupling | mitigate | SDK imports confined to StripeAdapter/RazorpayAdapter; secret keys never reach business logic (PAY-01, Pitfall 9). |
| T-05-SC | Tampering | stripe/razorpay packages | mitigate | Packages verified in plan 05-02 legitimacy checkpoint before this plan runs. |
</threat_model>

<verification>
- `pnpm --filter @grovio/api test --run src/modules/payments` green.
- Grep enforces the abstraction: `grep -rln "from \"stripe\"\|from 'stripe'\|require(\"stripe\")" apps/api/src` returns ONLY StripeAdapter.ts; same for razorpay → only RazorpayAdapter.ts.
- `pnpm --filter @grovio/api typecheck` passes.
</verification>

<success_criteria>
PaymentProvider abstraction complete: interface + two SDK adapters (SDK imports confined) + PaymentService with config-based provider selection and idempotent webhook recording, covering PAY-01..04.
</success_criteria>

<output>
Create `.planning/phases/05-commerce-core/05-07-SUMMARY.md` when done
</output>
