---
phase: 05-commerce-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/web-storefront/package.json
  - pnpm-lock.yaml
autonomous: false
requirements: [PAY-01, PAY-02, PAY-04]
user_setup:
  - service: stripe
    why: "Server-side PaymentIntent creation + webhook verification (PAY-04)"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard → Developers → API keys (test mode)"
      - name: STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard → Developers → API keys (test mode)"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard → Developers → Webhooks → signing secret"
  - service: razorpay
    why: "Server-side order creation + HMAC webhook verification (PAY-04)"
    env_vars:
      - name: RAZORPAY_KEY_ID
        source: "Razorpay Dashboard → Settings → API Keys (test mode)"
      - name: RAZORPAY_KEY_SECRET
        source: "Razorpay Dashboard → Settings → API Keys (test mode)"
      - name: RAZORPAY_WEBHOOK_SECRET
        source: "Razorpay Dashboard → Settings → Webhooks → secret"
must_haves:
  truths:
    - "stripe and razorpay SDKs are installed in apps/api"
    - "@stripe/stripe-js and @stripe/react-stripe-js are installed in apps/web-storefront"
    - "Each package was verified legitimate via a blocking human checkpoint before install"
  artifacts:
    - path: "apps/api/package.json"
      provides: "stripe + razorpay dependencies"
      contains: "stripe"
    - path: "apps/web-storefront/package.json"
      provides: "@stripe/stripe-js + @stripe/react-stripe-js dependencies"
      contains: "@stripe/react-stripe-js"
  key_links:
    - from: "apps/api/package.json"
      to: "node_modules/stripe + node_modules/razorpay"
      via: "pnpm install"
      pattern: "\"stripe\""
---

<objective>
Install the four new Phase 5 payment packages behind a blocking legitimacy checkpoint: `stripe` + `razorpay` (backend SDKs, used only in infrastructure adapters per PAY-01), and `@stripe/stripe-js` + `@stripe/react-stripe-js` (storefront Stripe Elements).

Purpose: All other packages Phase 5 needs (drizzle, bullmq, react-query, motion) are already installed. These four are new and require external accounts. Per project convention (Phases 2-4), every new package install passes a `checkpoint:human-verify` legitimacy gate first.
Output: Updated package.json files + lockfile with the four verified packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-commerce-core/05-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (blocking)</name>
  <what-built>
    No code yet — this gate verifies the four new Phase 5 packages before install. Per RESEARCH.md Package Legitimacy Audit, slopcheck was unavailable, so all four are tagged [VERIFIED: npm registry] but still require a manual human verification per protocol.
  </what-built>
  <how-to-verify>
    Confirm each package is the official SDK by checking its npm page:
    1. https://www.npmjs.com/package/stripe — publisher is Stripe, repo github.com/stripe/stripe-node, version 22.2.x, ~12M weekly downloads
    2. https://www.npmjs.com/package/razorpay — publisher is Razorpay, repo github.com/razorpay/razorpay-node, version 2.9.6
    3. https://www.npmjs.com/package/@stripe/stripe-js — publisher Stripe, repo github.com/stripe/stripe-js, version 9.7.x
    4. https://www.npmjs.com/package/@stripe/react-stripe-js — publisher Stripe, repo github.com/stripe/react-stripe-js, version 6.5.x
    Confirm none are typosquats (exact names: stripe, razorpay, @stripe/stripe-js, @stripe/react-stripe-js).
  </how-to-verify>
  <verify>
    <automated>MISSING — human verification gate, no automated check</automated>
  </verify>
  <acceptance_criteria>
    - Human has confirmed all four packages are official Stripe/Razorpay SDKs and approved install
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or name any package to reject</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install verified packages</name>
  <files>apps/api/package.json, apps/web-storefront/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/api/package.json (current dependency list + existing pinning style)
    - apps/web-storefront/package.json (current dependency list)
    - .planning/phases/05-commerce-core/05-RESEARCH.md (Installation section — exact pnpm commands and versions)
  </read_first>
  <action>
    Run `pnpm --filter @grovio/api add stripe@22.2.0 razorpay@2.9.6` to add both backend SDKs. Run `pnpm --filter @grovio/web-storefront add @stripe/stripe-js@9.7.0 @stripe/react-stripe-js@6.5.0` to add both storefront packages. Do NOT add any Razorpay frontend package — Razorpay checkout loads from the CDN script `https://checkout.razorpay.com/v1/checkout.js` (no npm package exists; per RESEARCH.md). Verify the lockfile updated and typecheck still passes.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api typecheck && pnpm --filter @grovio/web-storefront typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/package.json dependencies include `stripe` and `razorpay`
    - apps/web-storefront/package.json dependencies include `@stripe/stripe-js` and `@stripe/react-stripe-js`
    - No razorpay frontend package was added to apps/web-storefront/package.json
    - `pnpm --filter @grovio/api typecheck` exits 0
    - `pnpm --filter @grovio/web-storefront typecheck` exits 0
  </acceptance_criteria>
  <done>All four packages installed and both workspaces typecheck cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party package code enters the build; supply-chain risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npm installs (stripe, razorpay, @stripe/*) | mitigate | Blocking human-verify checkpoint confirms each package is the official SDK (publisher, repo, download count) before install. Exact package names pinned to versions in RESEARCH.md. |
</threat_model>

<verification>
- Both workspaces typecheck after install.
- `grep -c "razorpay" apps/web-storefront/package.json` returns 0 (no frontend Razorpay package).
</verification>

<success_criteria>
Four official payment packages installed (backend: stripe, razorpay; storefront: @stripe/stripe-js, @stripe/react-stripe-js), each verified before install, both workspaces typecheck.
</success_criteria>

<output>
Create `.planning/phases/05-commerce-core/05-02-SUMMARY.md` when done
</output>
