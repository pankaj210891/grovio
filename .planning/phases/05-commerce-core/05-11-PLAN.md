---
phase: 05-commerce-core
plan: 11
type: execute
wave: 8
depends_on: ["05-10", "05-01", "05-02"]
files_modified:
  - apps/web-storefront/src/hooks/useBasket.ts
  - apps/web-storefront/src/components/basket/BasketItem.tsx
  - apps/web-storefront/src/components/basket/OrderSummary.tsx
  - apps/web-storefront/src/pages/CartPage.tsx
  - apps/web-storefront/src/pages/ProductDetailPage.tsx
  - apps/web-storefront/src/components/layout/Header.tsx
  - apps/web-storefront/src/router.tsx
autonomous: false
requirements: [CHK-01, CHK-02, STORE-05]
must_haves:
  truths:
    - "useBasket provides React Query basket state + optimistic add/update/remove/merge mutations (D-04)"
    - "The PDP Add to Cart button is wired to addToBasket and variant selectors are enabled (activates Phase 4 D-13/D-15)"
    - "CartPage groups items by vendor with an order summary sidebar (D-24, D-25)"
    - "Header shows a live basket item count from the basket query"
  artifacts:
    - path: "apps/web-storefront/src/hooks/useBasket.ts"
      provides: "basket query + mutations"
      contains: "useAddToBasket"
    - path: "apps/web-storefront/src/pages/CartPage.tsx"
      provides: "vendor-grouped cart + summary"
      contains: "OrderSummary"
  key_links:
    - from: "apps/web-storefront/src/pages/ProductDetailPage.tsx"
      to: "useBasket addToBasket"
      via: "onClick mutation (replaces data-phase=5)"
      pattern: "addToBasket|addItem"
    - from: "apps/web-storefront/src/router.tsx"
      to: "CartPage"
      via: "route /cart"
      pattern: "CartPage"
---

<objective>
Build the storefront basket experience: the `useBasket` React Query hook (D-04, server-authoritative, optimistic mutations), the CartPage (vendor-grouped items D-24 + order summary sidebar D-25), and activate Phase 4's deferred Add to Cart button + variant selectors on the PDP (D-13/D-15). Header gets a live basket count.

Purpose: This is the first customer-visible Phase 5 surface and unblocks the checkout flow (05-12). Basket state is React Query only — no Zustand store (D-04). The PDP wiring removes the `data-phase="5"` placeholder added in Phase 4.
Output: useBasket hook + basket components + CartPage + PDP/Header wiring + /cart route. Ends with a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-commerce-core/05-CONTEXT.md
@.planning/phases/05-commerce-core/05-PATTERNS.md
@apps/web-storefront/src/hooks/useAuth.ts
@apps/web-storefront/src/pages/account/AddressesPage.tsx
@apps/web-storefront/src/pages/ProductDetailPage.tsx
@apps/web-storefront/src/components/layout/Header.tsx
@apps/web-storefront/src/router.tsx
@apps/web-storefront/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useBasket hook + basket components + CartPage + /cart route</name>
  <files>apps/web-storefront/src/hooks/useBasket.ts, apps/web-storefront/src/components/basket/BasketItem.tsx, apps/web-storefront/src/components/basket/OrderSummary.tsx, apps/web-storefront/src/pages/CartPage.tsx, apps/web-storefront/src/router.tsx</files>
  <read_first>
    - apps/web-storefront/src/hooks/useAuth.ts (useQuery session probe + useMutation pattern — exact analog)
    - apps/web-storefront/src/pages/account/AddressesPage.tsx (loading/error/empty states, AnimatePresence list, toast, PageTransition)
    - apps/web-storefront/src/lib/api-client.ts (apiClient + ApiError, credentials:'include')
    - apps/web-storefront/src/router.tsx (createBrowserRouter children — where /cart is added)
    - .planning/phases/05-commerce-core/05-PATTERNS.md (useBasket.ts + CartPage.tsx sections; D-24/D-25)
    - packages/contracts/src/basket/types.ts (Basket, BasketItem shapes)
  </read_first>
  <action>
    Create `useBasket.ts` per PATTERNS.md: a useQuery(['basket']) reading GET /basket (returns null on 404), plus useMutation hooks `useAddToBasket` (POST /basket/items, optimistic itemCount bump per RESEARCH Pattern 8), `useUpdateBasketItem` (PATCH /basket/items/:id), `useRemoveBasketItem` (DELETE), and `useMergeBasket` (POST /basket/merge). Every mutation invalidates ['basket'] onSettled. Export an `itemCount` convenience.
    Create `BasketItem.tsx`: renders one line (image, name, variant label, unit price, quantity stepper, remove button) using the Button/Skeleton UI kit; wraps in motion.div with exit collapse for removal (PATTERNS AnimatePresence pattern).
    Create `OrderSummary.tsx` (D-25): subtotal, coupon code input (disabled/hidden when feature flag off — read GET /checkout/providers or a flags endpoint; if not available, render the input but show "coupons unavailable" gracefully), wallet credit toggle + balance display (from useWallet — created in 05-12; for this plan render the toggle as a placeholder reading balance via a lightweight GET /account/wallet call or omit until 05-12 and add a TODO marker referencing 05-12), delivery estimate, order total, and a "Proceed to checkout" button navigating to /checkout/address.
    Create `CartPage.tsx` (D-24): groups basket.groupedByVendor into labeled vendor sections with per-vendor delivery estimate headers, renders BasketItem rows with AnimatePresence, shows OrderSummary in a right sidebar (desktop) / bottom (mobile), loading skeletons + empty state + error state per the AddressesPage analog, wrapped in PageTransition.
    Update `router.tsx`: add a `{ path: 'cart', element: <CartPage /> }` child of the root AppLayout route and import CartPage.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/web-storefront typecheck && pnpm --filter @grovio/web-storefront build</automated>
  </verify>
  <acceptance_criteria>
    - useBasket.ts exports a basket query hook and `useAddToBasket`, `useUpdateBasketItem`, `useRemoveBasketItem`, `useMergeBasket`, each invalidating ['basket'] onSettled
    - CartPage.tsx renders items grouped by vendor (D-24) and an OrderSummary sidebar (D-25)
    - router.tsx contains a `/cart` route rendering CartPage
    - useBasket mutations use apiClient (credentials:'include') — no direct fetch
    - `pnpm --filter @grovio/web-storefront typecheck` and `build` exit 0
  </acceptance_criteria>
  <done>useBasket hook, basket components, vendor-grouped CartPage, and the /cart route compile and build.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PDP Add to Cart + variant selectors + Header basket count</name>
  <files>apps/web-storefront/src/pages/ProductDetailPage.tsx, apps/web-storefront/src/components/layout/Header.tsx</files>
  <read_first>
    - apps/web-storefront/src/pages/ProductDetailPage.tsx (lines around the data-phase="5" button at ~299 + variant selector block at ~269; D-13/D-15 markers)
    - apps/web-storefront/src/components/layout/Header.tsx (where a cart icon/count goes)
    - apps/web-storefront/src/hooks/useBasket.ts (addToBasket + itemCount)
  </read_first>
  <action>
    Edit `ProductDetailPage.tsx`: replace the disabled Add to Cart button — remove `data-phase="5"`, the disabled attribute, the shake handler, and the "available in a future update" aria-label. Wire `onClick` to `useAddToBasket().mutate({ productId, productVariantId: selectedVariantId ?? null, quantity })`. Enable the variant selector pill buttons (remove opacity-60/disabled from D-15); track the selected variant in state and resolve the matching variant id for the add-to-cart call; require a variant selection before enabling Add to Cart when the product has variants. On success show a toast ("Added to cart") and keep the user on the page. Preserve all Phase 4 layout/specifications/animation structure.
    Edit `Header.tsx`: add a cart icon link to /cart showing the live `itemCount` from useBasket (badge); hide the badge when count is 0.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/web-storefront typecheck && pnpm --filter @grovio/web-storefront build</automated>
  </verify>
  <acceptance_criteria>
    - ProductDetailPage.tsx no longer contains `data-phase="5"` on the Add to Cart button (grep returns 0)
    - The Add to Cart onClick calls the useAddToBasket mutation with productId, productVariantId, quantity
    - Variant selector pills are interactive (no disabled/opacity-60) and a variant is required before adding when variants exist
    - Header.tsx renders a /cart link with a live basket item-count badge that hides at 0
    - `pnpm --filter @grovio/web-storefront typecheck` and `build` exit 0
  </acceptance_criteria>
  <done>PDP Add to Cart adds real basket items, variant selection is functional, and the header shows a live cart count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify basket flow in the browser</name>
  <what-built>
    A working storefront basket: enabled PDP Add to Cart + variant selection, a live header cart badge, and a vendor-grouped CartPage with an order summary sidebar. Backend basket API (05-10) persists items via the grovio_basket_token cookie.
  </what-built>
  <how-to-verify>
    Start the storefront + API (`pnpm dev` or the documented dev command). Then:
    1. Open a product detail page with variants. Confirm variant pills are clickable and Add to Cart is disabled until a variant is selected.
    2. Select a variant, click Add to Cart — confirm a success toast and the header cart badge increments.
    3. Click the header cart icon → /cart. Confirm items are grouped by vendor with section headers and an order summary sidebar showing subtotal.
    4. Change a quantity and remove an item — confirm the summary updates and the remove animates (collapse).
    5. Refresh the page — confirm the cart persists (server-side basket via cookie).
    6. As a guest add an item, then log in — confirm the guest item is still present (merge, CHK-02).
  </how-to-verify>
  <verify>
    <automated>MISSING — visual/interactive browser verification</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms add-to-cart, variant selection, vendor grouping, summary updates, persistence across refresh, and guest-merge-on-login all work
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → basket API | Basket mutations carry the grovio_basket_token cookie. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | basket cookie | mitigate | All basket calls use apiClient credentials:'include'; the grovio_basket_token cookie is httpOnly (set server-side, 05-10) — not readable by JS, preventing XSS theft. |
| T-05-UI | Tampering | client-side prices | mitigate | CartPage displays prices for information only; all authoritative totals are recomputed server-side at checkout (CHK-04, plan 05-09). |
</threat_model>

<verification>
- `pnpm --filter @grovio/web-storefront typecheck && build` green.
- `grep -c "data-phase=\"5\"" apps/web-storefront/src/pages/ProductDetailPage.tsx` returns 0.
- Human checkpoint confirms the end-to-end basket flow including guest merge.
</verification>

<success_criteria>
A customer can add variant products to a server-side basket from the PDP, see a live header count, and manage a vendor-grouped cart with an order summary — verified in the browser, covering CHK-01, CHK-02, STORE-05.
</success_criteria>

<output>
Create `.planning/phases/05-commerce-core/05-11-SUMMARY.md` when done
</output>
