---
phase: 05-commerce-core
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - apps/api/drizzle/
  - apps/api/src/db/seed-commerce.ts
autonomous: true
requirements: [CHK-05, CHK-06, MKT-01, WAL-01]
must_haves:
  truths:
    - "A Drizzle migration is generated for all 13 new Phase 5 tables + customers.wallet_balance_minor"
    - "The migration is applied to the database successfully (db:migrate exits 0)"
    - "A COUPONS_ENABLED feature flag row exists (CHK-06)"
    - "A global default commission_rules row exists (MKT-01 fallback)"
    - ".for('update') and onConflictDoNothing target syntax are confirmed working in the installed drizzle-orm version"
  artifacts:
    - path: "apps/api/drizzle/"
      provides: "generated SQL migration for Phase 5 tables"
      contains: "inventory_items"
    - path: "apps/api/src/db/seed-commerce.ts"
      provides: "COUPONS_ENABLED flag + global commission rate seed"
      contains: "COUPONS_ENABLED"
  key_links:
    - from: "apps/api/src/db/seed-commerce.ts"
      to: "feature_flags + commission_rules tables"
      via: "insert onConflictDoNothing"
      pattern: "COUPONS_ENABLED"
---

<objective>
[BLOCKING] Generate and apply the Drizzle migration for all Phase 5 schema changes, then seed the `COUPONS_ENABLED` feature flag and a global default commission rate. Also verify the two Drizzle query-builder features Phase 5 depends on (`.for('update')` row locking and `onConflictDoNothing({ target })`) actually work in the installed `drizzle-orm` version (RESEARCH.md Open Question 3, Assumption A2).

Purpose: Build/type checks pass without migrating (types come from schema config, not the live DB) — so this BLOCKING step is mandatory before any service can read/write the new tables. The probe de-risks the SELECT FOR UPDATE pattern before InventoryService is built in 05-05.
Output: Applied migration + seed script + a confirmed-working note on the two query features.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-commerce-core/05-RESEARCH.md
@apps/api/src/db/schema/index.ts
@apps/api/src/db/schema/feature-flags.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Generate + apply migration and probe Drizzle query features</name>
  <files>apps/api/drizzle/</files>
  <read_first>
    - apps/api/src/db/schema/index.ts (the barrel drizzle-kit reads for generation)
    - apps/api/src/db/schema/inventory-items.ts (the table the .for('update') probe targets)
    - .planning/phases/05-commerce-core/05-RESEARCH.md (Open Question 3 + Pattern 2 + Pattern 4 — fallback to raw SQL db.execute if .for('update') is unavailable)
    - apps/api/package.json (db:generate + db:migrate script names)
  </read_first>
  <action>
    Run `pnpm --filter @grovio/api db:generate` to produce the SQL migration for the 13 new tables + customers.wallet_balance_minor column. Inspect the generated SQL to confirm it includes CREATE TABLE for inventory_items, orders, vendor_orders, order_items, wallet_entries, payment_events, commission_rules, vendor_commission_entries, coupons, return_requests, vendor_return_policies, basket_sessions, basket_items, inventory_reservations, the unique index `payment_events_provider_event_uniq`, and `ALTER TABLE customers ADD COLUMN wallet_balance_minor`. Then run `pnpm --filter @grovio/api db:migrate` to apply it.
    Probe the two query features with a throwaway script or test: (a) `db.select().from(inventoryItems).where(eq(inventoryItems.id, someId)).for('update')` inside a `db.transaction` compiles and runs without error; (b) `db.insert(paymentEvents).values(...).onConflictDoNothing({ target: [paymentEvents.provider, paymentEvents.providerEventId] }).returning(...)` compiles and the second insert of the same (provider, eventId) returns an empty array. If `.for('update')` is NOT supported in the installed version, record the raw-SQL fallback `db.execute(sql\`SELECT ... FOR UPDATE\`)` in the SUMMARY so 05-05 uses it.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api db:migrate</automated>
  </verify>
  <acceptance_criteria>
    - A new SQL file exists under apps/api/drizzle/ containing `CREATE TABLE "inventory_items"` and `CREATE TABLE "wallet_entries"`
    - The migration SQL contains a unique index on payment_events (provider, provider_event_id)
    - The migration SQL contains `ALTER TABLE "customers" ADD COLUMN "wallet_balance_minor"`
    - `pnpm --filter @grovio/api db:migrate` exits 0
    - SUMMARY records whether `.for('update')` is natively supported or requires the raw-SQL fallback
  </acceptance_criteria>
  <done>All 13 tables + wallet_balance_minor column exist in the database; the two Drizzle query features are confirmed (or fallback documented).</done>
</task>

<task type="auto">
  <name>Task 2: Seed COUPONS_ENABLED flag and global commission rate</name>
  <files>apps/api/src/db/seed-commerce.ts</files>
  <read_first>
    - apps/api/src/db/schema/feature-flags.ts (feature_flags column shape for the COUPONS_ENABLED row)
    - apps/api/src/db/schema/commission-rules.ts (commission_rules columns for the global row)
    - apps/api/src/config/env.ts (DB connection for the seed script)
  </read_first>
  <action>
    Create `seed-commerce.ts` that connects to the DB and idempotently inserts (via onConflictDoNothing): (1) a `feature_flags` row with key `COUPONS_ENABLED` defaulting to enabled=false (admin toggles in Phase 6; CHK-06 short-circuits when off); (2) a `commission_rules` row with scope='global', vendorId=null, categoryId=null, ratePercent='10.00' (the global default fallback for the MKT-01 priority chain, D-14). Add an npm script `db:seed:commerce` in apps/api/package.json that runs this file with tsx. Run the seed once.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api db:seed:commerce</automated>
  </verify>
  <acceptance_criteria>
    - seed-commerce.ts inserts a COUPONS_ENABLED feature flag row using onConflictDoNothing (idempotent on re-run)
    - seed-commerce.ts inserts a commission_rules row with scope='global' and ratePercent='10.00'
    - apps/api/package.json has a `db:seed:commerce` script
    - `pnpm --filter @grovio/api db:seed:commerce` exits 0 and is safe to run twice
  </acceptance_criteria>
  <done>COUPONS_ENABLED flag and a global default commission rate exist in the database via an idempotent seed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | Schema changes applied to the running database; financial table constraints become enforced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | payment_events unique index | mitigate | Migration creates the (provider, provider_event_id) unique index; probe confirms onConflictDoNothing dedup works at runtime before webhook handlers are built. |
| T-05-01 | Tampering | inventory_items row lock | mitigate | Probe confirms `.for('update')` works (or documents raw-SQL fallback) before InventoryService relies on it in 05-05. |
| T-05-FF | Tampering | COUPONS_ENABLED flag | accept | Coupons default OFF; enabling is an admin action (Phase 6). No customer-facing coupon path is active until the flag is on. |
</threat_model>

<verification>
- `pnpm --filter @grovio/api db:migrate` reports success / no pending migrations.
- Seed runs twice without error (idempotent).
- Drizzle `.for('update')` / `onConflictDoNothing` behavior recorded in SUMMARY.
</verification>

<success_criteria>
Migration applied for all 13 tables + wallet_balance_minor; COUPONS_ENABLED flag and global commission rate seeded idempotently; Drizzle row-lock + upsert features confirmed working (or fallback documented).
</success_criteria>

<output>
Create `.planning/phases/05-commerce-core/05-04-SUMMARY.md` when done
</output>
