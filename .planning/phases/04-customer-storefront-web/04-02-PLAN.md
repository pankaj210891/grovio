---
phase: 04-customer-storefront-web
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/api/src/db/schema/customers.ts
  - apps/api/src/db/schema/password-reset-tokens.ts
  - apps/api/src/db/schema/customer-addresses.ts
  - apps/api/src/db/schema/homepage-blocks.ts
  - apps/api/src/db/schema/index.ts
  - apps/api/src/config/env.ts
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-05, STORE-01]
must_haves:
  truths:
    - "customers, password_reset_tokens, customer_addresses, homepage_blocks tables exist in the database after migration"
    - "STOREFRONT_ORIGIN, SMTP_*, and HOMEPAGE_BLOCKS_TTL_SECONDS env vars are defined and validated"
  artifacts:
    - path: "apps/api/src/db/schema/customers.ts"
      provides: "customers table"
      contains: "pgTable(\"customers\""
    - path: "apps/api/src/db/schema/password-reset-tokens.ts"
      provides: "password_reset_tokens table"
      contains: "pgTable(\"password_reset_tokens\""
    - path: "apps/api/src/db/schema/customer-addresses.ts"
      provides: "customer_addresses table"
      contains: "pgTable(\"customer_addresses\""
    - path: "apps/api/src/db/schema/homepage-blocks.ts"
      provides: "homepage_blocks table with JSONB payload"
      contains: "pgTable(\"homepage_blocks\""
    - path: "apps/api/src/config/env.ts"
      provides: "Phase 4 env vars"
      contains: "STOREFRONT_ORIGIN"
  key_links:
    - from: "apps/api/src/db/schema/password-reset-tokens.ts"
      to: "customers.id"
      via: "FK reference onDelete cascade"
      pattern: "references\\(\\(\\) => customers\\.id"
    - from: "apps/api/src/db/schema/index.ts"
      to: "all four new schema files"
      via: "barrel re-export"
      pattern: "export \\* from \"\\./customers"
---

<objective>
Create the four new Drizzle schema tables for Phase 4 (customers, password_reset_tokens, customer_addresses, homepage_blocks), extend the env schema with the new CORS/SMTP/cache vars, wire the schema barrel, then run the [BLOCKING] migration so the tables exist before any service is built against them.

Purpose: Every Phase 4 backend service (CustomerAuthService, CustomerAddressService, HomepageService) reads/writes these tables. The migration must run before Wave 3 service tests, which will fail without the tables.
Output: Four schema files, updated barrel + env, and applied DB migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-customer-storefront-web/04-PATTERNS.md
@.planning/phases/04-customer-storefront-web/04-RESEARCH.md
@apps/api/src/db/schema/vendors.ts
@apps/api/src/db/schema/products.ts
@apps/api/src/db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the four Phase 4 schema files + barrel exports</name>
  <read_first>
    - apps/api/src/db/schema/vendors.ts (exact column conventions: uuid id, email/passwordHash/name, archivedAt, createdAt, updatedAt; $inferInsert/$inferSelect type exports)
    - apps/api/src/db/schema/products.ts (jsonb + integer + boolean column imports for homepage_blocks)
    - apps/api/src/db/schema/index.ts (barrel export ordering)
    - .planning/phases/04-customer-storefront-web/04-PATTERNS.md (§ customers.ts, § password-reset-tokens.ts, § customer-addresses.ts, § homepage-blocks.ts, Pattern F, Pattern G — concrete column definitions)
  </read_first>
  <action>
    Create four schema files mirroring the vendors.ts conventions (uuid id `.defaultRandom().primaryKey()`, `withTimezone: true` timestamps, camelCase TS → snake_case SQL, `$inferInsert`/`$inferSelect` type exports).

    `apps/api/src/db/schema/customers.ts`: table `"customers"` with columns id (uuid pk), email (text notNull unique), passwordHash (`"password_hash"` text notNull), name (text notNull), phone (`"phone"` text nullable — needed for address association per PATTERNS.md), archivedAt (`"archived_at"` timestamp nullable), createdAt, updatedAt. Export `InsertCustomer` and `SelectCustomer`.

    `apps/api/src/db/schema/password-reset-tokens.ts`: import `customers` from `./customers.js`. Table `"password_reset_tokens"`: id (uuid pk), customerId (`"customer_id"` uuid notNull `.references(() => customers.id, { onDelete: "cascade" })`), tokenHash (`"token_hash"` text notNull unique — SHA-256 of the raw UUID token), expiresAt (`"expires_at"` timestamp withTimezone notNull), createdAt. No updatedAt, no archivedAt (single-use, hard-deleted on use per D-10). Export `InsertPasswordResetToken`, `SelectPasswordResetToken`.

    `apps/api/src/db/schema/customer-addresses.ts`: import `customers` from `./customers.js`. Table `"customer_addresses"`: id (uuid pk), customerId (uuid notNull FK cascade), label (text nullable), street (text notNull), city (text notNull), state (text notNull), pincode (text notNull), country (text notNull), lat (`doublePrecision("lat")` nullable), lng (`doublePrecision("lng")` nullable), placeId (`"place_id"` text nullable), isDefault (`"is_default"` boolean notNull default false), createdAt, updatedAt. Export `InsertCustomerAddress`, `SelectCustomerAddress`.

    `apps/api/src/db/schema/homepage-blocks.ts`: table `"homepage_blocks"`: id (uuid pk), type (text notNull — validated by Zod at service layer, no pgEnum), payload (`jsonb("payload").notNull()` — validated against MerchandisingBlockSchema at read), sortOrder (`"sort_order"` integer notNull default 0), isActive (`"is_active"` boolean notNull default true), createdAt, updatedAt. Export `InsertHomepageBlock`, `SelectHomepageBlock`.

    Update `apps/api/src/db/schema/index.ts`: add `export * from` lines for the four files, with `customers` exported BEFORE `password-reset-tokens` and `customer-addresses` (FK dependency ordering per Pattern G), followed by `homepage-blocks`.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - customers.ts contains `pgTable("customers"` and exports `SelectCustomer`
    - password-reset-tokens.ts contains `references(() => customers.id, { onDelete: "cascade" })` and `tokenHash` with `.unique()`
    - customer-addresses.ts contains `doublePrecision("lat")`, `isDefault`, and FK to customers.id
    - homepage-blocks.ts contains `jsonb("payload").notNull()` and `sortOrder` with `integer`
    - index.ts contains `export * from "./customers.js"` before the password-reset-tokens and customer-addresses exports
    - `pnpm --filter @grovio/api typecheck` exits 0
  </acceptance_criteria>
  <done>Four schema files exist following vendors.ts/products.ts conventions; barrel re-exports them in FK-safe order; API typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Extend env schema with Phase 4 vars</name>
  <read_first>
    - apps/api/src/config/env.ts (existing envSchema field patterns — FEATURE_FLAG_TTL_SECONDS, CATEGORY_TREE_TTL_SECONDS coerce.number defaults; optional infrastructure vars)
    - .planning/phases/04-customer-storefront-web/04-PATTERNS.md (§ env.ts, Pattern H — exact field definitions)
  </read_first>
  <action>
    Add the following fields to the envSchema in `apps/api/src/config/env.ts`, each with a JSDoc comment matching the existing field documentation style:
    - `STOREFRONT_ORIGIN: z.string().url().default("http://localhost:5173")` — CORS allowed origin for the httpOnly cookie flow (D-09).
    - `SMTP_HOST: z.string().optional()` — Google SMTP host for password reset email.
    - `SMTP_USER: z.string().optional()` — SMTP user (Google account email).
    - `SMTP_PASS: z.string().optional()` — SMTP app password.
    - `SMTP_FROM: z.string().optional()` — From address on transactional emails.
    - `HOMEPAGE_BLOCKS_TTL_SECONDS: z.coerce.number().default(300)` — Redis TTL for homepage block cache.
    Use `.optional()` for SMTP vars so the API still boots without email configured (dev fallback logs the reset link — implemented in Wave 3). Use `.default(...)` for STOREFRONT_ORIGIN and the TTL.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - env.ts contains `STOREFRONT_ORIGIN: z.string().url().default("http://localhost:5173")`
    - env.ts contains `SMTP_HOST: z.string().optional()`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`
    - env.ts contains `HOMEPAGE_BLOCKS_TTL_SECONDS: z.coerce.number().default(300)`
    - `pnpm --filter @grovio/api typecheck` exits 0
  </acceptance_criteria>
  <done>Env schema validates the six new Phase 4 vars; STOREFRONT_ORIGIN and TTL have safe dev defaults; SMTP vars are optional.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] db:generate + db:migrate</name>
  <read_first>
    - apps/api/src/db/schema/index.ts (confirm all four new tables are exported so drizzle-kit picks them up)
    - .planning/phases/03-catalog-search/03-04-SUMMARY.md (prior [BLOCKING] migration pattern, if present)
  </read_first>
  <action>
    Run the Drizzle migration generation and application AFTER the four schema files and barrel are complete (Task 1) so the new tables exist before any Wave 3 service is built. Commands: `pnpm --filter @grovio/api db:generate` (produces a new migration SQL file under the drizzle migrations folder) then `pnpm --filter @grovio/api db:migrate` (applies it to the Neon database). These run non-interactively; no extra flags needed. Confirm the generated migration includes `CREATE TABLE` statements for all four tables and the two foreign keys to customers.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api db:generate && pnpm --filter @grovio/api db:migrate</automated>
  </verify>
  <acceptance_criteria>
    - A new migration file is generated containing `CREATE TABLE "customers"`, `CREATE TABLE "password_reset_tokens"`, `CREATE TABLE "customer_addresses"`, `CREATE TABLE "homepage_blocks"`
    - The migration includes FOREIGN KEY constraints from password_reset_tokens.customer_id and customer_addresses.customer_id to customers.id
    - `db:migrate` exits 0 with no pending migrations remaining
  </acceptance_criteria>
  <done>All four tables exist in the database; migration applied cleanly; Wave 3 services can query the tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → database | Schema defines storage for passwords (hashed) and reset tokens (hashed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | password_reset_tokens.token_hash | mitigate | Column stores SHA-256 of the raw token, never the raw token; `expires_at` enforces 1-hour validity (D-10); single-use deletion handled in Wave 3 service |
| T-04-03 | Tampering | customers.password_hash | mitigate | Column stores argon2id hash only (hashing done in Wave 3 CustomerAuthService); never stores plaintext |
| T-04-04 | Information Disclosure | CORS origin (STOREFRONT_ORIGIN) | mitigate | Env var is a specific origin URL (`z.string().url()`), never `*`; consumed by @fastify/cors with credentials in Wave 4 |
</threat_model>

<verification>
- `pnpm --filter @grovio/api typecheck` exits 0 after schema + env changes
- Migration generated and applied; four tables present with FKs to customers
</verification>

<success_criteria>
- customers, password_reset_tokens, customer_addresses, homepage_blocks tables exist in the database
- Env schema validates all Phase 4 vars with correct optional/default behavior
</success_criteria>

<output>
Create `.planning/phases/04-customer-storefront-web/04-02-SUMMARY.md` when done
</output>
