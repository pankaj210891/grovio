---
phase: 04-customer-storefront-web
plan: 03
type: tdd
wave: 3
depends_on: [04-02]
files_modified:
  - apps/api/src/modules/customer-auth/CustomerAuthService.ts
  - apps/api/src/modules/customer-auth/CustomerAuthService.test.ts
  - apps/api/src/modules/customer-auth/index.ts
  - apps/api/src/modules/mailer/mailer.ts
  - apps/api/src/middleware/customerAuth.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "CustomerAuthService.register() hashes the password with argon2 and inserts a customer"
    - "register() throws CustomerAlreadyExistsError on duplicate email (pg 23505)"
    - "login() verifies the password and issues a JWT with role=customer; wrong password and unknown email both throw InvalidCredentialsError"
    - "verifyToken() rejects tokens whose role is not customer"
    - "forgotPassword() stores a hashed single-use token with 1-hour expiry and dispatches a reset email (or logs the link in dev when SMTP unset)"
    - "resetPassword() rejects expired or already-used tokens"
    - "requireCustomerAuth middleware reads the access_token httpOnly cookie and sets request.customerId"
  artifacts:
    - path: "apps/api/src/modules/customer-auth/CustomerAuthService.ts"
      provides: "Customer auth domain logic"
      contains: "class CustomerAuthService"
    - path: "apps/api/src/modules/customer-auth/CustomerAuthService.test.ts"
      provides: "AUTH-01/02/03/04 unit coverage"
      contains: "describe"
    - path: "apps/api/src/middleware/customerAuth.ts"
      provides: "Customer cookie JWT guard"
      contains: "requireCustomerAuth"
    - path: "apps/api/src/modules/mailer/mailer.ts"
      provides: "nodemailer transport factory with dev fallback"
      contains: "createMailerTransport"
  key_links:
    - from: "apps/api/src/modules/customer-auth/CustomerAuthService.ts"
      to: "customers + password_reset_tokens tables"
      via: "drizzle db queries"
      pattern: "customers|passwordResetTokens"
    - from: "apps/api/src/middleware/customerAuth.ts"
      to: "request.cookies.access_token"
      via: "jose jwtVerify + role guard"
      pattern: "request\\.cookies"
---

<objective>
Build the CustomerAuthService (argon2 + jose, role=customer), the nodemailer transport factory with a dev-mode console fallback, and the requireCustomerAuth cookie-based middleware. Test-first per the RESEARCH.md Validation Architecture test map (AUTH-01/02/03/04).

Purpose: This is the security core of the phase. It mirrors VendorAuthService exactly but adds password reset (D-10), httpOnly-cookie token issuance (D-09 — service returns tokens, route sets cookies in Wave 4), and a customer-role guard (D-11/AUTH-04). The middleware gates the /account/* routes built in Wave 4.
Output: CustomerAuthService + tests + index barrel, mailer factory, customerAuth middleware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-customer-storefront-web/04-PATTERNS.md
@.planning/phases/04-customer-storefront-web/04-RESEARCH.md
@apps/api/src/modules/vendor-auth/VendorAuthService.ts
@apps/api/src/modules/vendor-auth/VendorAuthService.test.ts
@apps/api/src/middleware/vendorAuth.ts
@packages/contracts/src/auth.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: CustomerAuthService (RED→GREEN→REFACTOR) with tests</name>
  <read_first>
    - apps/api/src/modules/vendor-auth/VendorAuthService.ts (argon2.hash/verify, jose SignJWT/jwtVerify, error classes, deps interface, 23505 mapping, TTL constant)
    - apps/api/src/modules/vendor-auth/VendorAuthService.test.ts (vi.mock argon2/jose, makeDbMock chain helper, test structure)
    - apps/api/src/db/schema/customers.ts (SelectCustomer/InsertCustomer shape)
    - apps/api/src/db/schema/password-reset-tokens.ts (token table columns)
    - packages/contracts/src/auth.ts (JwtPayload, RoleSchema customer literal, AuthTokenPair)
    - .planning/phases/04-customer-storefront-web/04-PATTERNS.md (§ CustomerAuthService.ts, § CustomerAuthService.test.ts — full patterns)
  </read_first>
  <behavior>
    - register(email, password, name): argon2.hash → insert into customers → return sanitized customer (no passwordHash); on pg code 23505 throws CustomerAlreadyExistsError
    - login(email, password): looks up customer by email → argon2.verify → on success returns { accessToken, refreshToken, expiresIn }; JWT carries sub=customerId and role="customer" (no vendorId claim); unknown email AND wrong password both throw InvalidCredentialsError with identical message (no user enumeration)
    - verifyToken(token): jwtVerify → throws if payload.role !== "customer" → returns customer token payload
    - refreshTokens(refreshToken): verifies refresh token, issues a new access (and refresh) token pair
    - forgotPassword(email): generates a raw UUID token, stores its SHA-256 hash in password_reset_tokens with expiresAt = now + 1h, dispatches reset email via mailer (link contains the raw token); when SMTP is unconfigured, logs the reset link to the console (dev fallback) and does NOT throw; for an unknown email it silently succeeds (no enumeration)
    - resetPassword(rawToken, newPassword): hashes the raw token, looks up the matching row, rejects if missing/expired (expiresAt < now) by throwing an InvalidResetTokenError, otherwise argon2.hash the new password, updates the customer, and hard-deletes the token row
  </behavior>
  <action>
    Create `apps/api/src/modules/customer-auth/CustomerAuthService.ts` as a structural copy of VendorAuthService with role="customer". Error classes: `CustomerAlreadyExistsError` (code "CUSTOMER_ALREADY_EXISTS"), `InvalidCredentialsError` (code "INVALID_CREDENTIALS", identical message for unknown email and wrong password), and a new `InvalidResetTokenError` (code "INVALID_RESET_TOKEN"). Deps interface `CustomerAuthServiceDeps { db, env, mailer }`. TTL constants `ACCESS_TTL_SECONDS = 3600`, `REFRESH_TTL_SECONDS = 604800`. Use Node `crypto` (`randomUUID` for the token, `createHash("sha256")` for the token hash) — do not store the raw token. Use the existing 23505 unique-violation mapping pattern. login() returns `{ accessToken, refreshToken, expiresIn }` (the route handler sets cookies in Wave 4 — service does NOT touch reply). Create `apps/api/src/modules/customer-auth/index.ts` re-exporting the service and error classes (mirror vendor-auth/index.ts).
    Write `CustomerAuthService.test.ts` first (RED), following the vendor test mock structure (vi.mock argon2 + jose, makeDbMock chain helper, a mailer mock with a `sendMail` spy). Cover every behavior listed above. Commit RED (failing), then implement to GREEN, then REFACTOR if needed.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api vitest run src/modules/customer-auth/CustomerAuthService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CustomerAuthService.ts contains `class CustomerAuthService` and `argon2.hash(`
    - JWT signing sets `role: "customer"` and does not set a `vendorId` claim
    - login throws InvalidCredentialsError for both unknown email and wrong password (test asserts identical error/message)
    - verifyToken throws when payload role !== "customer" (test asserts rejection)
    - forgotPassword stores `createHash("sha256")` of the token (not raw) with a 1-hour expiry and calls mailer.sendMail; SMTP-unset path logs link and does not throw (test asserts)
    - resetPassword rejects expired/missing token rows (test asserts InvalidResetTokenError)
    - index.ts re-exports CustomerAuthService, CustomerAlreadyExistsError, InvalidCredentialsError, InvalidResetTokenError
    - Test file exits 0 (all cases pass)
  </acceptance_criteria>
  <done>CustomerAuthService implements register/login/verifyToken/refreshTokens/forgotPassword/resetPassword with full test coverage matching the RESEARCH.md test map; commits follow RED→GREEN→REFACTOR.</done>
</task>

<task type="auto">
  <name>Task 2: Mailer transport factory with dev fallback</name>
  <read_first>
    - apps/api/src/config/env.ts (SMTP_HOST/USER/PASS/FROM optional vars added in 04-02)
    - .planning/phases/04-customer-storefront-web/04-RESEARCH.md (§"Environment Availability" — SMTP dev fallback: log reset link to console when SMTP not configured)
  </read_first>
  <action>
    Create `apps/api/src/modules/mailer/mailer.ts` exporting `createMailerTransport(env: Env): nodemailer.Transporter`. When `env.SMTP_HOST`, `SMTP_USER`, and `SMTP_PASS` are all set, return a real `nodemailer.createTransport({ host, port: 587, secure: false, auth: { user, pass } })`. When SMTP is not fully configured, return a JSON/stream transport (`nodemailer.createTransport({ jsonTransport: true })`) so calls to `sendMail` resolve without sending — the CustomerAuthService dev fallback logs the reset link itself. Export a typed `Mailer` alias for `nodemailer.Transporter` for Awilix injection. Keep this null-safe: the API must boot with no SMTP env vars.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - mailer.ts contains `export function createMailerTransport`
    - Returns a real SMTP transport only when SMTP_HOST/USER/PASS are all present, else a jsonTransport stub
    - `pnpm --filter @grovio/api typecheck` exits 0
  </acceptance_criteria>
  <done>createMailerTransport returns a working transport or a no-op dev transport; never throws on missing SMTP config.</done>
</task>

<task type="auto">
  <name>Task 3: requireCustomerAuth cookie middleware</name>
  <read_first>
    - apps/api/src/middleware/vendorAuth.ts (full requireVendorAuth pattern — jose jwtVerify, role guard, 401 envelope, request augmentation)
    - .planning/phases/04-customer-storefront-web/04-PATTERNS.md (§ customerAuth.ts — exact middleware, key difference is cookie read + customer role)
  </read_first>
  <action>
    Create `apps/api/src/middleware/customerAuth.ts` exporting `requireCustomerAuth(request, reply)` mirroring requireVendorAuth, with two changes: (1) read the token from `request.cookies?.["access_token"]` (httpOnly cookie, D-09) instead of the Authorization Bearer header; (2) guard `payload.role === "customer"` and set `request.customerId = payload.sub` on success. On missing token or verify failure, return the 401 envelope `{ success: false, error: { code: "UNAUTHORIZED", message: ... } }`. Augment the FastifyRequest type with `customerId?: string` following however vendorAuth augments `vendorId` (module augmentation in the same file or the existing types file). This middleware requires `@fastify/cookie` registered before it runs (wired in Wave 4 app.ts).
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - customerAuth.ts contains `export async function requireCustomerAuth`
    - Reads `request.cookies` (not Authorization header)
    - Guards `payload["role"] !== "customer"` and sets `request.customerId`
    - Returns 401 envelope with code "UNAUTHORIZED" on failure
    - `pnpm --filter @grovio/api typecheck` exits 0
  </acceptance_criteria>
  <done>requireCustomerAuth validates the access_token cookie, enforces customer role, and exposes request.customerId; API typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /auth/* | Untrusted credentials and reset tokens enter the auth service |
| client → /account/* | Customer JWT cookie validated by middleware before access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | login() error messages | mitigate | Identical InvalidCredentialsError + HTTP status for unknown email and wrong password — no user enumeration (test-enforced) |
| T-04-06 | Spoofing | JWT role claim | mitigate | verifyToken() and requireCustomerAuth enforce `role === "customer"`; vendor/admin tokens cannot access customer routes (AUTH-04, D-11) |
| T-04-07 | Elevation of Privilege | password reset token reuse | mitigate | Token stored as SHA-256 hash, single-use (hard-deleted on success), expiresAt checked before acceptance (D-10) |
| T-04-08 | Information Disclosure | forgotPassword for unknown email | mitigate | Silent success for unknown email — no account-existence disclosure |
| T-04-09 | Tampering | password storage | mitigate | argon2id hashing only; plaintext never persisted |
</threat_model>

<verification>
- `pnpm --filter @grovio/api vitest run src/modules/customer-auth/CustomerAuthService.test.ts` exits 0
- `pnpm --filter @grovio/api typecheck` exits 0
- `pnpm --filter @grovio/api vitest run` (full suite) stays green
</verification>

<success_criteria>
- CustomerAuthService passes all AUTH-01/02/03/04 behavior tests
- Mailer factory degrades gracefully without SMTP
- requireCustomerAuth enforces customer-role cookie auth
</success_criteria>

<output>
Create `.planning/phases/04-customer-storefront-web/04-03-SUMMARY.md` when done
</output>
