---
phase: 04-customer-storefront-web
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/category/blocks.ts
  - apps/api/package.json
  - apps/web-storefront/package.json
autonomous: false
requirements: [STORE-01, STORE-05]
must_haves:
  truths:
    - "The contracts package exports a `FeaturedCategoriesBlock` type consumable by the frontend and backend without breaking existing block consumers"
    - "All Phase 4 npm packages install cleanly with no peer-dependency errors across the workspace"
  artifacts:
    - path: "packages/contracts/src/category/blocks.ts"
      provides: "FeaturedCategoriesBlockSchema + union member"
      contains: "FeaturedCategoriesBlockSchema"
    - path: "apps/api/package.json"
      provides: "Backend Phase 4 deps"
      contains: "@fastify/cors"
    - path: "apps/web-storefront/package.json"
      provides: "Frontend Phase 4 deps"
      contains: "react-router-dom"
  key_links:
    - from: "packages/contracts/src/category/blocks.ts"
      to: "MerchandisingBlockSchema discriminated union"
      via: "FeaturedCategoriesBlockSchema added as 4th union member"
      pattern: "FeaturedCategoriesBlockSchema"
---

<objective>
Extend the shared block contract with the `featured_categories` block type (D-02) and install all new Phase 4 npm dependencies behind a package legitimacy checkpoint.

Purpose: Phase 4 frontend and backend both depend on these. The contract extension is a non-breaking addition to the discriminated union that every consumer (homepage renderer, homepage service validation) imports. The dependency installs unblock all subsequent waves.
Output: Updated `packages/contracts/src/category/blocks.ts`; new dependencies in `apps/api/package.json` and `apps/web-storefront/package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-customer-storefront-web/04-CONTEXT.md
@.planning/phases/04-customer-storefront-web/04-RESEARCH.md
@.planning/phases/04-customer-storefront-web/04-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Package legitimacy verification before install</name>
  <read_first>
    - .planning/phases/04-customer-storefront-web/04-RESEARCH.md (§"Package Legitimacy Audit" — all six packages tagged [ASSUMED])
  </read_first>
  <action>
    Pause for human package legitimacy verification before any install runs (Task 3). Present the six [ASSUMED] packages and their npm/source-repo evidence and wait for explicit human approval (or a substitution decision). Do not auto-approve — slopcheck was unavailable at research time so manual verification is mandatory; this checkpoint is never auto-advanced.
  </action>
  <what-built>
    Research (04-RESEARCH.md) flagged six packages as [ASSUMED] because slopcheck was unavailable at research time. Per the package legitimacy gate, each must be human-verified before install. The six packages: react-router-dom (^7.16.0), lucide-react (^0.525.0 per UI-SPEC / ^1.17.0 per research — verify latest), react-intersection-observer (^10.0.3), @googlemaps/js-api-loader (^2.0.2), @fastify/cors (^11.2.0), nodemailer (^8.0.0).
  </what-built>
  <how-to-verify>
    1. Confirm react-router-dom at https://www.npmjs.com/package/react-router-dom (Remix/remix-run, Apache 2.0, 13M+ weekly downloads)
    2. Confirm lucide-react at https://www.npmjs.com/package/lucide-react (lucide-icons, ISC license) — note the exact current major/minor to install
    3. Confirm react-intersection-observer at https://www.npmjs.com/package/react-intersection-observer (thebuilder, MIT)
    4. Confirm @googlemaps/js-api-loader at https://www.npmjs.com/package/@googlemaps/js-api-loader (googlemaps, Apache 2.0)
    5. Confirm @fastify/cors at https://www.npmjs.com/package/@fastify/cors (fastify, v11.x targets Fastify 5)
    6. Confirm nodemailer at https://www.npmjs.com/package/nodemailer (15M+ weekly downloads)
  </how-to-verify>
  <acceptance_criteria>
    - Human approves all six packages OR identifies a substitution
    - Resume signal received before any install runs
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with installs, or name any package to replace.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add featured_categories block to the contracts union</name>
  <read_first>
    - packages/contracts/src/category/blocks.ts (current discriminated union — BannerBlockSchema, ProductGridBlockSchema, TextBlockSchema, MerchandisingBlockSchema)
    - .planning/phases/04-customer-storefront-web/04-PATTERNS.md (§"packages/contracts/src/category/blocks.ts" — exact extension pattern)
  </read_first>
  <action>
    Add `FeaturedCategoriesBlockSchema` to `packages/contracts/src/category/blocks.ts` as a non-breaking extension per D-02. Shape: `type` literal `"featured_categories"`, `title` string, `categoryIds` array of `z.string().uuid()`, `layout` enum `["grid", "row"]`. Export the inferred type `FeaturedCategoriesBlock`. Add `FeaturedCategoriesBlockSchema` as the fourth member of the existing `MerchandisingBlockSchema` discriminated union — do NOT modify BannerBlockSchema, ProductGridBlockSchema, or TextBlockSchema. Match the JSDoc comment style of the existing block schemas in the file. Ensure the contracts package builds (the storefront and API import `MerchandisingBlock` and `MerchandisingBlockSchema` from `@grovio/contracts`).
  </action>
  <verify>
    <automated>pnpm --filter @grovio/contracts build && pnpm --filter @grovio/contracts typecheck</automated>
  </verify>
  <acceptance_criteria>
    - blocks.ts contains `export const FeaturedCategoriesBlockSchema = z.object({`
    - blocks.ts contains `type: z.literal("featured_categories")`
    - blocks.ts contains `layout: z.enum(["grid", "row"])`
    - blocks.ts contains `export type FeaturedCategoriesBlock = z.infer<typeof FeaturedCategoriesBlockSchema>`
    - MerchandisingBlockSchema discriminatedUnion array lists `FeaturedCategoriesBlockSchema` as a member
    - The three Phase 2 block schemas are unchanged (BannerBlockSchema, ProductGridBlockSchema, TextBlockSchema literals intact)
    - `pnpm --filter @grovio/contracts build` exits 0
  </acceptance_criteria>
  <done>FeaturedCategoriesBlock is part of the shared MerchandisingBlock union; contracts package builds and typechecks; existing block types untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Install Phase 4 backend and frontend dependencies</name>
  <read_first>
    - apps/api/package.json (current dependencies — confirm @fastify/cors, @fastify/cookie, nodemailer not yet present)
    - apps/web-storefront/package.json (current dependencies — confirm react-router-dom, lucide-react, react-intersection-observer, @googlemaps/js-api-loader not yet present)
    - .planning/phases/04-customer-storefront-web/04-RESEARCH.md (§"Installation (frontend)" and §"Installation (backend)")
  </read_first>
  <action>
    Install Phase 4 dependencies after the legitimacy checkpoint is approved.
    Frontend: `pnpm --filter @grovio/web-storefront add react-router-dom@^7.16.0 lucide-react@latest react-intersection-observer@^10.0.3 @googlemaps/js-api-loader@^2.0.2`. Also add `@types/google.maps` as a dev dependency for the `google.maps.places.Autocomplete` types used by PlacesAutocompleteInput: `pnpm --filter @grovio/web-storefront add -D @types/google.maps`.
    Backend: `pnpm --filter @grovio/api add @fastify/cors@^11.2.0 @fastify/cookie@latest nodemailer@^8.0.0` then `pnpm --filter @grovio/api add -D @types/nodemailer`. Note: `@fastify/cookie` is required to enable `reply.setCookie()` / `request.cookies` (D-09) per RESEARCH.md Open Question 1. Verify `@fastify/cookie` is not already a registered plugin in app.ts before relying on it; install regardless since it is not in the current package.json.
    Do not pin lucide-react to a stale version; use whatever the human approved in Task 1 / the current latest.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @grovio/api typecheck && pnpm --filter @grovio/web-storefront typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/package.json dependencies contain `@fastify/cors`, `@fastify/cookie`, `nodemailer`
    - apps/api/package.json devDependencies contain `@types/nodemailer`
    - apps/web-storefront/package.json dependencies contain `react-router-dom`, `lucide-react`, `react-intersection-observer`, `@googlemaps/js-api-loader`
    - `pnpm install` completes with no unmet peer dependency errors that break the workspace
    - `pnpm --filter @grovio/api typecheck` exits 0
    - `pnpm --filter @grovio/web-storefront typecheck` exits 0
  </acceptance_criteria>
  <done>All six runtime packages plus type packages are installed in the correct workspace packages; lockfile updated; both packages typecheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → workspace | Untrusted third-party package code enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm installs (six packages) | mitigate | Blocking human checkpoint (Task 1) verifies each [ASSUMED] package on npmjs.com before install; slopcheck was unavailable so manual verification is mandatory |
| T-04-01 | Tampering | featured_categories block payload | mitigate | z.discriminatedUnion rejects unknown/malformed blocks at parse time; categoryIds constrained to `z.string().uuid()` so non-UUID payloads are rejected before reaching the DB |
</threat_model>

<verification>
- `pnpm --filter @grovio/contracts build` exits 0
- `pnpm install` succeeds; lockfile reflects all six new packages
- `pnpm --filter @grovio/api typecheck` and `pnpm --filter @grovio/web-storefront typecheck` exit 0
</verification>

<success_criteria>
- featured_categories is a member of the MerchandisingBlock union and importable from @grovio/contracts
- All Phase 4 npm dependencies installed in the correct packages after human legitimacy approval
</success_criteria>

<output>
Create `.planning/phases/04-customer-storefront-web/04-01-SUMMARY.md` when done
</output>
</content>
</invoke>
