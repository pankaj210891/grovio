---
phase: 04-customer-storefront-web
plan: 04
type: tdd
wave: 3
depends_on: [04-02]
files_modified:
  - apps/api/src/modules/homepage/HomepageService.ts
  - apps/api/src/modules/homepage/HomepageService.test.ts
  - apps/api/src/modules/homepage/index.ts
  - apps/api/src/modules/customer-addresses/CustomerAddressService.ts
  - apps/api/src/modules/customer-addresses/CustomerAddressService.test.ts
  - apps/api/src/modules/customer-addresses/index.ts
autonomous: true
requirements: [STORE-01, AUTH-05]
must_haves:
  truths:
    - "HomepageService.getBlocks() returns only is_active=true blocks ordered by sort_order ascending"
    - "getBlocks() returns Redis-cached blocks on a cache hit and populates the cache on a miss"
    - "JSONB payloads are validated against MerchandisingBlockSchema at read"
    - "CustomerAddressService.listAddresses(customerId) returns only that customer's addresses"
    - "Address create/update/delete are scoped to the owning customer"
  artifacts:
    - path: "apps/api/src/modules/homepage/HomepageService.ts"
      provides: "Homepage block read with Redis cache"
      contains: "getBlocks"
    - path: "apps/api/src/modules/homepage/HomepageService.test.ts"
      provides: "STORE-01 coverage"
      contains: "describe"
    - path: "apps/api/src/modules/customer-addresses/CustomerAddressService.ts"
      provides: "Customer address CRUD"
      contains: "class CustomerAddressService"
    - path: "apps/api/src/modules/customer-addresses/CustomerAddressService.test.ts"
      provides: "AUTH-05 isolation coverage"
      contains: "describe"
  key_links:
    - from: "apps/api/src/modules/homepage/HomepageService.ts"
      to: "homepage_blocks table + Redis homepage:blocks key"
      via: "Redis-first read with DB fallback"
      pattern: "homepage:blocks"
    - from: "apps/api/src/modules/customer-addresses/CustomerAddressService.ts"
      to: "customer_addresses table"
      via: "drizzle queries scoped by customerId"
      pattern: "eq\\(customerAddresses\\.customerId"
---

<objective>
Build the HomepageService (Redis-first block read, STORE-01) and the CustomerAddressService (per-customer address CRUD, AUTH-05), test-first. These two services touch no files in plan 04-03 and run in parallel with it in Wave 3.

Purpose: HomepageService backs the `GET /homepage` public endpoint (D-04). CustomerAddressService backs the `/account/addresses` routes. Both are read/write services wired in Wave 4.
Output: HomepageService + tests + index, CustomerAddressService + tests + index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-customer-storefront-web/04-PATTERNS.md
@.planning/phases/04-customer-storefront-web/04-RESEARCH.md
@apps/api/src/modules/feature-flags/FeatureFlagService.ts
@apps/api/src/modules/categories/CategoryService.ts
@packages/contracts/src/category/blocks.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: HomepageService (RED→GREEN→REFACTOR) with tests</name>
  <read_first>
    - apps/api/src/modules/feature-flags/FeatureFlagService.ts (Redis-first read + DB fallback + setex TTL pattern; deps interface { db, redis, env })
    - apps/api/src/modules/feature-flags/FeatureFlagService.test.ts (Redis + DB mock structure)
    - apps/api/src/db/schema/homepage-blocks.ts (SelectHomepageBlock shape; type + payload columns)
    - packages/contracts/src/category/blocks.ts (MerchandisingBlockSchema for read-time validation — includes featured_categories from 04-01)
    - .planning/phases/04-customer-storefront-web/04-PATTERNS.md (§ HomepageService.ts — exact getBlocks/invalidateBlocks pattern)
  </read_first>
  <behavior>
    - getBlocks() cache hit: when Redis key "homepage:blocks" is set, returns the parsed cached array without hitting the DB
    - getBlocks() cache miss: queries homepage_blocks WHERE is_active = true ORDER BY sort_order ASC, validates each row's JSONB payload via MerchandisingBlockSchema.parse({ ...row.payload, type: row.type }), caches the result with setex using env.HOMEPAGE_BLOCKS_TTL_SECONDS, and returns it
    - getBlocks() excludes is_active=false blocks
    - getBlocks() returns blocks in ascending sort_order
    - invalidateBlocks() deletes the Redis "homepage:blocks" key
  </behavior>
  <action>
    Create `apps/api/src/modules/homepage/HomepageService.ts` mirroring FeatureFlagService's Redis-first read pattern, deps `{ db, redis, env }`. Implement `getBlocks(): Promise<MerchandisingBlock[]>` and `invalidateBlocks(): Promise<void>` exactly as in PATTERNS.md § HomepageService.ts (Redis "homepage:blocks" key, `asc(homepageBlocks.sortOrder)`, `eq(homepageBlocks.isActive, true)`, `MerchandisingBlockSchema.parse`, `setex(..., env.HOMEPAGE_BLOCKS_TTL_SECONDS, ...)`). Create `apps/api/src/modules/homepage/index.ts` re-exporting the service. Write `HomepageService.test.ts` FIRST (RED) covering all five behaviors using Redis + DB mocks following the FeatureFlagService.test.ts structure; then implement to GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api vitest run src/modules/homepage/HomepageService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - HomepageService.ts contains `getBlocks` and reads/writes the Redis key `"homepage:blocks"`
    - DB fallback query filters `eq(homepageBlocks.isActive, true)` and orders by `asc(homepageBlocks.sortOrder)`
    - Payload validated with `MerchandisingBlockSchema.parse`
    - Cache populated with `setex` using `env.HOMEPAGE_BLOCKS_TTL_SECONDS`
    - Test asserts: cache hit skips DB, miss queries+caches, is_active=false excluded, sort_order ascending order, invalidateBlocks deletes the key
    - Test file exits 0
  </acceptance_criteria>
  <done>HomepageService returns ordered active blocks with Redis caching and read-time Zod validation; full test coverage; RED→GREEN commits.</done>
</task>

<task type="tdd">
  <name>Task 2: CustomerAddressService (RED→GREEN→REFACTOR) with tests</name>
  <read_first>
    - apps/api/src/modules/categories/CategoryService.ts (CRUD list/insert/update/delete drizzle patterns, deps interface)
    - apps/api/src/modules/categories/CategoryService.test.ts (DB mock structure)
    - apps/api/src/db/schema/customer-addresses.ts (SelectCustomerAddress/InsertCustomerAddress shape)
    - .planning/phases/04-customer-storefront-web/04-PATTERNS.md (§ CustomerAddressService.ts — list/insert/update patterns, no Redis cache)
  </read_first>
  <behavior>
    - listAddresses(customerId): returns rows WHERE customer_id = customerId ORDER BY created_at ASC; never returns another customer's addresses
    - createAddress(customerId, input): inserts a row with the given customerId and the structured address fields (street/city/state/pincode/country/lat/lng/placeId/label/isDefault); returns the created row
    - updateAddress(id, customerId, patch): updates only the row matching BOTH id AND customer_id; returns the updated row or null when no row matches (prevents cross-customer edits)
    - deleteAddress(id, customerId): deletes only the row matching id AND customer_id; returns whether a row was deleted
    - setting isDefault=true is allowed (no enforcement of single-default in Phase 4 — multiple defaults are tolerated; document as acceptable)
  </behavior>
  <action>
    Create `apps/api/src/modules/customer-addresses/CustomerAddressService.ts` mirroring CategoryService CRUD methods, deps `{ db }` (no Redis — address data is customer-specific, not shared, per PATTERNS.md). Every mutating/reading method takes `customerId` and scopes the query with `eq(customerAddresses.customerId, customerId)` (and `eq(customerAddresses.id, id)` for single-row ops) so a customer can never read or mutate another customer's addresses. Create `apps/api/src/modules/customer-addresses/index.ts` re-exporting the service. Write `CustomerAddressService.test.ts` FIRST (RED) — the critical AUTH-05 test asserts listAddresses/update/delete are scoped to the requesting customerId (a row owned by a different customer is not returned/updated/deleted). Then implement to GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api vitest run src/modules/customer-addresses/CustomerAddressService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CustomerAddressService.ts contains `class CustomerAddressService`
    - listAddresses filters `eq(customerAddresses.customerId, customerId)`
    - updateAddress and deleteAddress scope by both id AND customerId
    - Test asserts cross-customer isolation (other customer's address not returned/mutated)
    - index.ts re-exports the service
    - Test file exits 0
  </acceptance_criteria>
  <done>CustomerAddressService performs per-customer address CRUD with enforced isolation; full test coverage; RED→GREEN commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer → address data | A customer must only access their own addresses |
| DB JSONB → homepage response | Block payloads must be structurally validated before serving |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | CustomerAddressService queries | mitigate | All reads/writes scoped by customerId; cross-customer access impossible (test-enforced AUTH-05 isolation) |
| T-04-11 | Tampering | homepage_blocks JSONB payload | mitigate | MerchandisingBlockSchema.parse at read rejects malformed/unknown block types before they reach the response |
| T-04-12 | Denial of Service | repeated homepage DB reads | accept | Redis cache with configurable TTL absorbs load; homepage is public read-only, low-value target for ASVS L1 |
</threat_model>

<verification>
- `pnpm --filter @grovio/api vitest run src/modules/homepage/HomepageService.test.ts` exits 0
- `pnpm --filter @grovio/api vitest run src/modules/customer-addresses/CustomerAddressService.test.ts` exits 0
- `pnpm --filter @grovio/api vitest run` (full suite) stays green
</verification>

<success_criteria>
- HomepageService serves ordered active blocks with Redis caching (STORE-01)
- CustomerAddressService enforces per-customer isolation (AUTH-05)
</success_criteria>

<output>
Create `.planning/phases/04-customer-storefront-web/04-04-SUMMARY.md` when done
</output>
