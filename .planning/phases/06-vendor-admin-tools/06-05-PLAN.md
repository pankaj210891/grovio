---
phase: 06-vendor-admin-tools
plan: 05
type: tdd
wave: 4
depends_on: [06-03]
files_modified:
  - apps/api/src/modules/settings/SettingsService.ts
  - apps/api/src/modules/settings/SettingsService.test.ts
  - apps/api/src/modules/settings/index.ts
  - apps/api/src/modules/audit/AuditService.ts
  - apps/api/src/modules/audit/AuditService.test.ts
  - apps/api/src/modules/audit/index.ts
autonomous: true
requirements: [ADM-05, ADM-07]
must_haves:
  truths:
    - "SettingsService reads marketplace_settings Redis-first and invalidates cache on update (D-19)"
    - "AuditService appends a row to audit_log per call and never updates existing rows (D-13)"
  artifacts:
    - path: "apps/api/src/modules/settings/SettingsService.ts"
      provides: "Redis-first marketplace settings read/write"
      exports: ["SettingsService"]
    - path: "apps/api/src/modules/audit/AuditService.ts"
      provides: "append-only audit log writer"
      exports: ["AuditService"]
  key_links:
    - from: "apps/api/src/modules/settings/SettingsService.ts"
      to: "marketplace_settings table + redis"
      via: "Redis-first read, UPSERT + del on write"
      pattern: "settings:"
    - from: "apps/api/src/modules/audit/AuditService.ts"
      to: "audit_log table"
      via: "insert only"
      pattern: "insert\\(auditLog\\)"
---

<objective>
Create the two foundational shared backend services: SettingsService (marketplace_settings, Redis-first cache mirroring FeatureFlagService, D-19) and AuditService (append-only audit_log writer, D-13). These have no file overlap with the auth migration (06-04) and run in the same wave.

Purpose: Both are consumed by nearly every admin/vendor service in Wave 5 (audit on every mutation; settings for low-stock threshold, currency, return window). TDD because both have crisp I/O contracts.
Output: settings + audit modules, test-covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-vendor-admin-tools/06-CONTEXT.md
@.planning/phases/06-vendor-admin-tools/06-RESEARCH.md
@.planning/phases/06-vendor-admin-tools/06-PATTERNS.md
@apps/api/src/modules/feature-flags/FeatureFlagService.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: SettingsService (Redis-first marketplace settings)</name>
  <read_first>
    - apps/api/src/modules/feature-flags/FeatureFlagService.ts (FULL — deps interface, redisKey, getFlag Redis-first, invalidateFlag; SettingsService is a near-exact copy)
    - apps/api/src/db/schema/marketplace-settings.ts (key PK + jsonb value)
    - apps/api/src/config/env.ts (FEATURE_FLAG_TTL_SECONDS — reuse as the settings TTL)
    - packages/contracts/src/admin/settings.ts (MarketplaceSettingKey enum, UpdateSettingInput)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (SettingsService section: getSetting/updateSetting/invalidateSetting bodies)
  </read_first>
  <behavior>
    - Test: getSetting returns parsed value from Redis when cache hit (DB not queried).
    - Test: getSetting on cache miss reads DB, populates Redis with `setex(redisKey, FEATURE_FLAG_TTL_SECONDS, ...)`, returns the value.
    - Test: getSetting returns null when key absent in both Redis and DB.
    - Test: updateSetting UPSERTs (insert onConflictDoUpdate on key) then calls `redis.del("settings:<key>")`.
    - Test: getAllSettings returns a record of every marketplace_settings row keyed by key (for the admin settings page).
  </behavior>
  <action>
    Create `apps/api/src/modules/settings/SettingsService.ts`. Copy FeatureFlagService structure: deps `{ db, redis, env }`; `redisKey(key)` returns `"settings:" + key`. Implement:
    - `getSetting(key): Promise<unknown | null>` — Redis-first read; on miss read `marketplaceSettings` where key; setex with `env.FEATURE_FLAG_TTL_SECONDS`; JSON.stringify the JSONB value.
    - `getAllSettings(): Promise<Record<string, unknown>>` — select all rows, return key→value map (no caching needed; admin page only).
    - `updateSetting(key, value): Promise<void>` — `db.insert(marketplaceSettings).values({ key, value }).onConflictDoUpdate({ target: marketplaceSettings.key, set: { value, updatedAt: new Date() } })` then `redis.del(this.redisKey(key))`.
    - `invalidateSetting(key): Promise<void>` — `redis.del(this.redisKey(key))`.
    Create `apps/api/src/modules/settings/index.ts` barrel.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api test --run -- SettingsService</automated>
  </verify>
  <acceptance_criteria>
    - SettingsService.ts redisKey returns the prefix `settings:`
    - getSetting uses `setex` with `env.FEATURE_FLAG_TTL_SECONDS`
    - updateSetting calls `onConflictDoUpdate` then `redis.del`
    - On Redis cache hit, the DB select is NOT invoked (verified via mock in test)
    - `pnpm --filter @grovio/api test --run -- SettingsService` passes
  </acceptance_criteria>
  <done>SettingsService Redis-first read + UPSERT/invalidate, test-covered.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: AuditService (append-only audit_log writer)</name>
  <read_first>
    - apps/api/src/modules/wallet/WalletService.ts (append-only insert pattern — insert only, no update)
    - apps/api/src/db/schema/audit-log.ts (columns)
    - packages/contracts/src/admin/audit.ts (AuditActorType, AuditLogEntry, AuditLogQuery shapes)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (AuditService section: log() signature, no Redis dep)
  </read_first>
  <behavior>
    - Test: log() inserts exactly one audit_log row with the provided actorType/actorId/actorEmail/action/entityType/entityId and stores before/after as JSONB.
    - Test: log() with before/after omitted stores nulls for those columns.
    - Test: query() returns rows filtered by actorType, action, entityType, and date range (from/to), ordered by createdAt desc, with offset pagination, plus a total count.
    - Test: AuditService never issues an UPDATE statement (it has no update method).
  </behavior>
  <action>
    Create `apps/api/src/modules/audit/AuditService.ts` with deps `{ db }` (NO redis — append-only writer). Implement:
    - `log(params: { actorType: "admin"|"vendor"|"system"; actorId: string; actorEmail: string; action: string; entityType: string; entityId: string; before?: unknown; after?: unknown; ipAddress?: string }): Promise<void>` — `db.insert(auditLog).values({ ...params, before: params.before ?? null, after: params.after ?? null, createdAt: new Date() })`.
    - `query(filter: AuditLogQuery): Promise<{ items: AuditLogEntry[]; total: number; limit: number; offset: number }>` — build WHERE conditions for each optional filter, order by createdAt desc, apply limit/offset (offset pagination is fine per D-claude-discretion), return items + total.
    Create `apps/api/src/modules/audit/index.ts` barrel.
    Use the dot-namespaced action convention (e.g., `vendor.approved`) — but the convention is enforced by callers (Wave 5 services), not by AuditService; AuditService stores whatever action string it receives.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api test --run -- AuditService</automated>
  </verify>
  <acceptance_criteria>
    - AuditService.ts deps interface contains `db` and NOT `redis`
    - log() calls `insert(auditLog)` and never `update(`
    - query() supports actorType, action, entityType, from, to filters and returns { items, total, limit, offset }
    - AuditService class exposes no method that issues an UPDATE
    - `pnpm --filter @grovio/api test --run -- AuditService` passes
  </acceptance_criteria>
  <done>AuditService appends log rows and queries with filters; no update path exists; test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → Redis/DB | SettingsService cache must not serve stale data after writes; AuditService must durably record every action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Repudiation | Audit log tampering / missing entries | mitigate | AuditService is append-only (insert only, no update method). Callers in Wave 5 invoke log() on every sensitive mutation. |
| T-06-14 | Tampering | Stale settings served after update (e.g., stale currency/return window) | mitigate | updateSetting invalidates Redis (`del`) AFTER the DB UPSERT (Pitfall 3 ordering). |
| T-06-SC | Tampering | npm installs | mitigate | No installs (ioredis, drizzle already present). |
</threat_model>

<verification>
- `pnpm --filter @grovio/api test --run -- SettingsService AuditService` passes.
- Grep confirms AuditService has no `.update(` call.
</verification>

<success_criteria>
SettingsService mirrors FeatureFlagService Redis-first pattern with cache invalidation; AuditService is append-only with filtered query; both test-covered.
</success_criteria>

<output>
Create `.planning/phases/06-vendor-admin-tools/06-05-SUMMARY.md` when done.
</output>
