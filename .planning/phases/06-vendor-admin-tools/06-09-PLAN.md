---
phase: 06-vendor-admin-tools
plan: 09
type: execute
wave: 7
depends_on: [06-08]
files_modified:
  - apps/web-vendor/package.json
  - apps/web-vendor/src/main.tsx
  - apps/web-vendor/src/router.tsx
  - apps/web-vendor/src/lib/apiClient.ts
  - apps/web-vendor/src/lib/queryClient.ts
  - apps/web-vendor/src/stores/vendorAuthStore.ts
  - apps/web-vendor/src/stores/uiStore.ts
  - apps/web-vendor/src/hooks/useVendorAuth.ts
  - apps/web-vendor/src/components/layout/PanelLayout.tsx
  - apps/web-vendor/src/components/layout/Sidebar.tsx
  - apps/web-vendor/src/components/layout/Header.tsx
  - apps/web-vendor/src/components/layout/ProtectedVendorRoute.tsx
  - apps/web-vendor/src/pages/auth/LoginPage.tsx
  - apps/web-vendor/src/pages/auth/AcceptInvitePage.tsx
  - apps/web-vendor/src/pages/DashboardPage.tsx
  - apps/web-vendor/src/pages/ProductsPage.tsx
  - apps/web-vendor/src/pages/InventoryPage.tsx
  - apps/web-vendor/src/pages/OrdersPage.tsx
  - apps/web-vendor/src/pages/ReturnsPage.tsx
  - apps/web-vendor/src/pages/EarningsPage.tsx
  - apps/web-vendor/src/pages/CouponsPage.tsx
  - apps/web-vendor/src/pages/TeamPage.tsx
  - apps/web-vendor/src/pages/StoreProfilePage.tsx
  - apps/web-vendor/src/pages/SettingsPage.tsx
autonomous: false
requirements: [VEN-01, VEN-02, VEN-03, VEN-04, VEN-05, VEN-06, MKT-05]
must_haves:
  truths:
    - "A vendor can log in, see a role-aware sidebar panel, and navigate all vendor sections"
    - "Vendor can view dashboard KPIs + low-stock, manage inventory/pricing inline, handle orders + returns (approve/reject), view earnings/payout status, manage team (owner), edit store profile + return policy + payout info (owner)"
    - "Coupons nav item is hidden when COUPONS_ENABLED is off; lower roles cannot see owner-only sections"
    - "An invited staff member can accept via the emailed link and set a password"
  artifacts:
    - path: "apps/web-vendor/src/router.tsx"
      provides: "createBrowserRouter with protected panel routes"
      contains: "createBrowserRouter"
    - path: "apps/web-vendor/src/components/layout/PanelLayout.tsx"
      provides: "sidebar + header + animated outlet"
      contains: "Outlet"
    - path: "apps/web-vendor/src/pages/EarningsPage.tsx"
      provides: "earnings + outstanding balance + settlements (MKT-05)"
      min_lines: 30
  key_links:
    - from: "apps/web-vendor/src/lib/apiClient.ts"
      to: "backend /vendor/*"
      via: "fetch credentials:'include'"
      pattern: "credentials.*include"
    - from: "apps/web-vendor/src/components/layout/Sidebar.tsx"
      to: "COUPONS_ENABLED flag"
      via: "conditional nav item"
      pattern: "COUPONS|coupons"
---

<objective>
Build the complete vendor panel (`apps/web-vendor`) from its placeholder shell: add react-router-dom, the cookie-based apiClient + queryClient, vendor auth store/hook with session probe, the persistent sidebar PanelLayout, role-aware ProtectedVendorRoute, and all vendor pages (Login, AcceptInvite, Dashboard, Products, Inventory & Pricing, Orders, Returns, Earnings & Payouts, Coupons, Team, Store Profile, Settings).

Purpose: This is the vendor-facing deliverable for VEN-01 through VEN-06 and MKT-05. Ends with a human-verify checkpoint because UI/UX and role behavior require visual confirmation.
Output: Full vendor SPA wired to the Phase 6 backend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-vendor-admin-tools/06-CONTEXT.md
@.planning/phases/06-vendor-admin-tools/06-RESEARCH.md
@.planning/phases/06-vendor-admin-tools/06-PATTERNS.md
@apps/web-storefront/src/lib/api-client.ts
@apps/web-storefront/src/lib/query-client.ts
@apps/web-storefront/src/router.tsx
@apps/web-storefront/src/components/layout/AppLayout.tsx
@apps/web-storefront/src/components/layout/ProtectedRoute.tsx
@apps/web-storefront/src/hooks/useAuth.ts
@apps/web-storefront/src/store/ui-store.ts
@apps/web-admin/src/pages/categories/CategoryListPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor panel foundation — router, apiClient, auth store/hook, layout, protected route</name>
  <read_first>
    - apps/web-vendor/package.json (deps — react-router-dom is MISSING, must add)
    - apps/web-vendor/src/App.tsx + main.tsx (current placeholder; main.tsx mounts App — switch to RouterProvider)
    - apps/web-vendor/vite.config.ts (confirm motion/react import works — PATTERNS.md says it does; no alias change needed)
    - apps/web-storefront/src/lib/api-client.ts (exact apiClient with credentials:'include' to copy)
    - apps/web-storefront/src/lib/query-client.ts + router.tsx + hooks/useAuth.ts + components/layout/AppLayout.tsx + ProtectedRoute.tsx + store/ui-store.ts
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (apiClient, queryClient, vendorAuthStore, uiStore, ProtectedVendorRoute, PanelLayout, router sections — exact analogs)
    - packages/contracts/src/vendor/profile.ts (VendorRole for hasRole helper)
  </read_first>
  <action>
    Add `react-router-dom` to apps/web-vendor/package.json (workspace version 7.16.0) and run install.

    Create:
    - `lib/apiClient.ts` — copy storefront api-client.ts; `credentials: 'include'` on every method; ApiError class; BASE_URL from VITE_API_URL. On 401, the queryClient handles it (no retry).
    - `lib/queryClient.ts` — copy storefront query-client.ts (no retry on 401/403/404).
    - `stores/vendorAuthStore.ts` — Zustand store holding `{ vendorUser, role, setVendorUser }` (PATTERNS.md analog).
    - `stores/uiStore.ts` — Zustand with `sidebarCollapsed` + toggle + toasts (PATTERNS.md).
    - `hooks/useVendorAuth.ts` — session probe `GET /vendor/auth/me` (200=auth, 401=null), login mutation `POST /vendor/auth/login`, logout mutation `POST /vendor/auth/logout`; exposes isAuthenticated/isLoading/user/role; sets vendorAuthStore on success. Include a `hasRole(role, requiredRole)` helper implementing the owner>manager>staff hierarchy.
    - `components/layout/ProtectedVendorRoute.tsx` — wait for isLoading; redirect to /auth/login if unauthenticated; optional requiredRole prop redirects to /dashboard if role insufficient (PATTERNS.md).
    - `components/layout/PanelLayout.tsx` — `<Sidebar/> + <Header/> + AnimatePresence mode="wait" Outlet keyed on location.pathname` (import from `motion/react`).
    - `components/layout/Sidebar.tsx` — nav items per D-20: Dashboard, Products, Inventory & Pricing, Orders, Returns, Earnings & Payouts, Coupons, Team, Store Profile, Settings. Hide Coupons when COUPONS_ENABLED is off (fetch the flag via a small public/vendor endpoint or the dashboard payload — use `GET /vendor/dashboard` flag or a feature-flags read; simplest: query `GET /feature-flags` public endpoint and check COUPONS_ENABLED). Hide Team + Store Profile for non-owner roles. Collapsible per uiStore. Active-link styling with grovio tokens.
    - `components/layout/Header.tsx` — breadcrumb + user email + logout button.
    - `router.tsx` — createBrowserRouter: public `/auth/login` + `/accept-invite`; protected `/` → ProtectedVendorRoute → PanelLayout → all section routes (index redirects to /dashboard). Owner-only routes (team, store-profile) wrapped with `<ProtectedVendorRoute requiredRole="owner"/>`.
    - `main.tsx` — render `<QueryClientProvider><RouterProvider router={router}/></QueryClientProvider>`.

    Use grovio design tokens (bg-grovio-surface, border-grovio-border, etc.) per PATTERNS.md.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/web-vendor build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web-vendor/package.json dependencies include react-router-dom
    - lib/apiClient.ts contains `credentials: 'include'` on every fetch method
    - router.tsx uses createBrowserRouter and wraps team + store-profile routes in an owner role guard
    - Sidebar.tsx conditionally renders the Coupons nav item based on COUPONS_ENABLED
    - main.tsx renders RouterProvider (not the placeholder App)
    - `pnpm --filter @grovio/web-vendor build` exits 0
  </acceptance_criteria>
  <done>Vendor panel boots with cookie auth, session probe, protected role-aware routing, and persistent sidebar layout.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor pages — auth, dashboard, inventory, orders, returns, earnings, team, profile, settings, coupons</name>
  <read_first>
    - apps/web-admin/src/pages/categories/CategoryListPage.tsx (FULL — useQuery/useMutation, motion entry, loading/error/empty states, slide-over panel, form inputs, submit button — the page template for ALL pages)
    - apps/web-vendor/src/lib/apiClient.ts + hooks/useVendorAuth.ts (from Task 1)
    - packages/contracts/src/vendor/* (response/input types for each page)
    - .planning/phases/06-vendor-admin-tools/06-CONTEXT.md (D-08/D-09 earnings, D-15 inventory inline edit, D-16 returns approve/reject, D-14 coupons)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (Page Entry Animation, React Query, Tailwind token sections)
  </read_first>
  <action>
    Build each page using the CategoryListPage template (motion entry, React Query data fetch, loading/error/empty states, grovio tokens). All money displayed by dividing minor units by 100 with the configured currency:

    - `auth/LoginPage.tsx` — email+password form → useVendorAuth.login; redirect to /dashboard (or the `from` location) on success.
    - `auth/AcceptInvitePage.tsx` — reads `?token=` from URL; password form → `POST /vendor/team/accept-invite`; on success redirect to /auth/login with a success message.
    - `DashboardPage.tsx` (VEN-02) — period toggle (7d/30d/90d) → `GET /vendor/dashboard`; KPI cards (orders, gross sales, net earnings, outstanding balance); Low-stock alerts list (product name, qty, link to /inventory).
    - `ProductsPage.tsx` — list vendor products via existing `GET /vendor/products`; link out to create/edit (reuse existing Phase 3 vendor product endpoints; this page is the management list).
    - `InventoryPage.tsx` (VEN-03, D-15) — combined table: product/variant name, quantity_available (editable), quantity_reserved (read-only), base_price_minor (editable). Inline click-to-edit or row modal → `PATCH /vendor/inventory/:id` (qty) and `PATCH /vendor/products/:productId/pricing` (price). Show a toast on save.
    - `OrdersPage.tsx` (VEN-04) — list vendor sub-orders via `GET /vendor/orders`; status update via the existing patch endpoint; staff can mark shipped/delivered only (hide other actions per role).
    - `ReturnsPage.tsx` (VEN-04, D-16) — list `GET /vendor/returns` filtered by status; per-row Approve (optional note) → `POST /vendor/returns/:id/approve`; Reject (required reason) → `POST /vendor/returns/:id/reject`. manager+owner only.
    - `EarningsPage.tsx` (MKT-05, D-09) — `GET /vendor/earnings`; highlighted Outstanding Balance card; summary strip (earned/reversed/net/settled); commission ledger table; settlements received table.
    - `CouponsPage.tsx` (VEN-06, D-14) — `GET /vendor/coupons` + create form (code, type flat/percent, value, min order, expiry, max redemptions) → `POST /vendor/coupons`. (Nav hidden when flag off, but page also handles a 404 gracefully.)
    - `TeamPage.tsx` (VEN-05) — owner only; list staff `GET /vendor/team`; invite form (email + role manager|staff) → `POST /vendor/team/invite`; remove (soft-delete) staff.
    - `StoreProfilePage.tsx` (VEN-01) — owner only; edit D-01 profile fields (URL inputs for logo/banner — no upload) → `PATCH /vendor/profile`; separate owner-only payout-info form → `PATCH /vendor/profile/payout-info`.
    - `SettingsPage.tsx` (VEN-01) — owner only; Return Policy section (return window days, returns enabled) → `PATCH /vendor/profile/return-policy` (completing Phase 5 D-22 setup).

    Use AnimatePresence slide-over panels for create/edit forms (CategoryListPage analog). Wire React Query invalidation after each mutation.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/web-vendor build && pnpm --filter @grovio/web-vendor lint</automated>
  </verify>
  <acceptance_criteria>
    - EarningsPage.tsx renders an outstanding-balance value sourced from `GET /vendor/earnings`
    - InventoryPage.tsx renders quantity_reserved as read-only and issues PATCH to /vendor/inventory/:id and /vendor/products/:productId/pricing
    - ReturnsPage.tsx Reject action requires a non-empty reason before enabling submit
    - TeamPage.tsx invite role select offers only manager and staff (not owner)
    - All pages display money by dividing minor units (no raw minor-unit numbers shown to the user)
    - `pnpm --filter @grovio/web-vendor build` exits 0
  </acceptance_criteria>
  <done>All vendor pages implemented against the Phase 6 backend with role-aware behavior and minor-unit money formatting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify vendor panel end-to-end</name>
  <what-built>Complete vendor panel: cookie login, role-aware sidebar, dashboard KPIs + low-stock, inline inventory/pricing, orders, return approve/reject, earnings + outstanding balance, team invite/accept, store profile + payout info + return policy, feature-gated coupons.</what-built>
  <how-to-verify>
    1. Start backend (`pnpm --filter @grovio/api dev`) and vendor panel (`pnpm --filter @grovio/web-vendor dev`).
    2. Log in as an existing vendor owner (credentials migrated to vendor_users in 06-03). Confirm redirect to /dashboard with KPI cards.
    3. Confirm sidebar shows Team + Store Profile (owner). Toggle COUPONS_ENABLED off in admin/DB and confirm the Coupons nav item disappears.
    4. Inventory & Pricing: edit a quantity and a price inline; confirm save toast and that the value persists on refresh.
    5. Returns: approve one return (confirm wallet/payment refund happens) and reject another with a reason.
    6. Team: invite a staff member; check the invite email/console link; open /accept-invite?token=..., set a password, then log in as that staff user and confirm Team + Store Profile are NOT visible and pricing edit is blocked.
    7. Earnings: confirm the Outstanding Balance card matches earned - reversed - settled.
  </how-to-verify>
  <resume-signal>Type "approved" once all flows work and role restrictions hold, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /vendor/* | Cookie-based auth; frontend hides UI by role but the API is the authority (D-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-30 | Elevation of Privilege | Staff seeing/using owner-only UI | mitigate | Frontend hides owner-only nav/routes; backend (06-08) is the enforcing authority — UI hiding is UX only. |
| T-06-31 | Information Disclosure | Banking data cached in client | accept | Payout-info shown only on owner profile page on demand; not persisted client-side beyond the form session. Backend isolates it (D-02). |
| T-06-SC | Tampering | npm installs (react-router-dom) | mitigate | react-router-dom 7.16.0 already installed workspace-wide (RESEARCH.md Package Legitimacy Audit: Approved); no net-new package. |
</threat_model>

<verification>
- `pnpm --filter @grovio/web-vendor build` and `lint` pass.
- Human checkpoint confirms login, role hiding, inline edits, returns, team invite/accept, earnings.
</verification>

<success_criteria>
Vendor panel fully functional for VEN-01..06 + MKT-05; role restrictions enforced visually and confirmed against backend; coupons feature-gated; money shown in major units.
</success_criteria>

<output>
Create `.planning/phases/06-vendor-admin-tools/06-09-SUMMARY.md` when done.
</output>
