---
phase: 06-vendor-admin-tools
plan: 08
type: execute
wave: 6
depends_on: [06-06, 06-07]
files_modified:
  - apps/api/src/routes/admin/auth.ts
  - apps/api/src/routes/admin/vendors.ts
  - apps/api/src/routes/admin/commission-rules.ts
  - apps/api/src/routes/admin/payouts.ts
  - apps/api/src/routes/admin/homepage-blocks.ts
  - apps/api/src/routes/admin/feature-flags.ts
  - apps/api/src/routes/admin/settings.ts
  - apps/api/src/routes/admin/audit-log.ts
  - apps/api/src/routes/admin/analytics.ts
  - apps/api/src/routes/admin/categories.ts
  - apps/api/src/routes/admin/products.ts
  - apps/api/src/routes/vendor/auth.ts
  - apps/api/src/routes/vendor/team.ts
  - apps/api/src/routes/vendor/profile.ts
  - apps/api/src/routes/vendor/inventory.ts
  - apps/api/src/routes/vendor/returns.ts
  - apps/api/src/routes/vendor/earnings.ts
  - apps/api/src/routes/vendor/coupons.ts
  - apps/api/src/routes/vendor/dashboard.ts
  - apps/api/src/container.ts
  - apps/api/src/app.ts
autonomous: true
requirements: [MKT-04, MKT-05, VEN-01, VEN-02, VEN-03, VEN-04, VEN-05, VEN-06, ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06, ADM-07]
must_haves:
  truths:
    - "All new admin routes are guarded by requireAdminAuth; the X-Internal-Admin-Token placeholder is removed from existing admin routes (Pitfall 2)"
    - "All new vendor routes are guarded by requireVendorAuth; owner-only and manager-only routes enforce role server-side (D-05)"
    - "Admin and vendor login set httpOnly cookies; auth/me + logout endpoints exist for both panels"
    - "All Phase 6 services are registered in the Awilix container and all route plugins registered in app.ts"
  artifacts:
    - path: "apps/api/src/routes/admin/auth.ts"
      provides: "admin login/logout/me (httpOnly cookie)"
      contains: "/admin/auth/login"
    - path: "apps/api/src/container.ts"
      provides: "Phase 6 service registrations"
      contains: "adminAuthService"
    - path: "apps/api/src/app.ts"
      provides: "all Phase 6 route plugin registrations"
      contains: "adminAuthRoutes"
  key_links:
    - from: "apps/api/src/routes/admin/categories.ts"
      to: "requireAdminAuth"
      via: "preHandler replacing X-Internal-Admin-Token"
      pattern: "requireAdminAuth"
    - from: "apps/api/src/app.ts"
      to: "all Phase 6 route plugins"
      via: "fastify.register"
      pattern: "register\\("
---

<objective>
Wire all Phase 6 backend HTTP surface: new admin route plugins (auth, vendors, commission-rules, payouts, homepage-blocks, feature-flags, settings, audit-log, analytics), new vendor route plugins (team, profile, inventory, returns, earnings, coupons, dashboard), upgrade admin login/logout/me on the existing auth routes, replace the X-Internal-Admin-Token placeholder with `requireAdminAuth` on ALL admin routes (Pitfall 2), enforce role guards on vendor routes (D-05), register every Phase 6 service in the Awilix container, and register every plugin in app.ts.

Purpose: Without this plan the services from 06-06/06-07 are unreachable. This is the integration seam that exposes every requirement's endpoint.
Output: 17 route files + container + app.ts wired and reachable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-vendor-admin-tools/06-CONTEXT.md
@.planning/phases/06-vendor-admin-tools/06-PATTERNS.md
@apps/api/src/app.ts
@apps/api/src/container.ts
@apps/api/src/routes/vendor/orders.ts
@apps/api/src/routes/admin/categories.ts
@apps/api/src/routes/vendor/auth.ts
@apps/api/src/middleware/vendorAuth.ts
@apps/api/src/middleware/adminAuth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin route plugins + replace X-Internal-Admin-Token with requireAdminAuth</name>
  <read_first>
    - apps/api/src/routes/admin/categories.ts (FULL — current X-Internal-Admin-Token preHandler to REPLACE; Zod body + error envelope patterns)
    - apps/api/src/routes/admin/products.ts (same placeholder preHandler to replace)
    - apps/api/src/routes/vendor/auth.ts (httpOnly setCookie login pattern for admin auth)
    - apps/api/src/middleware/adminAuth.ts (requireAdminAuth from 06-04)
    - packages/contracts/src/admin/* (input/response schemas for every endpoint)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (admin/auth.ts + admin/categories.ts route sections)
  </read_first>
  <action>
    Create these admin route plugins, each `fastify.addHook("preHandler", requireAdminAuth)` (except auth.ts login which is public), each resolving services via `fastify.diContainer.resolve`, each Zod-validating bodies and using the `{ success, data }` / `{ success, error: { code, message } }` envelope:

    - `admin/auth.ts` (D-21): `POST /admin/auth/login` (public) → adminAuthService.login, setCookie("admin_token", token, httpOnly+sameSite lax+path/+maxAge); `POST /admin/auth/logout` (clears cookie); `GET /admin/auth/me` (requireAdminAuth → returns admin profile). Note: requireAdminAuth must read the token from the cookie OR Authorization header — extend adminAuth.ts to also check `request.cookies.admin_token` (mirror how customerAuth reads the cookie). Update middleware accordingly.
    - `admin/vendors.ts` (ADM-02): `GET /admin/vendors` (list, offset paging), `POST /admin/vendors/:id/approve`, `POST /admin/vendors/:id/suspend`, `POST /admin/vendors/:id/reinstate`, `PATCH /admin/vendors/:id/configure` → VendorManagementService; pass adminEmail from request.adminEmail + request.ip.
    - `admin/commission-rules.ts` (ADM-03): `GET /admin/commission-rules`, `POST`, `PATCH /:id`, `DELETE /:id` → CommissionService rule CRUD (DELETE on global returns 403 via the service's protected error).
    - `admin/payouts.ts` (MKT-04): `GET /admin/payouts/:vendorId` (per-vendor view), `POST /admin/payouts/:vendorId/settlements` → PayoutService.
    - `admin/homepage-blocks.ts` (ADM-04): `GET /admin/homepage-blocks` (admin list incl inactive), `POST`, `PATCH /:id`, `DELETE /:id`, `POST /:id/reorder` → HomepageService.
    - `admin/feature-flags.ts` (ADM-06): `GET /admin/feature-flags`, `PATCH /admin/feature-flags/:key` → FeatureFlagService.toggleFlag.
    - `admin/settings.ts` (ADM-05): `GET /admin/settings`, `PATCH /admin/settings/:key` → SettingsService.
    - `admin/audit-log.ts` (ADM-07): `GET /admin/audit-log` (filterable query) → AuditService.query.
    - `admin/analytics.ts` (ADM-01): `GET /admin/analytics/summary?period=`, `GET /admin/analytics/charts?period=` → AnalyticsService.

    Then EDIT `admin/categories.ts` and `admin/products.ts`: remove the X-Internal-Admin-Token preHandler/header logic entirely and replace with `fastify.addHook("preHandler", requireAdminAuth)` (Pitfall 2 — the placeholder is dead code).
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/routes/admin/categories.ts and admin/products.ts no longer contain the string `X-Internal-Admin-Token` and DO contain `requireAdminAuth`
    - admin/auth.ts `POST /admin/auth/login` sets an httpOnly cookie named `admin_token`
    - adminAuth middleware reads the token from `request.cookies.admin_token` as well as the Authorization header
    - admin/commission-rules.ts DELETE handler maps the global-rule protected error to a 403 response
    - Each admin route file (except auth login/logout) registers `requireAdminAuth` as a preHandler
    - `pnpm --filter @grovio/api typecheck` exits 0
  </acceptance_criteria>
  <done>All 9 admin route plugins created; both existing admin route files migrated to requireAdminAuth; placeholder token removed.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor route plugins with role guards + coupon feature-flag gate</name>
  <read_first>
    - apps/api/src/routes/vendor/orders.ts (FULL — preHandler requireVendorAuth, getVendorId helper, diContainer.resolve, error envelope)
    - apps/api/src/routes/vendor/auth.ts (add /vendor/auth/me + /vendor/auth/logout; httpOnly cookie login)
    - apps/api/src/middleware/vendorAuth.ts (request.vendorId + request.vendorRole from 06-04)
    - apps/api/src/modules/feature-flags/FeatureFlagService.ts (COUPONS_ENABLED check for the coupons gate)
    - packages/contracts/src/vendor/* (input/response schemas)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (vendor route role-check preHandler snippet)
    - .planning/phases/06-vendor-admin-tools/06-CONTEXT.md (D-05 role scopes, D-14 coupon feature gate, D-15, D-16)
  </read_first>
  <action>
    Create vendor route plugins, all guarded by `requireVendorAuth`, scoping data by `request.vendorId` (FK to vendors.id), with per-route role checks (a helper that 403s unless `request.vendorRole` is in an allowed set per D-05):

    - `vendor/dashboard.ts` (VEN-02): `GET /vendor/dashboard?period=` → AnalyticsService.getVendorDashboard(request.vendorId, period). All roles.
    - `vendor/profile.ts` (VEN-01): `GET /vendor/profile`, `PATCH /vendor/profile` (owner only), `GET/PATCH /vendor/profile/payout-info` (owner only — banking), `GET/PATCH /vendor/profile/return-policy` (owner only) → VendorProfileService.
    - `vendor/inventory.ts` (VEN-03): `GET /vendor/inventory` (list combined inventory+pricing), `PATCH /vendor/inventory/:inventoryItemId` (quantity — staff+manager+owner), `PATCH /vendor/products/:productId/pricing` (price — manager+owner only) → InventoryService.
    - `vendor/returns.ts` (VEN-04): `GET /vendor/returns`, `POST /vendor/returns/:id/approve` → ReturnService.approveReturn, `POST /vendor/returns/:id/reject` → ReturnService.rejectReturn (manager+owner only).
    - `vendor/earnings.ts` (MKT-05): `GET /vendor/earnings` → AnalyticsService.getVendorEarnings(request.vendorId) (owner+manager).
    - `vendor/team.ts` (VEN-05): `GET /vendor/team`, `POST /vendor/team/invite`, `DELETE /vendor/team/:userId` → VendorStaffService (ALL owner-only). Plus a PUBLIC `POST /vendor/team/accept-invite` route (no requireVendorAuth — the invitee has no account yet) → VendorStaffService.accept. Put the public accept route in a separate plugin or register it before the addHook guard.
    - `vendor/coupons.ts` (VEN-06, D-14): `GET /vendor/coupons`, `POST /vendor/coupons` → CouponService scoped to the vendor (created_by_type='vendor', created_by_id=vendorId, scope='vendor'); manager+owner. Gate the WHOLE plugin behind COUPONS_ENABLED — each handler first checks `featureFlagService.getFlag("COUPONS_ENABLED")`; if disabled, return 404.

    EDIT `vendor/auth.ts`: add `GET /vendor/auth/me` (returns the vendor_users profile from the JWT) and `POST /vendor/auth/logout`. Keep login but switch it to ALSO set an httpOnly cookie (`vendor_token`) in addition to / instead of returning the bearer token, to match the panel's cookie auth (mirror admin/auth.ts). Update vendorAuth middleware to also read `request.cookies.vendor_token`.

    Create the role-guard helper (e.g., `requireVendorRole(...allowed)`) in a shared spot (middleware or a small util) and apply per route.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - vendor/team.ts invite/list/delete routes return 403 when request.vendorRole !== 'owner'
    - vendor/team accept-invite route is PUBLIC (not behind requireVendorAuth)
    - vendor/inventory.ts pricing route is restricted to manager+owner; quantity route allows staff
    - vendor/coupons.ts handlers return 404 when COUPONS_ENABLED flag is off
    - vendor/profile payout-info routes are owner-only
    - vendorAuth middleware reads `request.cookies.vendor_token`
    - `pnpm --filter @grovio/api typecheck` exits 0
  </acceptance_criteria>
  <done>All 8 vendor route plugins (incl auth extensions) created with D-05 role enforcement and coupon feature gate.</done>
</task>

<task type="auto">
  <name>Task 3: Register all Phase 6 services in container + all plugins in app.ts</name>
  <read_first>
    - apps/api/src/container.ts (FULL — asClass registration block + import grouping; add Phase 6 services)
    - apps/api/src/app.ts (FULL — route import + register ordering; cookie + cors already registered)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (container.ts section: Phase 6 service registration block)
  </read_first>
  <action>
    In `container.ts`: add a `// Phase 6 services` block registering as singletons: `adminAuthService`, `settingsService`, `auditService`, `vendorManagementService`, `vendorStaffService`, `analyticsService`, `vendorProfileService`, `payoutService`. Add matching imports. Confirm the services that gained new deps in 06-06/06-07 (CommissionService gained auditService; FeatureFlagService gained auditService; HomepageService gained auditService; InventoryService gained productIndexQueue if not already; ReturnService unchanged; PayoutService needs analyticsService + auditService; VendorManagementService needs auditService; VendorStaffService needs mailer+env) resolve correctly — Awilix injects by name, so each dep name must match a registered key.

    In `app.ts`: import and register all new route plugins after the existing Phase 5 registrations, in a `// --- Routes (Phase 6) ---` block: adminAuthRoutes, adminVendorRoutes, adminCommissionRuleRoutes, adminPayoutRoutes, adminHomepageBlockRoutes, adminFeatureFlagRoutes, adminSettingsRoutes, adminAuditLogRoutes, adminAnalyticsRoutes, vendorDashboardRoutes, vendorProfileRoutes, vendorInventoryRoutes, vendorReturnRoutes, vendorEarningsRoutes, vendorTeamRoutes (+ the public accept-invite plugin), vendorCouponRoutes. Cookie + CORS are already registered before routes (verified) — no change needed there.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api typecheck && pnpm --filter @grovio/api test --run</automated>
  </verify>
  <acceptance_criteria>
    - container.ts registers adminAuthService, settingsService, auditService, vendorManagementService, vendorStaffService, analyticsService, vendorProfileService, payoutService (all asClass singleton)
    - app.ts registers all 9 admin + all vendor Phase 6 route plugins
    - The app boots without an Awilix resolution error (test suite or a boot smoke test passes)
    - `pnpm --filter @grovio/api test --run` (full suite) passes
  </acceptance_criteria>
  <done>All Phase 6 services DI-registered and all route plugins mounted; full backend test suite green; API boots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /admin/* | Must require admin JWT (cookie or bearer); the Phase 2 dev token bypass must be eliminated. |
| client → /vendor/* | Must require vendor JWT and enforce role scope per route. |
| public → /vendor/team/accept-invite | Intentionally unauthenticated (invitee has no account) — guarded by token validity only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-25 | Authentication bypass | Admin endpoints reachable via X-Internal-Admin-Token | mitigate | All admin routes (incl existing categories/products) switched to requireAdminAuth; token header logic deleted (Pitfall 2). |
| T-06-26 | Elevation of Privilege | Staff hitting owner-only routes (team, payout-info) | mitigate | requireVendorRole guard 403s unless role in allowed set (D-05). |
| T-06-27 | Information Disclosure | Coupons surface when feature disabled | mitigate | vendor/coupons handlers 404 when COUPONS_ENABLED off (D-14). |
| T-06-28 | Elevation of Privilege | IDOR across vendors via path params | mitigate | All vendor handlers scope by request.vendorId from JWT; path-supplied vendorId never trusted for scoping. |
| T-06-29 | Elevation of Privilege | accept-invite route abused | mitigate | Public route validates token expiry + acceptedAt in VendorStaffService; no role granted beyond invite (06-06). |
| T-06-SC | Tampering | npm installs | mitigate | No installs. |
</threat_model>

<verification>
- `pnpm --filter @grovio/api typecheck` and full `test --run` pass.
- Grep: no `X-Internal-Admin-Token` remains in apps/api/src/routes/admin/.
- API boots (smoke) without Awilix unresolved-dependency errors.
</verification>

<success_criteria>
Every Phase 6 endpoint is reachable, admin auth enforced everywhere (no token bypass), vendor role scopes enforced, coupons feature-gated, all services DI-registered, full suite green.
</success_criteria>

<output>
Create `.planning/phases/06-vendor-admin-tools/06-08-SUMMARY.md` when done.
</output>
