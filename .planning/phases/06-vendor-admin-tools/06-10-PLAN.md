---
phase: 06-vendor-admin-tools
plan: 10
type: execute
wave: 7
depends_on: [06-08]
files_modified:
  - apps/web-admin/src/lib/apiClient.ts
  - apps/web-admin/src/stores/adminAuthStore.ts
  - apps/web-admin/src/hooks/useAdminAuth.ts
  - apps/web-admin/src/components/layout/PanelLayout.tsx
  - apps/web-admin/src/components/layout/Sidebar.tsx
  - apps/web-admin/src/components/layout/Header.tsx
  - apps/web-admin/src/components/layout/ProtectedAdminRoute.tsx
  - apps/web-admin/src/components/charts/MiniChart.tsx
  - apps/web-admin/src/pages/auth/LoginPage.tsx
  - apps/web-admin/src/pages/DashboardPage.tsx
  - apps/web-admin/src/pages/VendorsPage.tsx
  - apps/web-admin/src/pages/CatalogModerationPage.tsx
  - apps/web-admin/src/pages/CommissionRulesPage.tsx
  - apps/web-admin/src/pages/PayoutManagementPage.tsx
  - apps/web-admin/src/pages/CmsPage.tsx
  - apps/web-admin/src/pages/FeatureFlagsPage.tsx
  - apps/web-admin/src/pages/SettingsPage.tsx
  - apps/web-admin/src/pages/AuditLogPage.tsx
  - apps/web-admin/src/App.tsx
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06, ADM-07, MKT-04]
must_haves:
  truths:
    - "Admin logs in via cookie auth (X-Internal-Admin-Token removed) and sees a sidebar control-plane panel"
    - "Existing Phase 2 category routes are preserved under the new PanelLayout (not deleted)"
    - "Admin can view KPI dashboard with charts, manage vendors, moderate catalog, configure commission rules (global undeletable), record payouts, manage CMS blocks, toggle feature flags, edit settings/branding, and filter the audit log"
  artifacts:
    - path: "apps/web-admin/src/App.tsx"
      provides: "createBrowserRouter with PanelLayout + protected routes + preserved category routes"
      contains: "createBrowserRouter"
    - path: "apps/web-admin/src/lib/apiClient.ts"
      provides: "cookie auth apiClient (no admin token header)"
      contains: "credentials"
    - path: "apps/web-admin/src/pages/PayoutManagementPage.tsx"
      provides: "per-vendor payout ledger + settlement recording (MKT-04)"
      min_lines: 30
  key_links:
    - from: "apps/web-admin/src/lib/apiClient.ts"
      to: "backend /admin/*"
      via: "credentials:'include' (token header removed)"
      pattern: "credentials"
    - from: "apps/web-admin/src/App.tsx"
      to: "existing category pages"
      via: "preserved routes under PanelLayout"
      pattern: "categories"
---

<objective>
Upgrade the admin panel (`apps/web-admin`): replace the X-Internal-Admin-Token apiClient with cookie auth, migrate App.tsx from `<Routes>` to `createBrowserRouter` with a PanelLayout sidebar shell while preserving the existing Phase 2 category routes, add an admin login page + auth store/hook + ProtectedAdminRoute, and build all admin pages (Dashboard with KPIs/charts, Vendors, Catalog Moderation, Commission Rules, Payout Management, CMS/Homepage, Feature Flags, Settings & Branding, Audit Log).

Purpose: This is the admin control-plane deliverable for ADM-01 through ADM-07 and MKT-04. Begins with a charting-approach decision checkpoint and ends with a human-verify checkpoint.
Output: Full admin SPA with cookie auth and all control-plane pages.
</objective>

<skill_hint>
Use the `/frontend-design` skill when generating UI components, pages, and layouts in this plan. It produces production-grade, distinctively styled React interfaces — invoke it before writing any new page or component.
Use the `/git-workflow` skill for all branching, committing, and merging throughout this plan. Each plan executes on its own feature branch — never accumulate work across plans or phases on one branch.
</skill_hint>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-vendor-admin-tools/06-CONTEXT.md
@.planning/phases/06-vendor-admin-tools/06-RESEARCH.md
@.planning/phases/06-vendor-admin-tools/06-PATTERNS.md
@apps/web-admin/src/App.tsx
@apps/web-admin/src/lib/apiClient.ts
@apps/web-admin/src/pages/categories/CategoryListPage.tsx
@apps/web-storefront/src/lib/api-client.ts
@apps/web-storefront/src/components/layout/ProtectedRoute.tsx
@apps/web-storefront/src/router.tsx
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide admin dashboard charting approach</name>
  <decision>How to render the ADM-01 dashboard charts (orders-by-day line, GMV-by-category bar)?</decision>
  <context>RESEARCH.md found no charting library installed (recharts is [ASSUMED] absent). D-10 needs one line chart + one bar chart with a small number of data points. Adding a dependency requires the package-legitimacy checkpoint; inline SVG avoids a new dependency.</context>
  <options>
    <option id="inline-svg">
      <name>Inline SVG MiniChart component</name>
      <pros>Zero new dependencies; no legitimacy gate; full control; sufficient for v1's small datasets</pros>
      <cons>More component code to write; less polished than a charting lib for future complex charts</cons>
    </option>
    <option id="recharts">
      <name>Add recharts</name>
      <pros>Richer charts with less code; easier to extend later</pros>
      <cons>New dependency → requires package-legitimacy human-verify checkpoint before install; heavier bundle</cons>
    </option>
  </options>
  <resume-signal>Select: inline-svg or recharts. (If recharts, a blocking package-legitimacy checkpoint will be inserted before install.)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Admin panel foundation — cookie apiClient, auth, PanelLayout, router migration (preserve category routes)</name>
  <read_first>
    - apps/web-admin/src/lib/apiClient.ts (FULL — REMOVE X-Internal-Admin-Token + VITE_INTERNAL_ADMIN_TOKEN; ADD credentials:'include')
    - apps/web-admin/src/App.tsx (FULL — current `<Routes>` with /categories routes to PRESERVE; migrate to createBrowserRouter)
    - apps/web-admin/src/pages/categories/* (existing pages — must remain reachable, unchanged)
    - apps/web-storefront/src/lib/api-client.ts + components/layout/ProtectedRoute.tsx + router.tsx (analogs)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (admin apiClient update, PanelLayout (framer-motion import), App.tsx migration, ProtectedAdminRoute sections)
    - packages/contracts/src/admin/auth.ts
  </read_first>
  <action>
    Update `lib/apiClient.ts`: remove the `ADMIN_TOKEN`/`VITE_INTERNAL_ADMIN_TOKEN` constant, the warning, and the `X-Internal-Admin-Token` header injection; add `credentials: 'include'` to every fetch call (Pitfall 2). Keep the ApiError class and get/post/patch/put/del helpers. On 401, redirect to /auth/login.

    Create:
    - `stores/adminAuthStore.ts` — Zustand `{ admin, setAdmin }`.
    - `hooks/useAdminAuth.ts` — session probe `GET /admin/auth/me`, login `POST /admin/auth/login`, logout `POST /admin/auth/logout`; exposes isAuthenticated/isLoading/admin.
    - `components/layout/ProtectedAdminRoute.tsx` — wait isLoading; redirect to /auth/login if unauthenticated.
    - `components/layout/PanelLayout.tsx` — Sidebar + Header + AnimatePresence Outlet. IMPORTANT: web-admin imports from `framer-motion` (not motion/react) per PATTERNS.md.
    - `components/layout/Sidebar.tsx` — nav per D-20: Dashboard, Vendors, Catalog Moderation, Commission Rules, Payout Management, CMS / Homepage, Feature Flags, Settings & Branding, Audit Log, plus a Categories section linking to the existing /categories pages.
    - `components/layout/Header.tsx` — breadcrumb + admin email + logout.

    Migrate `App.tsx` to `createBrowserRouter` + `RouterProvider`: public `/auth/login`; protected `/` → ProtectedAdminRoute → PanelLayout → all admin pages AND the preserved `/categories` + `/categories/:id` routes (do NOT delete them — Pitfall: preserve Phase 2 routes). Index redirects to /dashboard.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/web-admin build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web-admin/src/lib/apiClient.ts no longer contains `X-Internal-Admin-Token` or `VITE_INTERNAL_ADMIN_TOKEN` and contains `credentials: 'include'`
    - App.tsx uses createBrowserRouter and still registers the /categories and /categories/:id routes under PanelLayout
    - PanelLayout.tsx imports AnimatePresence from 'framer-motion' (not 'motion/react')
    - ProtectedAdminRoute redirects to /auth/login when unauthenticated
    - `pnpm --filter @grovio/web-admin build` exits 0
  </acceptance_criteria>
  <done>Admin panel uses cookie auth, sidebar PanelLayout, protected routing; existing category pages preserved and reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Admin pages — login, dashboard+charts, vendors, moderation, commission rules, payouts, CMS, flags, settings, audit log</name>
  <read_first>
    - apps/web-admin/src/pages/categories/CategoryListPage.tsx (FULL — the page template: useQuery/useMutation, motion entry, loading/error/empty, slide-over panel, form inputs, submit button)
    - apps/web-admin/src/lib/apiClient.ts + hooks/useAdminAuth.ts (from Task 2)
    - packages/contracts/src/admin/* (every page's input/response types)
    - .planning/phases/06-vendor-admin-tools/06-CONTEXT.md (D-08 payout view, D-10 dashboard, D-11 CMS, D-12 flags, D-13 audit, D-17 vendors, D-18 commission rules, D-19 settings)
    - the charting decision from Task 1
  </read_first>
  <action>
    Build pages using the CategoryListPage template (motion entry, React Query, loading/error/empty states, grovio tokens, slide-over forms). Money displayed in major units (minor/100).

    - `auth/LoginPage.tsx` — email+password → useAdminAuth.login; redirect to /dashboard.
    - `components/charts/MiniChart.tsx` — per Task-1 decision (inline SVG line + bar, OR recharts wrappers).
    - `DashboardPage.tsx` (ADM-01, D-10) — 7d/30d/90d toggle → `GET /admin/analytics/summary` + `GET /admin/analytics/charts`; KPI cards (GMV, Orders, Active Vendors, New Customers, Commission Earned, Pending Payouts); orders-by-day line chart; Top 5 vendors by GMV table; GMV-by-category bar chart. Manual refresh button (no polling).
    - `VendorsPage.tsx` (ADM-02, D-17) — paginated `GET /admin/vendors` table with onboarding_status colored badges (pending=amber, approved=green, suspended=red, archived=gray); row actions Approve/Suspend/Reinstate; Configure side panel (category restrictions + commission override) → respective endpoints.
    - `CatalogModerationPage.tsx` (ADM-07) — list pending vendor products via existing `GET /admin/products`; approve/reject via existing admin product endpoints (now cookie-guarded).
    - `CommissionRulesPage.tsx` (ADM-03, D-18) — three sections: Global rate (pinned top row, no delete button, inline edit), Category overrides (add/edit/delete), Vendor overrides (add/edit/delete) → `/admin/commission-rules` CRUD. DELETE on global is not offered in UI; a 403 from the server is surfaced gracefully.
    - `PayoutManagementPage.tsx` (MKT-04, D-08) — vendor selector → `GET /admin/payouts/:vendorId`; summary strip (earned/reversed/net/settled/outstanding highlighted); commission ledger table; settlements table; read-only payout-info panel; Record Settlement form (amount as decimal string, reference, note) → `POST /admin/payouts/:vendorId/settlements`.
    - `CmsPage.tsx` (ADM-04, D-11) — ordered homepage block list (`GET /admin/homepage-blocks`); per block: type badge, active toggle, Edit/Delete, Up/Down arrows (reorder endpoint); type-specific edit modal (banner/product_grid/text_block/featured_categories) with URL-only image inputs. No drag-and-drop.
    - `FeatureFlagsPage.tsx` (ADM-06, D-12) — `GET /admin/feature-flags` toggle list; toggle → `PATCH /admin/feature-flags/:key`.
    - `SettingsPage.tsx` (ADM-05, D-19) — grouped sections General/Branding/Email/Integration-visibility → `GET /admin/settings` + `PATCH /admin/settings/:key`. Integration secret keys shown masked/read-only.
    - `AuditLogPage.tsx` (ADM-07, D-13) — filterable table (actor, action type, entity type, date range) → `GET /admin/audit-log` with offset pagination; show before/after diffs.

    Invalidate React Query after each mutation.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/web-admin build && pnpm --filter @grovio/web-admin lint</automated>
  </verify>
  <acceptance_criteria>
    - CommissionRulesPage renders the global rate as a pinned row with NO delete button
    - PayoutManagementPage settlement form submits `amount` as a decimal string (not a pre-multiplied minor-unit number)
    - VendorsPage renders onboarding_status as colored badges (pending/approved/suspended/archived)
    - CmsPage reorders blocks via Up/Down buttons calling the reorder endpoint (no drag-and-drop library used)
    - SettingsPage shows integration secret keys masked/read-only
    - All money values displayed in major units (minor/100)
    - `pnpm --filter @grovio/web-admin build` exits 0
  </acceptance_criteria>
  <done>All admin control-plane pages implemented against the cookie-guarded Phase 6 backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify admin panel end-to-end</name>
  <what-built>Complete admin panel: cookie login, sidebar control plane, KPI dashboard + charts, vendor approve/suspend/configure, catalog moderation, commission rule CRUD (global protected), per-vendor payout ledger + settlement recording, CMS block management, feature-flag toggles, settings/branding, filterable audit log — with existing category pages preserved.</what-built>
  <how-to-verify>
    1. Seed an admin_users row (insert one with an argon2-hashed password — note the seeding method in the SUMMARY). Start backend + admin panel (`pnpm --filter @grovio/web-admin dev`).
    2. Log in; confirm redirect to /dashboard with KPI cards + both charts rendering for 7d/30d/90d.
    3. Confirm the existing Categories pages still work (Pitfall: must be preserved).
    4. Vendors: approve a pending vendor, suspend an approved one (confirm that vendor can no longer log into the vendor panel), reinstate it.
    5. Commission Rules: confirm the Global row has no delete; add a category override; attempt to delete global via devtools/API and confirm 403.
    6. Payouts: select a vendor, confirm summary strip math, record a settlement (₹ decimal), confirm it appears append-only and the audit log records 'payout.settled'.
    7. CMS: create/edit/reorder/delete a homepage block; confirm the storefront homepage reflects the change within one refresh (cache invalidated).
    8. Feature Flags: toggle COUPONS_ENABLED; confirm the vendor panel Coupons nav reacts.
    9. Settings: change a value (e.g., low_stock_threshold); Audit Log: confirm the action is recorded and filters work.
  </how-to-verify>
  <resume-signal>Type "approved" once all admin flows work, global rule is protected, category pages are intact, and audit entries appear, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /admin/* | Cookie-based admin JWT; the Phase 2 token bypass must be gone end-to-end. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-32 | Authentication bypass | Admin panel reachable with the old token | mitigate | apiClient sends no X-Internal-Admin-Token; backend (06-08) requires admin JWT cookie; placeholder removed. |
| T-06-33 | Tampering | Global commission rule deleted from UI | mitigate | UI offers no delete for global; backend returns 403; surfaced gracefully (D-18). |
| T-06-34 | Tampering | Inflated settlement entered in UI | mitigate | Settlement form sends decimal string; backend converts to minor units server-side and stores append-only (Pitfall 5). |
| T-06-35 | Information Disclosure | Integration secret keys editable/exposed | mitigate | Settings page renders secret keys masked/read-only (D-19). |
| T-06-SC | Tampering | npm installs (recharts if chosen) | mitigate | If recharts is selected in Task 1, a blocking package-legitimacy human-verify checkpoint precedes install (verify via npmjs.com/package/recharts). Default inline-svg adds no package. |
</threat_model>

<verification>
- `pnpm --filter @grovio/web-admin build` and `lint` pass.
- Human checkpoint confirms login, charts, vendor lifecycle, global-rule protection, payout recording, CMS invalidation, flag toggle, settings, audit log, and preserved category pages.
</verification>

<success_criteria>
Admin panel fully functional for ADM-01..07 + MKT-04 with cookie auth (no token bypass), global rule protected, settlements append-only via decimal-string input, audit log filterable, existing category pages preserved.
</success_criteria>

<output>
Create `.planning/phases/06-vendor-admin-tools/06-10-SUMMARY.md` when done.
</output>
