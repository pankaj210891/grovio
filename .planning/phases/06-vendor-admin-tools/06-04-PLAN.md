---
phase: 06-vendor-admin-tools
plan: 04
type: tdd
wave: 4
depends_on: [06-03]
files_modified:
  - apps/api/src/modules/vendor-auth/VendorAuthService.ts
  - apps/api/src/modules/vendor-auth/VendorAuthService.test.ts
  - apps/api/src/middleware/vendorAuth.ts
  - apps/api/src/modules/admin-auth/AdminAuthService.ts
  - apps/api/src/modules/admin-auth/AdminAuthService.test.ts
  - apps/api/src/modules/admin-auth/index.ts
  - apps/api/src/middleware/adminAuth.ts
autonomous: true
requirements: [AUTH-04, VEN-01, VEN-05, ADM-02]
must_haves:
  truths:
    - "VendorAuthService authenticates against vendor_users (not vendors) and issues JWT { sub: vendorUserId, vendorId, role, email }"
    - "Vendor JWT middleware accepts owner|manager|staff roles and exposes role + vendorId on the request"
    - "Suspended vendors cannot log in"
    - "AdminAuthService logs in admin_users and issues a role=admin JWT verified by requireAdminAuth"
  artifacts:
    - path: "apps/api/src/modules/admin-auth/AdminAuthService.ts"
      provides: "Admin login + JWT (argon2 + jose, 8h TTL)"
      exports: ["AdminAuthService"]
    - path: "apps/api/src/middleware/adminAuth.ts"
      provides: "requireAdminAuth Fastify preHandler"
      exports: ["requireAdminAuth"]
  key_links:
    - from: "apps/api/src/modules/vendor-auth/VendorAuthService.ts"
      to: "vendor_users table"
      via: "login query"
      pattern: "from\\(vendorUsers\\)"
    - from: "apps/api/src/middleware/adminAuth.ts"
      to: "jose jwtVerify"
      via: "role=admin check"
      pattern: "role.*admin"
---

<objective>
Migrate VendorAuthService to authenticate against `vendor_users` with the extended JWT payload (D-03/D-06), update the vendor JWT middleware to accept the three vendor roles and expose `role` on the request, add a suspended-vendor login block (D-17), and create a new AdminAuthService + `requireAdminAuth` middleware (D-21) mirroring the vendor auth pattern.

Purpose: This is the auth foundation all Phase 6 routes depend on. The vendor migration must not break existing Phase 3/4/5 vendor routes (Pitfall 1). TDD because auth has crisp input→output contracts.
Output: Updated vendor auth + new admin auth module + both middlewares, all test-covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-vendor-admin-tools/06-CONTEXT.md
@.planning/phases/06-vendor-admin-tools/06-RESEARCH.md
@.planning/phases/06-vendor-admin-tools/06-PATTERNS.md
@apps/api/src/modules/vendor-auth/VendorAuthService.ts
@apps/api/src/middleware/vendorAuth.ts
@apps/api/src/middleware/customerAuth.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Migrate VendorAuthService to vendor_users + update vendor middleware</name>
  <read_first>
    - apps/api/src/modules/vendor-auth/VendorAuthService.ts (FULL current file — login query, JWT payload, VendorTokenPayload interface, TTL, error classes)
    - apps/api/src/middleware/vendorAuth.ts (current role check `role !== "vendor"` and `request.vendorId` extraction)
    - apps/api/src/db/schema/vendor-users.ts (target table + role enum)
    - apps/api/src/db/schema/vendors.ts (onboardingStatus column for suspended check)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (VendorAuthService migration section: exact before/after queries + JWT payload + suspended check)
    - packages/contracts/src/vendor/profile.ts (VendorRole union — keep payload role type aligned)
  </read_first>
  <behavior>
    - Test: login with valid vendor_users credentials returns an accessToken whose decoded payload has `sub = vendorUsers.id`, `vendorId = vendorUsers.vendorId`, `role` ∈ {owner,manager,staff}, `email`.
    - Test: login with credentials that exist only on the old `vendors` table (not in vendor_users) throws InvalidCredentialsError.
    - Test: login for a vendor whose `vendors.onboardingStatus = 'suspended'` throws InvalidCredentialsError even with correct password (D-17).
    - Test: login for an archived vendor_user (archivedAt not null) throws InvalidCredentialsError.
    - Test: verifyToken accepts a token with role=owner|manager|staff and returns the VendorTokenPayload; rejects a token with role=admin or role=customer.
    - Test (middleware): requireVendorAuth sets `request.vendorId` to the JWT `vendorId` claim (FK to vendors.id) and `request.vendorRole` to the role claim; a token missing vendorId is rejected 401.
  </behavior>
  <action>
    Update `VendorAuthService.ts`:
    - Replace the login query `from(vendors).where(eq(vendors.email, email))` with `from(vendorUsers).where(and(eq(vendorUsers.email, email), isNull(vendorUsers.archivedAt)))`.
    - After finding the vendorUser, query `vendors.onboardingStatus` for `vendorUser.vendorId`; if `'suspended'`, throw InvalidCredentialsError.
    - Change JWT payload to `{ sub: vendorUser.id, role: vendorUser.role, vendorId: vendorUser.vendorId, email: vendorUser.email }` (role is now the union, not the literal "vendor").
    - Update `VendorTokenPayload` interface: `role: "owner" | "manager" | "staff"` (import VendorRole from @grovio/contracts to stay aligned), keep `sub`, `vendorId`, `email`, `iat`, `exp`.
    - Update `verifyToken` to accept role ∈ the three vendor roles (reject admin/customer).

    Update `middleware/vendorAuth.ts`:
    - Change the guard from `payload["role"] !== "vendor"` to: reject only if role is NOT one of owner|manager|staff.
    - Keep `request.vendorId = payload["vendorId"]` (this remains the FK to vendors.id — DO NOT switch to `sub`; per PATTERNS.md migration note this preserves all existing ownership checks).
    - Additionally set `request.vendorRole = payload["role"]`. Add a `vendorRole` augmentation to the Fastify request type (alongside the existing vendorId augmentation — find where vendorId is declared and add vendorRole).
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api test --run -- VendorAuthService</automated>
  </verify>
  <acceptance_criteria>
    - VendorAuthService.ts login query reads `from(vendorUsers)` (grep: `from(vendorUsers)`)
    - VendorAuthService.ts JWT payload sets `sub` to vendorUser.id and `vendorId` to vendorUser.vendorId (distinct values)
    - VendorAuthService throws InvalidCredentialsError when onboardingStatus is 'suspended'
    - middleware/vendorAuth.ts no longer hard-checks `role !== "vendor"`; accepts owner|manager|staff
    - middleware/vendorAuth.ts sets both `request.vendorId` and `request.vendorRole`
    - `pnpm --filter @grovio/api test --run -- VendorAuthService` passes
  </acceptance_criteria>
  <done>Vendor auth reads vendor_users, issues role-bearing JWT, blocks suspended vendors; middleware exposes role; existing vendorId FK semantics preserved.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Create AdminAuthService + requireAdminAuth middleware</name>
  <read_first>
    - apps/api/src/modules/vendor-auth/VendorAuthService.ts (post-Task-1 — copy structure: argon2.verify, SignJWT/jwtVerify, error classes, deps interface)
    - apps/api/src/middleware/vendorAuth.ts (post-Task-1 — copy structure for adminAuth)
    - apps/api/src/db/schema/admin-users.ts (admin_users table)
    - .planning/phases/06-vendor-admin-tools/06-PATTERNS.md (AdminAuthService + adminAuth.ts sections: full analog)
    - packages/contracts/src/admin/auth.ts (AdminProfile shape)
  </read_first>
  <behavior>
    - Test: AdminAuthService.login with valid admin_users credentials returns accessToken whose payload has `sub = adminUsers.id`, `role = "admin"`, `email`, and expiresIn = 28800 (8h).
    - Test: login with unknown email throws InvalidCredentialsError.
    - Test: login with wrong password (argon2.verify false) throws InvalidCredentialsError.
    - Test: verifyToken returns AdminTokenPayload for a role=admin token; throws for a role=owner (vendor) token.
    - Test (middleware): requireAdminAuth with a valid admin Bearer token sets `request.adminId` and `request.adminEmail`; a vendor-role token is rejected 401; a missing Authorization header is rejected 401.
  </behavior>
  <action>
    Create `apps/api/src/modules/admin-auth/AdminAuthService.ts` by copying VendorAuthService structure (argon2 + jose). Differences: query `adminUsers` table; `TTL_SECONDS = 28800` (8h, per RESEARCH.md); JWT payload `{ sub: admin.id, role: "admin" as const, email: admin.email }`; `verifyToken` checks `payload["role"] === "admin"` else throw. Export `AdminAuthService`, `InvalidCredentialsError`, and `AdminTokenPayload` interface (`sub`, `role: "admin"`, `email`, `iat`, `exp`). Create `apps/api/src/modules/admin-auth/index.ts` barrel.

    Create `apps/api/src/middleware/adminAuth.ts`: copy `requireVendorAuth` structure; check `payload["role"] !== "admin"` → 401; set `request.adminId = payload["sub"]` and `request.adminEmail = payload["email"]`. Add Fastify request augmentation for `adminId` and `adminEmail` (mirror how vendorId is declared).

    Reuse `env.JWT_SECRET` (no new env var per RESEARCH.md — AdminAuthService shares the secret).
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api test --run -- AdminAuthService</automated>
  </verify>
  <acceptance_criteria>
    - AdminAuthService.ts queries `from(adminUsers)` and sets JWT role to the literal "admin"
    - AdminAuthService TTL_SECONDS equals 28800
    - AdminAuthService.verifyToken rejects a token whose role is not "admin"
    - middleware/adminAuth.ts exports `requireAdminAuth`, rejects non-admin tokens with 401, sets request.adminId + request.adminEmail
    - `pnpm --filter @grovio/api test --run -- AdminAuthService` passes
  </acceptance_criteria>
  <done>AdminAuthService + requireAdminAuth created and test-covered; admin JWT (8h) verifiable and role-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /vendor/* and /admin/* | JWT Bearer token crosses here; role + identity must come from verified token, never request body. |
| login endpoint → DB credentials | Untrusted email/password validated against argon2 hash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Spoofing | Role claim forgery to escalate to admin | mitigate | Role read only from `jose.jwtVerify`-validated payload; JWT_SECRET server-side. adminAuth rejects any non-admin role. |
| T-06-10 | Elevation of Privilege | Vendor token used on admin routes | mitigate | requireAdminAuth checks `role === "admin"`; vendor tokens (owner/manager/staff) rejected 401. |
| T-06-11 | Denial of Service / Authn bypass | Suspended vendor still logging in | mitigate | VendorAuthService blocks login when onboardingStatus = 'suspended' (D-17). |
| T-06-12 | Repudiation | Stale Phase 3 JWTs (sub=vendors.id) accepted post-migration | accept | D-06 accepts re-login via JWT_SECRET rotation at deploy; documented in 06-03 deploy notes. Middleware reads `vendorId` claim which old tokens lack the new role on → rejected by role check. |
| T-06-SC | Tampering | npm installs | mitigate | No installs (argon2, jose already present). |
</threat_model>

<verification>
- `pnpm --filter @grovio/api test --run -- VendorAuthService AdminAuthService` passes.
- Full suite `pnpm --filter @grovio/api test --run` stays green (existing vendor route tests must still pass — Pitfall 1 regression guard).
</verification>

<success_criteria>
Vendor auth migrated to vendor_users with role JWT, suspended block, middleware role exposure; AdminAuthService + requireAdminAuth created; existing vendor route tests unbroken.
</success_criteria>

<output>
Create `.planning/phases/06-vendor-admin-tools/06-04-SUMMARY.md` when done.
</output>
