---
phase: 06-vendor-admin-tools
plan: 07
type: tdd
wave: 5
depends_on: [06-04, 06-05]
files_modified:
  - apps/api/src/modules/vendor-management/VendorManagementService.ts
  - apps/api/src/modules/vendor-management/VendorManagementService.test.ts
  - apps/api/src/modules/vendor-management/index.ts
  - apps/api/src/modules/commissions/CommissionService.ts
  - apps/api/src/modules/commissions/CommissionService.test.ts
  - apps/api/src/modules/payouts/PayoutService.ts
  - apps/api/src/modules/payouts/PayoutService.test.ts
  - apps/api/src/modules/payouts/index.ts
  - apps/api/src/modules/feature-flags/FeatureFlagService.ts
  - apps/api/src/modules/homepage/HomepageService.ts
autonomous: true
requirements: [ADM-02, ADM-03, ADM-04, ADM-06, MKT-04]
must_haves:
  truths:
    - "Admin can approve/suspend/reinstate vendors (onboarding_status transitions) with audit log writes (ADM-02)"
    - "Admin commission-rule CRUD invalidates the commission rate cache; the global rule cannot be deleted (ADM-03)"
    - "PayoutService records append-only settlements converting decimal string → BIGINT minor units (MKT-04, Pitfall 5)"
    - "FeatureFlagService gains a toggle path that invalidates Redis; HomepageService gains CMS write methods that invalidate the blocks cache (ADM-06, ADM-04)"
  artifacts:
    - path: "apps/api/src/modules/payouts/PayoutService.ts"
      provides: "append-only settlement recording + per-vendor payout query"
      exports: ["PayoutService"]
    - path: "apps/api/src/modules/vendor-management/VendorManagementService.ts"
      provides: "vendor approve/suspend/reinstate/configure"
      exports: ["VendorManagementService"]
  key_links:
    - from: "apps/api/src/modules/commissions/CommissionService.ts"
      to: "redis commission:rate cache"
      via: "invalidateRateCache on rule mutation"
      pattern: "commission:rate"
    - from: "apps/api/src/modules/payouts/PayoutService.ts"
      to: "vendor_payouts table"
      via: "append-only insert with minor-unit conversion"
      pattern: "insert\\(vendorPayouts\\)"
---

<objective>
Build the admin-side backend services: VendorManagementService (approve/suspend/reinstate/configure, ADM-02), commission-rule CRUD on CommissionService with cache invalidation + global-rule protection (ADM-03), PayoutService (append-only settlement + per-vendor payout view, MKT-04), the FeatureFlagService write/toggle path (ADM-06), and HomepageService CMS write methods with cache invalidation (ADM-04). Every sensitive mutation calls AuditService (D-13).

Purpose: Powers the admin control-plane pages. TDD for the money conversion, cache-invalidation, and global-rule-protection logic which are correctness-critical and security-critical.
Output: 2 new admin service modules + 3 existing-service extensions, test-covered.
</objective>

<skill_hint>
Use the `/git-workflow` skill for all branching, committing, and merging throughout this plan. Each plan executes on its own feature branch — never accumulate work across plans or phases on one branch.
</skill_hint>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-vendor-admin-tools/06-CONTEXT.md
@.planning/phases/06-vendor-admin-tools/06-RESEARCH.md
@.planning/phases/06-vendor-admin-tools/06-PATTERNS.md
@apps/api/src/modules/commissions/CommissionService.ts
@apps/api/src/modules/feature-flags/FeatureFlagService.ts
@apps/api/src/modules/homepage/HomepageService.ts
@apps/api/src/modules/audit/AuditService.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: VendorManagementService + PayoutService</name>
  <read_first>
    - apps/api/src/db/schema/vendors.ts (onboardingStatus enum + archivedAt) + vendor-payouts.ts + vendor-category-restrictions.ts + commission-rules.ts
    - apps/api/src/modules/audit/AuditService.ts (log() signature — every mutation calls it)
    - apps/api/src/modules/analytics/AnalyticsService.ts (getAdminVendorPayout — PayoutService.getVendorPayout delegates or reuses this aggregation)
    - packages/contracts/src/admin/vendors.ts + payouts.ts
    - .planning/phases/06-vendor-admin-tools/06-CONTEXT.md (D-07, D-08, D-17; Pitfall 5 minor-unit conversion)
    - .planning/phases/06-vendor-admin-tools/06-RESEARCH.md (Pitfall 5: Math.round(parseFloat(amount)*100))
  </read_first>
  <behavior>
    - VendorManagementService.approveVendor(vendorId, adminEmail, ip) sets onboarding_status='approved' and writes audit_log action 'vendor.approved' (before/after status).
    - suspendVendor → onboarding_status='suspended' + audit 'vendor.suspended'.
    - reinstateVendor → onboarding_status='approved' + audit 'vendor.reinstated'.
    - configureVendor(vendorId, { categoryRestrictionIds, commissionOverridePercent }, adminEmail) replaces category restrictions and upserts a vendor-scope commission rule; writes audit.
    - listVendors({ limit, offset }) returns AdminVendorListItem rows (status badge data + GMV last 30d + product count + restriction count) with offset pagination.
    - PayoutService.recordSettlement(vendorId, { amount, settlementReference, note }, adminEmail) converts `amount` (decimal string like "5000.00") to minor units via Math.round(parseFloat(amount)*100), inserts an append-only vendor_payouts row (settledByAdminEmail=adminEmail, settledAt=now), writes audit 'payout.settled'. Never accepts amountMinor from the client. Rejects amount <= 0.
    - PayoutService.getVendorPayout(vendorId) returns the admin payout view (summary + ledger + settlements + payoutInfo) — delegate to AnalyticsService.getAdminVendorPayout.
  </behavior>
  <action>
    Create `vendor-management/VendorManagementService.ts` (deps `{ db, auditService }`): implement approve/suspend/reinstate/configure (each loads before-state, performs the update, calls auditService.log with the dot-namespaced action and before/after), and listVendors. Barrel index.ts.

    Create `payouts/PayoutService.ts` (deps `{ db, auditService, analyticsService }`): implement recordSettlement (minor-unit conversion via Math.round(parseFloat()*100), reject non-positive, append-only insert into vendorPayouts, audit log) and getVendorPayout (delegate to analyticsService.getAdminVendorPayout). Barrel index.ts.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api test --run -- VendorManagementService PayoutService</automated>
  </verify>
  <acceptance_criteria>
    - approveVendor sets onboarding_status to 'approved' and calls auditService.log with action 'vendor.approved'
    - suspendVendor and reinstateVendor write audit actions 'vendor.suspended' and 'vendor.reinstated'
    - PayoutService.recordSettlement converts "5000.00" to 500000 minor units (test asserts the stored amount_minor)
    - PayoutService never reads an `amountMinor` field from its input (input is the decimal string `amount`)
    - PayoutService.recordSettlement only INSERTs into vendor_payouts (no UPDATE) and writes audit 'payout.settled'
    - recordSettlement rejects amount "0" or negative
    - `pnpm --filter @grovio/api test --run -- VendorManagementService PayoutService` passes
  </acceptance_criteria>
  <done>Vendor approve/suspend/reinstate/configure + append-only settlement recording with server-side minor-unit conversion + audit; test-covered.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: CommissionService rule CRUD + cache invalidation + global-rule protection</name>
  <read_first>
    - apps/api/src/modules/commissions/CommissionService.ts (FULL — resolveRate, the `commission:rate:` cache key, existing tests)
    - apps/api/src/db/schema/commission-rules.ts (scope column, global/category/vendor)
    - apps/api/src/modules/audit/AuditService.ts
    - packages/contracts/src/admin/commission-rules.ts
    - .planning/phases/06-vendor-admin-tools/06-CONTEXT.md (D-18) + 06-RESEARCH.md (Pitfall 4: invalidate commission:rate:* on mutation; anti-pattern: never delete global rule)
  </read_first>
  <behavior>
    - getRules() returns { global, categoryOverrides[], vendorOverrides[] } (D-18).
    - createRule({ scope, categoryId?, vendorId?, ratePercent }, adminEmail) inserts a category or vendor override, invalidates the commission rate cache, writes audit 'commission_rule.created'. Cannot create a second global rule.
    - updateRule(id, { ratePercent }, adminEmail) updates rate, invalidates cache, audit 'commission_rule.updated'.
    - deleteRule(id, adminEmail) deletes a category/vendor override, invalidates cache, audit 'commission_rule.deleted' — BUT throws a 403-mappable error if the rule scope is 'global' (global rule is undeletable, D-18).
    - invalidateRateCache() deletes all `commission:rate:*` keys from Redis (simple v1 approach per Pitfall 4).
  </behavior>
  <action>
    Extend `CommissionService.ts` (it already has db + redis deps): add getRules, createRule, updateRule, deleteRule, and invalidateRateCache. invalidateRateCache scans/deletes `commission:rate:*` (use redis.keys pattern then del, or a known-key approach — Claude's discretion, but it MUST clear stale rates). deleteRule must guard `scope === 'global'` → throw a CommissionRuleProtectedError (or similar coded error). Every mutation calls auditService.log — add auditService to CommissionService deps (update the deps interface; container registration handled in 06-08). Reuse the existing rate cache key format. Add tests to the existing CommissionService.test.ts file.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api test --run -- CommissionService</automated>
  </verify>
  <acceptance_criteria>
    - deleteRule throws a coded error (not a silent success) when the target rule scope is 'global'
    - createRule/updateRule/deleteRule each call invalidateRateCache (which clears commission:rate:* keys)
    - createRule rejects creating a second global-scope rule
    - each mutation calls auditService.log with the matching 'commission_rule.*' action
    - existing CommissionService tests (resolveRate priority chain) still pass
    - `pnpm --filter @grovio/api test --run -- CommissionService` passes
  </acceptance_criteria>
  <done>Commission rule CRUD with cache invalidation, global-rule deletion protection, and audit; existing rate-resolution tests unbroken.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: FeatureFlagService toggle + HomepageService CMS writes (cache-invalidated)</name>
  <read_first>
    - apps/api/src/modules/feature-flags/FeatureFlagService.ts (FULL — getFlag, invalidateFlag; add toggleFlag write path)
    - apps/api/src/modules/homepage/HomepageService.ts (FULL — read endpoint + invalidateBlocks(); add CRUD + reorder)
    - apps/api/src/db/schema/homepage-blocks.ts + feature-flags.ts
    - packages/contracts/src/admin/cms.ts (Create/Update/Reorder inputs) + the existing MerchandisingBlock schema
    - .planning/phases/06-vendor-admin-tools/06-CONTEXT.md (D-11, D-12) + 06-RESEARCH.md (Pitfall 3: invalidate AFTER write)
  </read_first>
  <behavior>
    - FeatureFlagService.toggleFlag(key, enabled) updates feature_flags.isEnabled, then calls invalidateFlag(key). listFlags() returns all rows (key, description, enabled, updatedAt) for the admin toggle list.
    - HomepageService.createBlock(input) inserts a block (payload validated against MerchandisingBlock), assigns the next sort order, then invalidateBlocks() AFTER the write succeeds (Pitfall 3 ordering).
    - HomepageService.updateBlock(id, input) updates, then invalidateBlocks().
    - HomepageService.deleteBlock(id) deletes, then invalidateBlocks().
    - HomepageService.reorderBlock(id, direction) swaps sort order with the adjacent block (up/down), then invalidateBlocks().
    - HomepageService.listBlocksForAdmin() returns all blocks (including inactive) ordered by sort order, for the CMS page.
  </behavior>
  <action>
    Extend `FeatureFlagService.ts`: add toggleFlag(key, enabled) (db.update isEnabled + updatedAt, then invalidateFlag) and listFlags(). Add auditService to deps for action 'feature_flag.toggled' (toggleFlag calls auditService.log).

    Extend `HomepageService.ts`: add createBlock/updateBlock/deleteBlock/reorderBlock/listBlocksForAdmin. Each mutation validates the block payload with the existing MerchandisingBlock schema, performs the DB write, then calls the existing invalidateBlocks() AFTER the write (never before — Pitfall 3). Add auditService to deps; each mutation logs 'homepage_block.created|updated|deleted'. (The CMS routes in 06-08 will call these.)
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/api test --run -- FeatureFlagService HomepageService</automated>
  </verify>
  <acceptance_criteria>
    - FeatureFlagService.toggleFlag calls invalidateFlag(key) after the DB update and logs 'feature_flag.toggled'
    - HomepageService.createBlock/updateBlock/deleteBlock/reorderBlock each call invalidateBlocks() AFTER the DB write (not before)
    - HomepageService block mutations validate the payload via the existing MerchandisingBlock schema
    - reorderBlock with direction 'up'/'down' swaps sort order with the adjacent block
    - existing FeatureFlagService and HomepageService read-path tests still pass
    - `pnpm --filter @grovio/api test --run -- FeatureFlagService HomepageService` passes
  </acceptance_criteria>
  <done>Feature flag toggle + homepage CMS CRUD/reorder with post-write cache invalidation and audit; read-path tests unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin request → marketplace-wide mutations | admin identity from JWT; every mutation must be audited and money inputs validated server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Tampering | Inflated settlement via client minor units | mitigate | PayoutService converts decimal string server-side (Math.round(parseFloat()*100)); never accepts amountMinor; vendor_payouts append-only (D-07, Pitfall 5). |
| T-06-21 | Tampering | Global commission rule deletion breaking pricing | mitigate | deleteRule throws when scope='global' (D-18 anti-pattern). |
| T-06-22 | Tampering | Stale commission rate after rule change | mitigate | Every rule mutation calls invalidateRateCache clearing commission:rate:* (Pitfall 4). |
| T-06-23 | Tampering | Stale homepage content after CMS edit | mitigate | invalidateBlocks() called AFTER each write (Pitfall 3). |
| T-06-24 | Repudiation | Admin action without audit trail | mitigate | Every vendor-management, payout, commission-rule, flag, and CMS mutation calls auditService.log with before/after. |
| T-06-SC | Tampering | npm installs | mitigate | No installs. |
</threat_model>

<verification>
- `pnpm --filter @grovio/api test --run -- VendorManagementService PayoutService CommissionService FeatureFlagService HomepageService` passes.
- Full suite green.
</verification>

<success_criteria>
Admin services implemented: vendor lifecycle + settlement (append-only, server-side minor units) + commission CRUD (cache-invalidated, global protected) + flag toggle + CMS writes (post-write invalidation); all mutations audited; test-covered.
</success_criteria>

<output>
Create `.planning/phases/06-vendor-admin-tools/06-07-SUMMARY.md` when done.
</output>
