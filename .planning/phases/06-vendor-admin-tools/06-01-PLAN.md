---
phase: 06-vendor-admin-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/vendor/index.ts
  - packages/contracts/src/vendor/profile.ts
  - packages/contracts/src/vendor/staff.ts
  - packages/contracts/src/vendor/earnings.ts
  - packages/contracts/src/admin/index.ts
  - packages/contracts/src/admin/auth.ts
  - packages/contracts/src/admin/vendors.ts
  - packages/contracts/src/admin/commission-rules.ts
  - packages/contracts/src/admin/payouts.ts
  - packages/contracts/src/admin/settings.ts
  - packages/contracts/src/admin/audit.ts
  - packages/contracts/src/admin/analytics.ts
  - packages/contracts/src/admin/cms.ts
  - packages/contracts/src/index.ts
autonomous: true
requirements: [MKT-04, MKT-05, VEN-01, VEN-02, VEN-03, VEN-04, VEN-05, VEN-06, ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06, ADM-07]
must_haves:
  truths:
    - "Backend and both web panels import Phase 6 vendor/admin types from @grovio/contracts with no local type duplication"
    - "Vendor role union (owner|manager|staff) is a shared contract type consumed by VendorAuthService and the vendor panel"
    - "Admin payout, settlement, and outstanding-balance shapes are defined once in contracts (D-08)"
  artifacts:
    - path: "packages/contracts/src/vendor/index.ts"
      provides: "Vendor domain contract barrel"
      contains: "export"
    - path: "packages/contracts/src/admin/index.ts"
      provides: "Admin domain contract barrel"
      contains: "export"
    - path: "packages/contracts/src/index.ts"
      provides: "Root barrel re-exporting vendor + admin domains"
      contains: "./vendor/index.js"
  key_links:
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/vendor/index.ts"
      via: "barrel re-export"
      pattern: "vendor/index"
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/admin/index.ts"
      via: "barrel re-export"
      pattern: "admin/index"
---

<objective>
Define all Phase 6 shared API contract types and Zod schemas in `packages/contracts` so the backend and both web panels consume a single source of truth for vendor profile, vendor staff/roles, vendor earnings, admin auth, admin vendor management, commission rules, payouts/settlement, marketplace settings, audit log, admin analytics, and CMS block management.

Purpose: Prevent type duplication across api, web-vendor, and web-admin. Establishes the `VendorRole` union (D-05/D-06) and money-shaped DTOs (BIGINT minor units) consumed downstream.
Output: New `vendor/` and `admin/` contract subfolders + root barrel updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-vendor-admin-tools/06-CONTEXT.md
@.planning/phases/06-vendor-admin-tools/06-RESEARCH.md
@.planning/phases/06-vendor-admin-tools/06-PATTERNS.md
@packages/contracts/src/index.ts
@packages/contracts/src/commissions/index.ts
@packages/contracts/src/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vendor domain contracts (profile, staff/roles, earnings)</name>
  <read_first>
    - packages/contracts/src/index.ts (root barrel — current export order)
    - packages/contracts/src/commissions/index.ts (existing money-shaped DTO + Zod pattern to replicate)
    - packages/contracts/src/auth.ts (existing JWT payload shape; VendorRole extends this domain)
    - packages/contracts/src/category/index.ts (subfolder barrel pattern: one index.ts re-exporting sibling files)
    - .planning/phases/06-vendor-admin-tools/06-CONTEXT.md (D-01 profile fields, D-05 roles, D-09 earnings view)
  </read_first>
  <action>
    Create `packages/contracts/src/vendor/` subfolder with these files, each exporting both TypeScript types AND matching Zod schemas (follow the `commissions/index.ts` dual export convention — Zod schema named `XSchema`, inferred type named `X`):

    - `profile.ts` (VEN-01, D-01): export `VendorRoleSchema = z.enum(["owner","manager","staff"])` and `VendorRole` type. Export `VendorOnboardingStatusSchema = z.enum(["pending","approved","suspended"])` and `VendorOnboardingStatus`. Export `VendorStoreProfile` shape with fields: `storeName`, `storeDescription` (nullable), `logoUrl` (nullable), `bannerUrl` (nullable), `contactEmail` (nullable), `contactPhone` (nullable), `address` (nullable), `onboardingStatus: VendorOnboardingStatus`. Export `UpdateVendorStoreProfileInput` (all profile fields optional except none required, omits onboardingStatus — only admin changes status). Export `VendorPayoutInfo` shape: `accountHolderName`, `bankAccountNumber`, `ifscOrRoutingCode`, `bankName` and `UpdateVendorPayoutInfoInput` (same fields). Export `VendorReturnPolicy` shape: `returnWindowDays: number`, `returnsEnabled: boolean` and `UpdateVendorReturnPolicyInput`.
    - `staff.ts` (VEN-05, D-03/D-04/D-05): export `VendorStaffMember` shape: `id`, `email`, `role: VendorRole`, `acceptedAt` (nullable ISO string), `invitedBy` (nullable), `createdAt`. Export `InviteVendorStaffInput`: `email: z.string().email()`, `role: VendorRoleSchema` (only "manager"|"staff" allowed — refine to reject "owner"). Export `AcceptVendorInviteInput`: `token: z.string()`, `password: z.string().min(8)`. Export `VendorStaffInvite` shape: `id`, `email`, `role`, `expiresAt`, `acceptedAt` (nullable).
    - `earnings.ts` (MKT-05, D-09): export `VendorEarningsSummary` shape with BIGINT-minor-unit number fields: `totalEarnedMinor`, `totalReversedMinor`, `netCommissionMinor`, `totalSettledMinor`, `outstandingBalanceMinor`. Export `VendorCommissionLedgerEntry` shape: `id`, `vendorOrderId`, `orderDisplayId`, `status: "earned"|"reversed"`, `ratePercent: number`, `subtotalMinor`, `commissionAmountMinor`, `createdAt`. Export `VendorSettlementRecord` shape: `id`, `amountMinor`, `settlementReference`, `note` (nullable), `settledAt`. Export `VendorEarningsResponse` combining summary + ledger entries + settlements arrays.
    - `dashboard.ts` (VEN-02, D-10 adapted): export `VendorDashboardSummary` shape: `ordersCount`, `grossSalesMinor`, `netEarningsMinor`, `outstandingBalanceMinor`, `lowStockCount`, plus `lowStockProducts: Array<{ productId; name; quantityAvailable }>`. Export `VendorDashboardPeriod = z.enum(["7d","30d","90d"])`.
    - `index.ts` (barrel): `export * from` each of the above with `.js` extension imports.

    All money fields are integer minor units (number mode), named `*Minor` per Pitfall 1. Cite D-01, D-05, D-09 in file header comments.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/contracts typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/contracts/src/vendor/profile.ts exports `VendorRoleSchema` (z.enum with exactly owner, manager, staff) and `VendorOnboardingStatusSchema` (z.enum with pending, approved, suspended)
    - packages/contracts/src/vendor/staff.ts `InviteVendorStaffInput` rejects role "owner" (zod refine or enum restricted to manager|staff)
    - packages/contracts/src/vendor/earnings.ts `VendorEarningsSummary` contains a field named `outstandingBalanceMinor`
    - All money/amount fields in vendor/*.ts end with the suffix `Minor`
    - `pnpm --filter @grovio/contracts typecheck` exits 0
  </acceptance_criteria>
  <done>Vendor contract subfolder compiles; VendorRole, earnings summary with outstandingBalanceMinor, and staff invite input all exported.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin domain contracts and wire root barrel</name>
  <read_first>
    - packages/contracts/src/index.ts (root barrel — append new domain exports at end, mirror existing ordering)
    - packages/contracts/src/commissions/index.ts (commission rule shape — admin CRUD reuses/extends)
    - packages/contracts/src/auth.ts (admin JWT role shape)
    - .planning/phases/06-vendor-admin-tools/06-CONTEXT.md (D-07, D-08, D-10, D-11, D-12, D-13, D-17, D-18, D-19, D-21)
  </read_first>
  <action>
    Create `packages/contracts/src/admin/` subfolder, each file exporting types + Zod schemas:

    - `auth.ts` (D-21): export `AdminLoginInput`: `email: z.string().email()`, `password: z.string().min(1)`. Export `AdminProfile`: `id`, `email`, `role: z.literal("admin")`.
    - `vendors.ts` (ADM-02, D-17): export `AdminVendorListItem`: `id`, `name`, `ownerEmail`, `storeName` (nullable), `onboardingStatus: VendorOnboardingStatus` (import from ../vendor), `joinedAt`, `gmvLast30dMinor`, `productCount`, `categoryRestrictionCount`. Export `AdminVendorListResponse` with `items` + offset pagination (`total`, `limit`, `offset`). Export `SuspendVendorInput`/`ApproveVendorInput`/`ReinstateVendorInput` (each just an action; vendorId is a path param). Export `ConfigureVendorInput`: `categoryRestrictionIds: string[]`, `commissionOverridePercent: number | null`.
    - `commission-rules.ts` (ADM-03, D-18): export `CommissionRuleScope = z.enum(["global","category","vendor"])`. Export `CommissionRule`: `id`, `scope`, `categoryId` (nullable), `vendorId` (nullable), `ratePercent: number`. Export `CreateCommissionRuleInput` and `UpdateCommissionRuleInput` (ratePercent + scope target). Export `CommissionRulesResponse`: `global: CommissionRule`, `categoryOverrides: CommissionRule[]`, `vendorOverrides: CommissionRule[]`.
    - `payouts.ts` (MKT-04, D-07/D-08): export `RecordSettlementInput`: `amount: z.string()` (decimal display string — converted to minor units server-side per Pitfall 5), `settlementReference: z.string().min(1)`, `note: z.string().optional()`. Export `AdminPayoutSummary` (same shape as VendorEarningsSummary minor-unit fields). Export `AdminVendorPayoutResponse`: summary + commission ledger entries + settlements + read-only `payoutInfo: VendorPayoutInfo | null` (import from ../vendor).
    - `settings.ts` (ADM-05, D-19): export `MarketplaceSettingKey` as a z.enum of: `store_name`, `default_currency`, `timezone`, `default_return_window_days`, `low_stock_threshold`, `primary_color`, `logo_url`, `favicon_url`, `smtp_sender_name`, `smtp_sender_email`. Export `UpdateSettingInput`: `key: MarketplaceSettingKey`, `value: z.unknown()`. Export `MarketplaceSettingsResponse` as a record of key→value.
    - `audit.ts` (ADM-07, D-13): export `AuditActorType = z.enum(["admin","vendor","system"])`. Export `AuditLogEntry`: `id`, `actorType`, `actorId`, `actorEmail`, `action`, `entityType`, `entityId`, `before` (unknown nullable), `after` (unknown nullable), `ipAddress` (nullable), `createdAt`. Export `AuditLogQuery`: optional `actorType`, `action`, `entityType`, `from`, `to`, `limit`, `offset`. Export `AuditLogResponse`: items + pagination.
    - `analytics.ts` (ADM-01, D-10): export `AnalyticsPeriod = z.enum(["7d","30d","90d"])`. Export `AdminAnalyticsSummary`: `gmvMinor`, `ordersCount`, `activeVendors`, `newCustomers`, `commissionEarnedMinor`, `pendingPayoutsMinor`. Export `OrdersByDayPoint`: `date`, `ordersCount`, `gmvMinor`. Export `TopVendorByGmv`: `vendorId`, `name`, `gmvMinor`. Export `GmvByCategoryPoint`: `categoryId`, `name`, `gmvMinor`. Export `AdminAnalyticsResponse` combining summary + charts arrays.
    - `cms.ts` (ADM-04, D-11): export `CreateHomepageBlockInput`, `UpdateHomepageBlockInput`, `ReorderHomepageBlockInput` (`blockId`, `direction: z.enum(["up","down"])`). Reuse the existing `MerchandisingBlock` type from the category/marketplace-config domain for block payload validation — import it; do NOT redefine block-type shapes.
    - `index.ts` (barrel): `export *` from each above.

    Then update `packages/contracts/src/index.ts` root barrel: append `export * from "./vendor/index.js";` and `export * from "./admin/index.js";` after the existing commissions export. Cite the D-numbers in each file header.
  </action>
  <verify>
    <automated>cd "D:/My Projects/grovio" && pnpm --filter @grovio/contracts typecheck && pnpm --filter @grovio/contracts build</automated>
  </verify>
  <acceptance_criteria>
    - packages/contracts/src/admin/payouts.ts `RecordSettlementInput.amount` is typed as a string (not number) per Pitfall 5
    - packages/contracts/src/admin/commission-rules.ts `CommissionRulesResponse` has exactly three groups: global (single), categoryOverrides (array), vendorOverrides (array)
    - packages/contracts/src/admin/cms.ts imports the existing MerchandisingBlock type rather than redefining banner/product_grid/text_block/featured_categories shapes
    - packages/contracts/src/index.ts contains the string `./vendor/index.js` and `./admin/index.js`
    - `pnpm --filter @grovio/contracts build` exits 0 and emits dist output
  </acceptance_criteria>
  <done>Admin contract subfolder + root barrel wired; package builds; vendor and admin domains importable as `@grovio/contracts`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed at this layer) | Contracts package is compile-time only — no runtime trust boundary. Schemas defined here are *consumed* by the API to enforce boundaries in later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | RecordSettlementInput accepting client minor units | mitigate | `amount` is a string display value; minor-unit conversion happens server-side (06-07). Contract intentionally has NO `amountMinor` input field (Pitfall 5). |
| T-06-02 | Elevation of Privilege | InviteVendorStaffInput allowing role=owner | mitigate | `InviteVendorStaffInput.role` zod-restricted to manager|staff; cannot mint an owner via invite (D-05). |
| T-06-SC | Tampering | npm installs | mitigate | No new packages installed in this plan (zod already present). RESEARCH.md Package Legitimacy Audit confirms zero net-new packages for Phase 6. |
</threat_model>

<verification>
- `pnpm --filter @grovio/contracts build` succeeds.
- `import { VendorRole, AdminAnalyticsSummary, RecordSettlementInput } from "@grovio/contracts"` resolves (verify via a throwaway typecheck or grep that the symbols are in dist .d.ts).
</verification>

<success_criteria>
All Phase 6 vendor and admin DTOs/Zod schemas exist in `@grovio/contracts`, money fields are minor-unit numbers (settlement input is a string), and the root barrel re-exports both new domains.
</success_criteria>

<output>
Create `.planning/phases/06-vendor-admin-tools/06-01-SUMMARY.md` when done.
</output>
