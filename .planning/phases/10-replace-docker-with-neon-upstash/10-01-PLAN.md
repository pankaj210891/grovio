---
phase: 10-replace-docker-with-neon-upstash
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/plugins/drizzle.ts
  - apps/api/src/plugins/drizzle.test.ts
  - apps/api/src/plugins/redis.ts
  - apps/api/src/plugins/redis.test.ts
  - apps/api/src/config/env.ts
  - apps/api/drizzle.config.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Backend connects to Neon over TLS automatically when DATABASE_URL targets Neon, and to local Postgres without TLS"
    - "Backend connects to Upstash over TLS automatically when REDIS_URL uses the rediss:// scheme"
    - "drizzle-kit migrations run against a direct (non-pooled) Neon URL when DATABASE_DIRECT_URL is set"
    - "env validation accepts DATABASE_DIRECT_URL and OPENSEARCH_URL as optional URLs"
  artifacts:
    - path: "apps/api/src/plugins/drizzle.ts"
      provides: "requiresSsl() named export + conditional ssl on pg.Pool (D-07)"
      contains: "export function requiresSsl"
    - path: "apps/api/src/plugins/drizzle.test.ts"
      provides: "Unit tests for requiresSsl()"
      contains: "requiresSsl"
    - path: "apps/api/src/plugins/redis.ts"
      provides: "detectRedisTls() named export + conditional tls on ioredis (D-07)"
      contains: "export function detectRedisTls"
    - path: "apps/api/src/plugins/redis.test.ts"
      provides: "Unit tests for detectRedisTls()"
      contains: "detectRedisTls"
    - path: "apps/api/src/config/env.ts"
      provides: "DATABASE_DIRECT_URL + OPENSEARCH_URL optional env fields (D-04, D-06)"
      contains: "DATABASE_DIRECT_URL"
    - path: "apps/api/drizzle.config.ts"
      provides: "Migration URL falls back DATABASE_DIRECT_URL -> DATABASE_URL"
      contains: "DATABASE_DIRECT_URL"
  key_links:
    - from: "apps/api/src/plugins/drizzle.ts"
      to: "env.DATABASE_URL"
      via: "requiresSsl(env.DATABASE_URL) gating ssl option on pg.Pool"
      pattern: "requiresSsl\\(env.DATABASE_URL\\)"
    - from: "apps/api/src/plugins/redis.ts"
      to: "env.REDIS_URL"
      via: "detectRedisTls(env.REDIS_URL) gating tls option on ioredis"
      pattern: "detectRedisTls\\(env.REDIS_URL\\)"
---

<objective>
Make the backend TLS-aware so the same code path works against local Postgres/Redis (no TLS) and cloud Neon/Upstash (TLS required) by changing only the connection URL. Extract the detection logic into pure, exported helpers, unit-test them first (Wave 0 requirement), then wire them into the Fastify plugins. Add the optional `DATABASE_DIRECT_URL` and `OPENSEARCH_URL` env fields, and point drizzle-kit migrations at the direct (non-pooled) URL.

Purpose: D-07 requires automatic SSL/TLS based on URL scheme; Neon's pooled URL breaks drizzle-kit migrations (must use a direct URL).
Output: Modified plugins with tested detection helpers, extended env schema, updated drizzle.config.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-replace-docker-with-neon-upstash/10-CONTEXT.md
@.planning/phases/10-replace-docker-with-neon-upstash/10-RESEARCH.md
@.planning/phases/10-replace-docker-with-neon-upstash/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract requiresSsl() and detectRedisTls() helpers and write their unit tests (Wave 0)</name>
  <files>apps/api/src/plugins/drizzle.ts, apps/api/src/plugins/drizzle.test.ts, apps/api/src/plugins/redis.ts, apps/api/src/plugins/redis.test.ts</files>
  <read_first>
    - apps/api/src/plugins/drizzle.ts (current Pool setup with no SSL — full file)
    - apps/api/src/plugins/redis.ts (current ioredis setup with no TLS — full file)
    - apps/api/src/modules/feature-flags/FeatureFlagService.test.ts (Vitest import + describe/it/expect pattern to copy)
    - .planning/phases/10-replace-docker-with-neon-upstash/10-PATTERNS.md (target-state patterns for both helpers and exact test cases)
  </read_first>
  <behavior>
    - requiresSsl: "postgresql://user:pass@ep-xxx-pooler.neon.tech/grovio?sslmode=require" -> true
    - requiresSsl: "postgresql://user:pass@host/db?sslmode=require" -> true (sslmode only, no neon.tech host)
    - requiresSsl: "postgresql://grovio:grovio@localhost:5432/grovio" -> false
    - detectRedisTls: "rediss://:token@xxx.upstash.io:6380" -> true
    - detectRedisTls: "redis://localhost:6379" -> false
    - detectRedisTls: "redis://:password@host:6379" -> false
  </behavior>
  <action>
    In drizzle.ts add a named export `export function requiresSsl(connectionString: string): boolean` at module scope (above the `fp(...)` plugin closure) that returns true when the string includes `.neon.tech` OR includes `sslmode=require`. In redis.ts add a named export `export function detectRedisTls(url: string): boolean` at module scope that returns true when the url `startsWith("rediss://")`. Both must be pure (no Fastify dependency) so they can be imported into test files without booting Fastify (per D-07 and the RESEARCH.md Wave 0 Gaps requirement). Create drizzle.test.ts importing `{ requiresSsl }` from "./drizzle.js" and redis.test.ts importing `{ detectRedisTls }` from "./redis.js", each covering the cases in the behavior block, using the Vitest `describe`/`it`/`expect` import style from FeatureFlagService.test.ts. Do NOT yet change the plugin bodies that call these helpers — that wiring is Task 2. This task only adds the exported helpers plus their tests so the tests exist before the integration changes.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/plugins/drizzle.ts` contains the exact string `export function requiresSsl(`
    - `apps/api/src/plugins/redis.ts` contains the exact string `export function detectRedisTls(`
    - `apps/api/src/plugins/drizzle.test.ts` imports `requiresSsl` and asserts true for a `.neon.tech` URL, true for a `sslmode=require`-only URL, and false for a `localhost` URL
    - `apps/api/src/plugins/redis.test.ts` imports `detectRedisTls` and asserts true for `rediss://` and false for `redis://`
    - `pnpm --filter @grovio/api test` exits 0
  </acceptance_criteria>
  <done>Both pure helpers are exported and covered by passing unit tests; plugin bodies not yet rewired.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ssl/tls detection into the plugins; add DATABASE_DIRECT_URL + OPENSEARCH_URL env fields; switch drizzle.config.ts to the direct URL</name>
  <files>apps/api/src/plugins/drizzle.ts, apps/api/src/plugins/redis.ts, apps/api/src/config/env.ts, apps/api/drizzle.config.ts</files>
  <read_first>
    - apps/api/src/plugins/drizzle.ts (now has requiresSsl from Task 1 — full file)
    - apps/api/src/plugins/redis.ts (now has detectRedisTls from Task 1 — full file)
    - apps/api/src/config/env.ts (current envSchema fields and JSDoc comment style — full file)
    - apps/api/drizzle.config.ts (current dbCredentials.url line — full file)
    - .planning/phases/10-replace-docker-with-neon-upstash/10-PATTERNS.md (target-state for plugin bodies, env fields, drizzle.config.ts, and JSDoc comment updates)
  </read_first>
  <action>
    In drizzle.ts: inside the plugin closure compute `const ssl = requiresSsl(env.DATABASE_URL)` and construct `new Pool({ connectionString: env.DATABASE_URL, ...(ssl ? { ssl: true } : {}) })`; change the startup log to include `(ssl=${ssl})`. In redis.ts: compute `const isTls = detectRedisTls(env.REDIS_URL)` and spread `...(isTls ? { tls: {} } : {})` into the existing ioredis options object (keep `lazyConnect` and `enableReadyCheck`); change the startup log to include `(tls=${isTls})`. The explicit `tls: {}` is required defense against the documented ioredis `parseRedisUrl` TLS-stripping bug — do not rely on URL parsing alone (per D-07 / RESEARCH.md Pitfall 3). In env.ts: add `DATABASE_DIRECT_URL: z.string().url().optional()` after DATABASE_URL and `OPENSEARCH_URL: z.string().url().optional()` after REDIS_URL (both optional — backend must still boot without them for local/pre-Phase-3 use, per D-04/D-06); rewrite the DATABASE_URL and REDIS_URL JSDoc comments to remove the `docker-compose up` references and describe the Neon pooled / Upstash rediss formats and the auto-detection behavior. In drizzle.config.ts: change `url: process.env["DATABASE_URL"]!` to `url: process.env["DATABASE_DIRECT_URL"] ?? process.env["DATABASE_URL"]!` and update the JSDoc to explain the pooled-vs-direct distinction (Neon PgBouncer transaction mode does not support migration session state — per RESEARCH.md Pitfall 1). Do NOT add `ssl: true` unconditionally anywhere — it must stay gated by the detection helpers.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api typecheck && pnpm --filter @grovio/api test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/plugins/drizzle.ts` contains `requiresSsl(env.DATABASE_URL)` and constructs the Pool with a conditional `ssl: true` (not an unconditional `ssl: true`)
    - `apps/api/src/plugins/redis.ts` contains `detectRedisTls(env.REDIS_URL)` and spreads a conditional `tls: {}` into the ioredis options
    - `apps/api/src/config/env.ts` contains `DATABASE_DIRECT_URL: z.string().url().optional()` and `OPENSEARCH_URL: z.string().url().optional()`
    - `apps/api/src/config/env.ts` JSDoc for DATABASE_URL and REDIS_URL contains no `docker-compose` substring
    - `apps/api/drizzle.config.ts` contains `process.env["DATABASE_DIRECT_URL"] ?? process.env["DATABASE_URL"]`
    - `pnpm --filter @grovio/api typecheck` exits 0 and `pnpm --filter @grovio/api test` exits 0
  </acceptance_criteria>
  <done>Both plugins gate TLS on the tested helpers, env schema validates the two new optional URLs, and drizzle-kit reads the direct URL with a safe fallback.</done>
</task>

</tasks>

<threat_model>
- id: T-10-01
  stride: [Tampering, Spoofing]
  threat: TLS not enforced on DB/Redis connections -> man-in-the-middle on credentials and data
  mitigation: requiresSsl()/detectRedisTls() auto-enable TLS from URL scheme; cloud URLs (.neon.tech / sslmode=require / rediss://) always trigger TLS; verified by Task 1 unit tests
  severity: high
- id: T-10-02
  stride: [Information Disclosure]
  threat: Credentials embedded in connection URLs leak via commits
  mitigation: env.ts only reads from process.env; no URLs hardcoded; .env stays gitignored (env.example placeholder handling is in plan 02)
  severity: medium
</threat_model>

<verification>
- `pnpm --filter @grovio/api test` is green (helper unit tests pass)
- `pnpm --filter @grovio/api typecheck` is green (env schema + config changes compile)
- No unconditional `ssl: true` or unconditional `tls: {}` in the plugin files
</verification>

<success_criteria>
- requiresSsl() and detectRedisTls() are exported, pure, and unit-tested
- Plugins apply TLS only when the URL indicates a cloud/TLS endpoint
- DATABASE_DIRECT_URL and OPENSEARCH_URL parse as optional URLs without breaking local boot
- drizzle-kit uses the direct URL when present, falling back to DATABASE_URL
</success_criteria>

<output>
Create `.planning/phases/10-replace-docker-with-neon-upstash/10-01-SUMMARY.md` when done
</output>
