---
phase: 10-replace-docker-with-neon-upstash
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - apps/api/.env.example
  - .github/workflows/ci.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Both .env.example files document the Neon pooled DATABASE_URL, the Neon direct DATABASE_DIRECT_URL, the Upstash rediss:// REDIS_URL, and the Bonsai https OPENSEARCH_URL with placeholder-only values"
    - "No .env.example comment references docker-compose"
    - "CI injects DATABASE_URL, DATABASE_DIRECT_URL, REDIS_URL, OPENSEARCH_URL from GitHub Actions repository secrets"
  artifacts:
    - path: ".env.example"
      provides: "Root infra env vars in Neon/Upstash/Bonsai format (D-04, D-05, D-06, D-11, D-12)"
      contains: "ep-cool-name-123456-pooler.neon.tech"
    - path: "apps/api/.env.example"
      provides: "API env vars in Neon/Upstash format + DATABASE_DIRECT_URL + OPENSEARCH_URL"
      contains: "rediss://"
    - path: ".github/workflows/ci.yml"
      provides: "Cloud credentials injected from repo secrets for all CI environments (D-08, D-09)"
      contains: "DATABASE_URL: ${{ secrets.DATABASE_URL }}"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub Actions repository secrets"
      via: "top-level env block reading ${{ secrets.* }}"
      pattern: "secrets.DATABASE_URL"
---

<objective>
Update both `.env.example` files to the cloud connection-string formats (Neon pooled + direct, Upstash rediss://, Bonsai https), replace every docker-compose comment reference, add the new `DATABASE_DIRECT_URL` and `OPENSEARCH_URL` variables, and inject the four cloud credentials into CI from GitHub Actions repository secrets.

Purpose: D-04/D-05/D-06 define the new URL formats; D-09 routes CI credentials through repo secrets; D-11 requires a full sweep of docker-compose references in env examples.
Output: Updated `.env.example`, `apps/api/.env.example`, and `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-replace-docker-with-neon-upstash/10-CONTEXT.md
@.planning/phases/10-replace-docker-with-neon-upstash/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite both .env.example files to Neon/Upstash/Bonsai formats and sweep docker-compose references</name>
  <files>.env.example, apps/api/.env.example</files>
  <read_first>
    - .env.example (current Database/Redis/OpenSearch sections with docker-compose comments — full file)
    - apps/api/.env.example (current DATABASE_URL/REDIS_URL with docker-compose comments — full file)
    - .planning/phases/10-replace-docker-with-neon-upstash/10-PATTERNS.md (exact target blocks for each section + the comment-style analog for each file)
  </read_first>
  <action>
    In the root `.env.example`: set `DATABASE_URL` to the Neon pooled placeholder `postgresql://user:pass@ep-cool-name-123456-pooler.neon.tech/grovio?sslmode=require`; add a new `DATABASE_DIRECT_URL` line immediately after it with the Neon direct (no `-pooler`) placeholder `postgresql://user:pass@ep-cool-name-123456.neon.tech/grovio?sslmode=require` and a comment noting it is only needed for `pnpm db:migrate` / `pnpm db:generate`; set `REDIS_URL` to `rediss://:your-upstash-token@your-db.upstash.io:6380` with a comment noting TLS auto-detection and the Upstash Fixed-plan recommendation for BullMQ (per RESEARCH.md Pitfall 2); set `OPENSEARCH_URL` to `https://your-user:your-pass@your-cluster.bonsai.io` with a comment noting it is required from Phase 3 and that Bonsai Sandbox needs `number_of_replicas:0` (per RESEARCH.md Pitfall 5). Replace every comment that mentions `docker-compose` (Database, Redis, OpenSearch sections) with Neon/Upstash/Bonsai "Obtain:" guidance, keeping the existing `# ====` banner section style. In `apps/api/.env.example`: apply the same DATABASE_URL/REDIS_URL format changes, add a `DATABASE_DIRECT_URL` block after DATABASE_URL, add an `OPENSEARCH_URL` block (optional, marked Phase 3) after `FEATURE_FLAG_TTL_SECONDS`, and replace the `docker-compose up -d postgres/redis` comment lines — match the lighter plain-`#` comment style already used in that file rather than forcing the root banner style. All values must remain placeholders only (no real credentials, per threat T-10-02).
  </action>
  <verify>
    <automated>grep -rn "docker-compose" .env.example apps/api/.env.example | grep -v '^#!' ; test $(grep -c "docker-compose" .env.example apps/api/.env.example) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains the substring `ep-cool-name-123456-pooler.neon.tech` and a `DATABASE_DIRECT_URL=` line and `rediss://` and `bonsai.io`
    - `apps/api/.env.example` contains `rediss://`, a `DATABASE_DIRECT_URL=` line, and an `OPENSEARCH_URL=` line
    - `grep -c "docker-compose" .env.example apps/api/.env.example` reports 0 matches in each file
    - Neither file contains a real token, password, or non-placeholder credential
  </acceptance_criteria>
  <done>Both env-example files use cloud URL formats with the two new variables and zero docker-compose references.</done>
</task>

<task type="auto">
  <name>Task 2: Inject cloud credentials into CI from repository secrets</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current top-level env block with only CI: "true"; confirmed no services: block — full file)
    - .planning/phases/10-replace-docker-with-neon-upstash/10-PATTERNS.md (target env block; note the no-services-block finding)
  </read_first>
  <action>
    Extend the existing top-level `env:` block (which currently holds only `CI: "true"`) to add four lines reading from repository secrets: `DATABASE_URL: ${{ secrets.DATABASE_URL }}`, `DATABASE_DIRECT_URL: ${{ secrets.DATABASE_DIRECT_URL }}`, `REDIS_URL: ${{ secrets.REDIS_URL }}`, `OPENSEARCH_URL: ${{ secrets.OPENSEARCH_URL }}`. This is purely additive — there is no `services:` block to remove (confirmed by inspection, RESEARCH.md assumption A3). Do not echo or print any of these variables in a CI step (GitHub masks secrets automatically; explicit echoing would defeat masking — per threat T-10-03). Leave all existing steps (lint, typecheck, format:check, test, build) unchanged.
  </action>
  <verify>
    <automated>grep -c "secrets.DATABASE_URL" .github/workflows/ci.yml; grep -c "secrets.OPENSEARCH_URL" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` `env:` block contains `DATABASE_URL: ${{ secrets.DATABASE_URL }}`
    - `.github/workflows/ci.yml` contains `DATABASE_DIRECT_URL: ${{ secrets.DATABASE_DIRECT_URL }}`, `REDIS_URL: ${{ secrets.REDIS_URL }}`, and `OPENSEARCH_URL: ${{ secrets.OPENSEARCH_URL }}`
    - No CI step echoes any of the four secret env vars
    - The five existing run steps (Lint, Typecheck, Format check, Test, Build) are unchanged
  </acceptance_criteria>
  <done>CI reads all four cloud credentials from repository secrets without echoing them.</done>
</task>

</tasks>

<threat_model>
- id: T-10-02
  stride: [Information Disclosure]
  threat: Credentials in DATABASE_URL/REDIS_URL/OPENSEARCH_URL committed via .env.example
  mitigation: .env.example values are placeholders only (user/pass/your-upstash-token/your-cluster); real .env stays gitignored
  severity: medium
- id: T-10-03
  stride: [Information Disclosure]
  threat: Secrets exposed in CI logs
  mitigation: credentials injected via ${{ secrets.* }} which GitHub masks; no CI step echoes DATABASE_URL or any secret
  severity: high
- id: T-10-05
  stride: [Denial of Service]
  threat: BullMQ continuous polling on Upstash pay-per-request plan causes runaway cost
  mitigation: REDIS_URL comment documents the Upstash Fixed-plan recommendation for BullMQ workloads
  severity: low
</threat_model>

<verification>
- `grep -c "docker-compose" .env.example apps/api/.env.example` reports 0 in each file
- CI env block contains all four secret-backed variables
- All example values are placeholders
</verification>

<success_criteria>
- Both env examples reflect Neon pooled + direct, Upstash rediss://, and Bonsai https formats
- DATABASE_DIRECT_URL and OPENSEARCH_URL documented in both files
- CI injects the four cloud credentials from repository secrets without logging them
- Zero docker-compose references remain in env examples
</success_criteria>

<output>
Create `.planning/phases/10-replace-docker-with-neon-upstash/10-02-SUMMARY.md` when done
</output>
