---
phase: 10-replace-docker-with-neon-upstash
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - docker-compose.yml
  - README.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "docker-compose.yml no longer exists"
    - "The project README has a self-contained Infrastructure Setup section covering Neon, Upstash, and Bonsai signup and the exact env var each produces"
    - "No buyer-facing markdown doc references docker-compose"
    - "ROADMAP.md Phase 1 plan 01-01 description no longer mentions docker-compose infra"
  artifacts:
    - path: "README.md"
      provides: "Infrastructure Setup section (D-02): Neon -> DATABASE_URL/DATABASE_DIRECT_URL, Upstash -> REDIS_URL, Bonsai -> OPENSEARCH_URL"
      contains: "Infrastructure Setup"
      min_lines: 25
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 plan description updated to cloud infra"
      contains: "cloud infra setup"
  key_links:
    - from: "README.md"
      to: "both .env.example files"
      via: "documents the same DATABASE_URL/DATABASE_DIRECT_URL/REDIS_URL/OPENSEARCH_URL variables"
      pattern: "DATABASE_DIRECT_URL"
---

<objective>
Delete `docker-compose.yml` entirely (D-10), create a project `README.md` with a self-contained "Infrastructure Setup" section that walks a buyer through creating free Neon, Upstash, and Bonsai accounts and copying each connection string into the matching env var (D-02), and update the one stale current-facing docker-compose reference in `ROADMAP.md` Phase 1.

Purpose: D-10 removes the last Docker artifact; D-02 gives buyers a no-external-links setup guide; the ROADMAP Phase 1 one-liner is current docs, not a historical record.
Output: deleted `docker-compose.yml`, new `README.md`, updated `ROADMAP.md`.

This plan runs after plans 01 and 02 so the README documents the final env var set and the env-example sweep is already complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-replace-docker-with-neon-upstash/10-CONTEXT.md
@.planning/phases/10-replace-docker-with-neon-upstash/10-RESEARCH.md
@.planning/phases/10-replace-docker-with-neon-upstash/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete docker-compose.yml and create the README Infrastructure Setup section</name>
  <files>docker-compose.yml, README.md</files>
  <read_first>
    - docker-compose.yml (the three services being removed: postgres:16-alpine, redis:7-alpine, opensearch:2.18 — confirm contents before deleting)
    - .env.example (the final root env var set the README must mirror — produced by plan 02)
    - .planning/phases/10-replace-docker-with-neon-upstash/10-RESEARCH.md (Bonsai Sandbox shard limit, Upstash Fixed-plan note, signup hosts)
    - .planning/phases/10-replace-docker-with-neon-upstash/10-CONTEXT.md (D-01, D-02, D-03 onboarding decisions and the specific signup-link expectation)
  </read_first>
  <action>
    Delete `docker-compose.yml` from the repository root (no commented fallback, no backup file — per D-10). Create `README.md` at the repository root. The README must include a self-contained numbered "Infrastructure Setup" section that requires no external doc links beyond the three signup URLs, covering in order: (1) Neon — sign up at neon.com, create a project named grovio, copy the pooled connection string into `DATABASE_URL`, and copy the direct (non-pooler hostname) connection string into `DATABASE_DIRECT_URL`, noting the direct URL is required for `pnpm db:migrate`; (2) Upstash — sign up at upstash.com, create a Redis database, copy the `rediss://` connection string into `REDIS_URL`, and note that BullMQ workloads should use the Upstash Fixed plan to avoid per-command billing (RESEARCH.md Pitfall 2); (3) Bonsai — sign up at bonsai.io, create a free Sandbox OpenSearch cluster, copy the cluster URL (with embedded credentials) into `OPENSEARCH_URL`, note it is required from Phase 3, and note that Sandbox is limited to 10 shards so indexes should be created with `number_of_replicas: 0` (RESEARCH.md Pitfall 5). State explicitly (per D-01) that there is no Docker fallback and that each buyer provisions their own free-tier accounts (per D-03). Do NOT include any `docker-compose up` instructions anywhere in the README. Use placeholder credentials only in any example URLs.
  </action>
  <verify>
    <automated>test ! -f docker-compose.yml && test -f README.md && grep -c "Infrastructure Setup" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` does not exist
    - `README.md` exists and contains the heading text `Infrastructure Setup`
    - `README.md` mentions all four env vars: `DATABASE_URL`, `DATABASE_DIRECT_URL`, `REDIS_URL`, `OPENSEARCH_URL`
    - `README.md` references neon.com, upstash.com, and bonsai.io as signup destinations
    - `README.md` contains no `docker-compose` substring
    - `README.md` documents the Bonsai `number_of_replicas: 0` Sandbox note and the Upstash Fixed-plan note
  </acceptance_criteria>
  <done>docker-compose.yml is gone and the README guides a buyer through all three cloud services and their env vars with no Docker references.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 1 plan description and confirm no docker-compose references remain in buyer-facing docs</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 plan list — locate the 01-01-PLAN.md one-liner mentioning "docker-compose infra")
    - .planning/phases/10-replace-docker-with-neon-upstash/10-RESEARCH.md (the open-question recommendation: update ROADMAP Phase 1 one-liner; .planning/phases/ docs are immutable historical records and must NOT be edited)
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, find the Phase 1 `01-01-PLAN.md` bullet whose description ends with `docker-compose infra` and replace that trailing phrase with `cloud infra setup (Neon/Upstash/Bonsai)`. Change only that phrase on that one line — leave the rest of the bullet and every other phase entry untouched. Do NOT modify any file under `.planning/phases/` (those are immutable execution records per RESEARCH.md Pitfall 6). After editing, confirm via the verify grep that no buyer-facing markdown (excluding `.planning/`) still references docker-compose.
  </action>
  <verify>
    <automated>grep -c "cloud infra setup" .planning/ROADMAP.md; grep -rn "docker-compose" --include="*.md" . | grep -v '\.planning/' | grep -v 'node_modules' || true</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 1 `01-01-PLAN.md` bullet contains `cloud infra setup (Neon/Upstash/Bonsai)` and no longer contains `docker-compose infra`
    - `grep -rn "docker-compose" --include="*.md" .` excluding `.planning/` and `node_modules` returns zero matches
    - No file under `.planning/phases/` was modified by this task
  </acceptance_criteria>
  <done>ROADMAP Phase 1 description reflects cloud infra and no buyer-facing markdown references docker-compose.</done>
</task>

</tasks>

<threat_model>
- id: T-10-02
  stride: [Information Disclosure]
  threat: Real credentials pasted into README example URLs and committed
  mitigation: README uses placeholder credentials only; buyers paste real strings into gitignored .env, never the README
  severity: medium
- id: T-10-04
  stride: [Spoofing]
  threat: Buyer disables Bonsai TLS validation (rejectUnauthorized:false) following bad guidance
  mitigation: README does not instruct disabling TLS validation; Bonsai uses a publicly trusted CA so default validation works (RESEARCH.md anti-pattern)
  severity: medium
</threat_model>

<verification>
- `docker-compose.yml` absent; `README.md` present with Infrastructure Setup section
- ROADMAP Phase 1 one-liner updated
- Zero docker-compose references in buyer-facing markdown
</verification>

<success_criteria>
- docker-compose.yml deleted entirely with no fallback
- README documents Neon/Upstash/Bonsai signup and all four env vars, self-contained
- ROADMAP Phase 1 description updated to cloud infra
- No buyer-facing doc references Docker
</success_criteria>

<output>
Create `.planning/phases/10-replace-docker-with-neon-upstash/10-03-SUMMARY.md` when done
</output>
