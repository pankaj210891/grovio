---
phase: 03-catalog-search
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
autonomous: false
requirements: [PROD-05, PROD-06, SRCH-01, PROD-04]
must_haves:
  truths:
    - "@opensearch-project/opensearch, bullmq, @aws-sdk/client-s3, @aws-sdk/s3-request-presigner, argon2 are installed in @grovio/api"
    - "Each package was verified legitimate on its registry before install"
  artifacts:
    - path: "apps/api/package.json"
      provides: "Phase 3 runtime dependencies"
      contains: "@opensearch-project/opensearch"
  key_links:
    - from: "apps/api/package.json"
      to: "node_modules"
      via: "pnpm install"
      pattern: "argon2"
---

<objective>
Install the five new runtime dependencies Phase 3 requires, each gated behind a package-legitimacy verification checkpoint because slopcheck was unavailable at research time (all packages tagged [ASSUMED] in RESEARCH.md Package Legitimacy Audit).

Purpose: Make the OpenSearch client, BullMQ queue, AWS S3 presigner, and argon2 hashing available to downstream service plans (03-05, 03-06) while honoring the security gate for unverified packages.
Output: Updated apps/api/package.json + lockfile with the five packages installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-catalog-search/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy (5 [ASSUMED] packages)</name>
  <what-built>
    Nothing built yet. RESEARCH.md Package Legitimacy Audit marked all five Phase 3 packages [ASSUMED] because slopcheck was unavailable. Per the security gate, a human must confirm each package on its registry before install.
  </what-built>
  <action>
    Pause before any install runs. Present the five registry URLs below for human package-legitimacy verification. Do NOT run `pnpm add` until the human replies "approved". If the human names any package as suspicious, block that package and stop. This is a blocking-human gate — never auto-approve regardless of workflow.auto_advance.
  </action>
  <how-to-verify>
    Open each registry page and confirm the package is the well-known authoritative package (publisher org, weekly downloads, source repo match):
    1. https://www.npmjs.com/package/@opensearch-project/opensearch — expect publisher opensearch-project, repo github.com/opensearch-project/opensearch-js, ~50K+ wk downloads
    2. https://www.npmjs.com/package/bullmq — expect repo github.com/taskforcesh/bullmq, ~2M wk downloads
    3. https://www.npmjs.com/package/@aws-sdk/client-s3 — expect publisher aws, repo github.com/aws/aws-sdk-js-v3, ~10M wk downloads
    4. https://www.npmjs.com/package/@aws-sdk/s3-request-presigner — expect publisher aws, same aws-sdk-js-v3 repo
    5. https://www.npmjs.com/package/argon2 — expect repo github.com/ranisalt/node-argon2, ~850K wk downloads
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking-human legitimacy gate; no automated verification (manual registry confirmation required before install)</automated>
  </verify>
  <acceptance_criteria>
    - Human has confirmed all five packages on their registry pages
    - Human replied "approved" (or named packages to block)
    - No `pnpm add` ran before approval
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or name any package that looks suspicious to block it.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the five Phase 3 packages</name>
  <files>apps/api/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/api/package.json (current dependency list and versions — confirm none of the five already present, confirm Node/engines field)
    - .planning/phases/03-catalog-search/03-RESEARCH.md (Standard Stack versions: @opensearch-project/opensearch 3.6.x, bullmq 5.77.x, @aws-sdk/client-s3 3.x, @aws-sdk/s3-request-presigner 3.x, argon2 latest)
    - CLAUDE.md (Version Compatibility Matrix — BullMQ 5.x requires ioredis 5.x already installed; argon2 needs Node 22 native build)
  </read_first>
  <action>
    Run `pnpm add @opensearch-project/opensearch bullmq @aws-sdk/client-s3 @aws-sdk/s3-request-presigner argon2 --filter @grovio/api`. argon2 is a native module — if the install fails to build native bindings on Windows, fall back to documenting the failure in the summary and DO NOT silently substitute bcrypt (argon2 is the locked choice from RESEARCH.md; report instead). After install, run `pnpm --filter @grovio/api install` to settle the lockfile.
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/api/package.json').dependencies; ['@opensearch-project/opensearch','bullmq','@aws-sdk/client-s3','@aws-sdk/s3-request-presigner','argon2'].forEach(k=>{if(!p[k]){console.error('MISSING '+k);process.exit(1)}}); console.log('all present')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/package.json dependencies contains @opensearch-project/opensearch
    - apps/api/package.json dependencies contains bullmq
    - apps/api/package.json dependencies contains @aws-sdk/client-s3
    - apps/api/package.json dependencies contains @aws-sdk/s3-request-presigner
    - apps/api/package.json dependencies contains argon2
    - the node -e verification command prints "all present" and exits 0
  </acceptance_criteria>
  <done>All five packages appear in apps/api/package.json dependencies; lockfile updated; node check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party package code enters the build/runtime; supply-chain trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (5 [ASSUMED] packages) | mitigate | slopcheck + blocking-human legitimacy checkpoint (Task 1) before any install; registry publisher/repo/download verification per RESEARCH.md audit |
| T-03-SC2 | Tampering | argon2 native binary | accept | argon2 compiles native bindings; build provenance trusted via npm publisher ranisalt; pinned via lockfile |
</threat_model>

<verification>
- All five packages present in apps/api/package.json
- pnpm lockfile resolves without unmet peer dependency errors for ioredis 5.x (BullMQ transport)
- Legitimacy checkpoint approved before install ran
</verification>

<success_criteria>
- Five Phase 3 packages installed in @grovio/api
- Human legitimacy verification completed (gate satisfied)
</success_criteria>

<output>
Create `.planning/phases/03-catalog-search/03-02-SUMMARY.md` when done
</output>
