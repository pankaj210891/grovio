---
phase: 03-catalog-search
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - apps/api/src/db/migrations/
  - apps/api/src/modules/attribute-definitions/AttributeDefinitionService.ts
  - apps/api/src/modules/attribute-definitions/AttributeDefinitionService.test.ts
  - apps/api/src/modules/filter-schema/FilterSchemaService.ts
  - apps/api/src/modules/filter-schema/FilterSchemaService.test.ts
autonomous: true
requirements: [PROD-03, PROD-04, SRCH-02, SRCH-04]
must_haves:
  truths:
    - "All Phase 3 tables exist in the live database after migration"
    - "The deferred vendor_id FK on vendor_category_restrictions points to vendors.id (D-18)"
    - "is_variant and is_filterable cannot both be true on the same attribute (D-02 specifics)"
    - "Changing a category filter schema invalidates the category_filter_schema:{id} Redis cache (Pitfall 6)"
  artifacts:
    - path: "apps/api/src/db/migrations/"
      provides: "Generated migration SQL for Phase 3 tables + deferred FK"
      contains: "CREATE TABLE"
  key_links:
    - from: "AttributeDefinitionService"
      to: "is_variant / is_filterable mutual exclusivity"
      via: "service-layer guard"
      pattern: "isVariant"
    - from: "FilterSchemaService.replaceFilterSchema"
      to: "redis.del"
      via: "write-through cache invalidation"
      pattern: "category_filter_schema"
---

<objective>
Run the [BLOCKING] database migration that creates all Phase 3 tables and adds the deferred vendor FK (D-18), and add the two Phase 2 service-layer guards that Phase 3 requires: is_variant/is_filterable mutual exclusivity in AttributeDefinitionService (D-02 specifics, Pitfall 5) and category_filter_schema Redis cache invalidation in FilterSchemaService (Pitfall 6, search-facing).

Purpose: The migration is mandatory — build/typecheck pass without it (types come from schema files, not the live DB), so the phase would otherwise reach a false-positive verification state. The two guards close consistency holes that the new variant axes and search facet caching introduce.
Output: Generated + applied migration; two updated services with new guards + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-catalog-search/03-CONTEXT.md
@.planning/phases/03-catalog-search/03-RESEARCH.md
@apps/api/src/db/schema/vendor-category-restrictions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_variant/is_filterable mutual-exclusivity guard to AttributeDefinitionService</name>
  <files>apps/api/src/modules/attribute-definitions/AttributeDefinitionService.ts, apps/api/src/modules/attribute-definitions/AttributeDefinitionService.test.ts</files>
  <read_first>
    - apps/api/src/modules/attribute-definitions/AttributeDefinitionService.ts (current createAttribute, updateAttribute, validateOptions private helper — add an analogous validateVariantExclusivity guard following the same throw style)
    - apps/api/src/modules/categories/CategoryService.test.ts (mock helper patterns — makeDbMock/makeInsertDbMock/makeUpdateDbMock — to extend the existing attribute test)
    - apps/api/src/db/schema/attribute-definitions.ts (confirm is_variant column added by plan 03-03)
    - .planning/phases/03-catalog-search/03-CONTEXT.md (D-02; specifics: is_variant mutually exclusive with is_filterable)
    - .planning/phases/03-catalog-search/03-RESEARCH.md (Pitfall 5)
  </read_first>
  <action>
    Add `isVariant?: boolean` to the `UpdateAttributeInput` interface. In `createAttribute`, accept `isVariant` from input (default false) and set it in insertValues. Add a private `validateVariantExclusivity(isVariant: boolean, isFilterable: boolean)` helper that throws when both are true (message: an attribute marked is_variant cannot also be is_filterable — a variant axis is not a standalone filter facet). Call it in `createAttribute` using the resolved isVariant/isFilterable values. In `updateAttribute`, before writing, load the current row, merge the incoming partial over it, and call validateVariantExclusivity on the MERGED values (so setting is_variant=true on an already-filterable attribute is rejected, and vice versa) per D-02 specifics / Pitfall 5. Keep the existing validateOptions behavior unchanged.
    Extend AttributeDefinitionService.test.ts with cases: createAttribute throws when isVariant=true AND isFilterable=true; createAttribute succeeds when only isVariant=true; updateAttribute throws when setting isFilterable=true on an attribute that already has isVariant=true (mocked current row). Follow the existing mock-chain helper style in the test file.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api test -- --run src/modules/attribute-definitions/AttributeDefinitionService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - AttributeDefinitionService.ts contains a guard referencing both `isVariant` and `isFilterable` that throws when both true
    - createAttribute sets isVariant in its insertValues
    - updateAttribute loads the current row and validates merged isVariant/isFilterable
    - test file contains a case asserting createAttribute rejects isVariant=true + isFilterable=true
    - `pnpm --filter @grovio/api test -- --run src/modules/attribute-definitions/AttributeDefinitionService.test.ts` exits 0
  </acceptance_criteria>
  <done>Mutual-exclusivity guard enforced in both create and update at the service layer; tests prove the rejection and the allowed single-flag case.</done>
</task>

<task type="auto">
  <name>Task 2: Add category_filter_schema Redis cache invalidation to FilterSchemaService</name>
  <files>apps/api/src/modules/filter-schema/FilterSchemaService.ts, apps/api/src/modules/filter-schema/FilterSchemaService.test.ts</files>
  <read_first>
    - apps/api/src/modules/filter-schema/FilterSchemaService.ts (current deps interface is db-only — must add redis; current replaceFilterSchema/upsertFilterEntry/removeFilterEntry/reorderFilterEntries methods are the mutation points)
    - apps/api/src/modules/categories/CategoryService.ts (write-through invalidation pattern — invalidateTree calling redis.del after DB write)
    - apps/api/src/modules/feature-flags/FeatureFlagService.ts (Redis dep injection via Awilix; redisKey helper style)
    - .planning/phases/03-catalog-search/03-RESEARCH.md (Pitfall 6; Open Question 3 recommendation — invalidation co-located in replaceFilterSchema)
    - .planning/phases/03-catalog-search/03-PATTERNS.md (write-through cache invalidation shared pattern; Redis key naming category_filter_schema:{categoryId})
  </read_first>
  <action>
    Change `FilterSchemaServiceDeps` to add `redis: Redis` (import ioredis Redis type as the other services do). Add a private `private invalidateFilterCache(categoryId: string)` that calls `await this.deps.redis.del(\`category_filter_schema:${categoryId}\`)` (Redis key convention from PATTERNS.md). Call invalidateFilterCache(categoryId) at the END of `replaceFilterSchema` (after all inserts), `upsertFilterEntry` (using input.categoryId), and `reorderFilterEntries` (using the _categoryId param — rename to categoryId and use it). For `removeFilterEntry`, the categoryId is not in scope; load the row's categoryId before delete, or document that removal invalidation is best-effort — prefer loading categoryId from the row before deletion and invalidating. Per Pitfall 6, the cache key must match what SearchService reads in plan 03-06.
    Update FilterSchemaService.test.ts: add a redis mock (`{ del: vi.fn(), get: vi.fn(), setex: vi.fn() }`) to the deps; assert redis.del is called with `category_filter_schema:${categoryId}` after replaceFilterSchema and upsertFilterEntry. Keep existing filterable-gate tests passing (they now pass a redis mock too).
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api test -- --run src/modules/filter-schema/FilterSchemaService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - FilterSchemaService.ts deps interface includes `redis`
    - FilterSchemaService.ts contains `category_filter_schema:` in a redis.del call
    - replaceFilterSchema and upsertFilterEntry both invalidate the cache
    - test file asserts redis.del called with the category_filter_schema key
    - `pnpm --filter @grovio/api test -- --run src/modules/filter-schema/FilterSchemaService.test.ts` exits 0
  </acceptance_criteria>
  <done>FilterSchemaService injects redis and invalidates category_filter_schema:{id} on every mutation; tests prove invalidation; existing filterable-gate tests still green.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Generate + apply Phase 3 migration (tables + deferred vendor FK)</name>
  <files>apps/api/src/db/migrations/</files>
  <read_first>
    - apps/api/drizzle.config.ts (confirms migration output dir, schema glob, and that it uses DATABASE_DIRECT_URL for migrations)
    - apps/api/package.json (db:generate = drizzle-kit generate, db:migrate = drizzle-kit migrate)
    - apps/api/src/db/schema/vendor-category-restrictions.ts (deferred FK comment — D-18 ALTER TABLE adds vcr_vendor_id_fk referencing vendors.id)
    - .planning/phases/03-catalog-search/03-CONTEXT.md (D-18)
  </read_first>
  <action>
    This is the mandatory schema-push gate. Run in sequence (drizzle-kit generate is non-interactive): `pnpm --filter @grovio/api db:generate` then `pnpm --filter @grovio/api db:migrate`. The generate step must produce a migration that CREATEs vendors, products (with product_status enum + GIN index on attributes), product_variants, product_images, and ALTERs attribute_definitions to add is_variant. The deferred FK from D-18 (vendor_category_restrictions.vendor_id → vendors.id) is NOT auto-generated by Drizzle because the Phase 2 column has no .references(). After running db:generate, manually append the deferred FK to the generated migration SQL file: `ALTER TABLE "vendor_category_restrictions" ADD CONSTRAINT "vcr_vendor_id_fk" FOREIGN KEY ("vendor_id") REFERENCES "vendors"("id");` (place AFTER the vendors CREATE TABLE statement so vendors exists first). Then run db:migrate to apply. If db:migrate cannot read DATABASE_DIRECT_URL, run it with the env file the dev script uses (the api dev script loads .env via --env-file); confirm DATABASE_DIRECT_URL is set before migrating. Migrating against the live Neon database is required — build/typecheck do NOT exercise the live schema.
  </action>
  <verify>
    <automated>pnpm --filter @grovio/api build && node -e "const fs=require('fs');const dir='apps/api/src/db/migrations';const f=fs.readdirSync(dir).filter(x=>x.endsWith('.sql'));const sql=f.map(x=>fs.readFileSync(dir+'/'+x,'utf8')).join('\n');['CREATE TABLE \"vendors\"','CREATE TABLE \"products\"','CREATE TABLE \"product_variants\"','CREATE TABLE \"product_images\"','vcr_vendor_id_fk','gin'].forEach(t=>{if(!sql.toLowerCase().includes(t.toLowerCase())){console.error('MISSING '+t);process.exit(1)}});console.log('migration-sql-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - A new .sql migration file exists under apps/api/src/db/migrations
    - Migration SQL contains CREATE TABLE for vendors, products, product_variants, product_images
    - Migration SQL contains the GIN index on products.attributes
    - Migration SQL contains the vcr_vendor_id_fk deferred FK ALTER (D-18)
    - `pnpm --filter @grovio/api db:migrate` applies cleanly (no pending migrations afterward)
    - `pnpm --filter @grovio/api build` exits 0
    - verification node check prints "migration-sql-ok"
  </acceptance_criteria>
  <done>All Phase 3 tables created in the live database; GIN index present on products.attributes; deferred vendor FK applied; no pending migrations remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin attribute config → DB | Admin-set is_variant/is_filterable flags drive both variant axes and search facets; inconsistency causes UI/search divergence |
| filter schema mutation → search cache | Stale Redis cache serves outdated facets to customers |
| migration runner → live DB | Schema DDL applied to production Neon instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-G1 | Tampering | attribute is_variant/is_filterable | mitigate | service-layer mutual-exclusivity guard on create + merged update (Pitfall 5); prevents an attribute being both a variant axis and a filter facet |
| T-03-G2 | Information Disclosure | category_filter_schema Redis cache | mitigate | write-through invalidation on every filter-schema mutation prevents serving stale/removed facets (Pitfall 6) |
| T-03-G3 | Tampering | vendor_category_restrictions.vendor_id | mitigate | deferred FK (D-18) enforces referential integrity — restriction rows cannot reference non-existent vendors |
| T-03-G4 | Denial of Service | db:migrate against live Neon | accept | migration is additive (CREATE/ALTER, no destructive drops); run during planned window; Neon supports rollback via branch if needed |
</threat_model>

<verification>
- `pnpm --filter @grovio/api build` exits 0
- `pnpm --filter @grovio/api db:migrate` reports no pending migrations
- `pnpm --filter @grovio/api test -- --run src/modules/attribute-definitions src/modules/filter-schema` green
- Migration SQL contains all four CREATE TABLEs + GIN index + deferred FK
</verification>

<success_criteria>
- All Phase 3 tables live in the database (D-04, D-12)
- Deferred vendor FK applied (D-18)
- is_variant/is_filterable mutual exclusivity enforced (D-02, Pitfall 5)
- Filter schema cache invalidation wired (Pitfall 6)
</success_criteria>

<output>
Create `.planning/phases/03-catalog-search/03-04-SUMMARY.md` when done
</output>
