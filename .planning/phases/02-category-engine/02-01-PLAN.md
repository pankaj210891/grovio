---
phase: 02-category-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/category/blocks.ts
  - packages/contracts/src/category/tree.ts
  - packages/contracts/src/category/attribute-definition.ts
  - packages/contracts/src/category/filter-schema.ts
  - packages/contracts/src/category/product-template.ts
  - packages/contracts/src/category/vendor-restriction.ts
  - packages/contracts/src/category/metadata.ts
  - packages/contracts/src/category/index.ts
  - packages/contracts/src/category/blocks.test.ts
  - packages/contracts/src/index.ts
autonomous: true
requirements: [CAT-03, CAT-04, CAT-05, CAT-06, CAT-07]
must_haves:
  truths:
    - "@grovio/contracts exports MerchandisingBlockSchema discriminated union (banner, product_grid, text_block) per D-12/D-14"
    - "@grovio/contracts exports CategoryTreeNode/CategoryTreeResponse with nav-only fields per D-04"
    - "@grovio/contracts exports AttrType enum of exactly 6 values per D-05/D-06"
    - "MerchandisingBlockSchema rejects unknown block types and accepts all 3 valid types"
  artifacts:
    - path: "packages/contracts/src/category/blocks.ts"
      provides: "Block discriminated union + Zod (D-14)"
      contains: "z.discriminatedUnion"
    - path: "packages/contracts/src/category/index.ts"
      provides: "Category contracts barrel"
    - path: "packages/contracts/src/category/blocks.test.ts"
      provides: "Block validation tests (CAT-07)"
  key_links:
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/category/index.js"
      via: "export *"
      pattern: "export \\* from \"./category/index.js\""
    - from: "packages/contracts/src/category/metadata.ts"
      to: "packages/contracts/src/category/blocks.js"
      via: "import MerchandisingBlockSchema"
      pattern: "from \"./blocks.js\""
---

<objective>
Create the `packages/contracts/src/category/` domain subfolder — the single source of truth for all Phase 2 category TypeScript types and Zod schemas. Every downstream consumer (API services, admin panel, Phase 3 catalog, Phase 4 storefront/CMS) imports from here. No duplication of block types, attribute types, or filter types anywhere else.

Purpose: Locks the cross-phase data contract before any service or UI code is written, enabling interface-first execution. The block discriminated union (D-14) and attribute type enum (D-06) are forward-compatible contracts that Phase 4 extends without migration.
Output: 8 new contract files + 1 test file + updated root barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-category-engine/02-CONTEXT.md
@.planning/phases/02-category-engine/02-PATTERNS.md
@.planning/phases/02-category-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create block + tree + attribute-definition contracts</name>
  <files>packages/contracts/src/category/blocks.ts, packages/contracts/src/category/tree.ts, packages/contracts/src/category/attribute-definition.ts, packages/contracts/src/category/blocks.test.ts</files>
  <read_first>
    - packages/contracts/src/feature-flags.ts (Zod schema + z.infer type-export pattern; JSDoc header convention)
    - packages/contracts/src/auth.ts (z.enum pattern for fixed value sets)
    - .planning/phases/02-category-engine/02-PATTERNS.md (sections for blocks.ts, tree.ts, attribute-definition.ts — exact schema shapes)
    - .planning/phases/02-category-engine/02-RESEARCH.md (§API Contract Shapes, Pattern 4)
  </read_first>
  <behavior>
    - blocks.test.ts: MerchandisingBlockSchema.parse() accepts a valid banner block {type:'banner', imageUrl, title}
    - blocks.test.ts: MerchandisingBlockSchema.parse() accepts a valid product_grid block {type:'product_grid', title, productIds:[uuid], layout:'grid'}
    - blocks.test.ts: MerchandisingBlockSchema.parse() accepts a valid text_block {type:'text_block', title, content}
    - blocks.test.ts: MerchandisingBlockSchema.safeParse() returns success:false for {type:'unknown_type', ...}
    - blocks.test.ts: MerchandisingBlockSchema.safeParse() returns success:false for a product_grid missing productIds
  </behavior>
  <action>
    Create blocks.ts exporting BannerBlockSchema (type literal "banner", imageUrl url, title, optional subtitle/ctaText/ctaUrl), ProductGridBlockSchema (type literal "product_grid", title, productIds array of uuid, layout z.enum ["grid","carousel"]), TextBlockSchema (type literal "text_block", title, content), and MerchandisingBlockSchema as z.discriminatedUnion("type", [...]) per D-12 and D-14. Export inferred types BannerBlock, ProductGridBlock, TextBlock, MerchandisingBlock.
    Create tree.ts exporting CategoryTreeNode (declared as an explicit TypeScript type first to resolve the recursive children reference), CategoryTreeNodeSchema using z.lazy() with fields id, name, slug, parentId (nullable uuid), sortOrder, depth (int 0..2), hasChildren, childCount, children (array of self) per D-04, and CategoryTreeResponseSchema { tree: array of nodes } with inferred CategoryTreeResponse.
    Create attribute-definition.ts exporting AttrTypeSchema = z.enum with exactly the 6 values text, textarea, number, boolean, enum, multi_select per D-05/D-06; AttributeOptionSchema { value, label } per D-07; AttributeDefinitionSchema (id, categoryId, key, label, attrType, options nullable array, isRequired, isFilterable, isSearchable, sortOrder); CreateAttributeInputSchema (key, label, attrType, optional options/isRequired/isFilterable/isSearchable/sortOrder). Export inferred types AttrType, AttributeOption, AttributeDefinition, CreateAttributeInput.
    Create blocks.test.ts with the 5 behavior cases above. Use the import path "./blocks.js".
    Do not use any free-form type strings for attrType — the z.enum is exhaustive per D-06.
  </action>
  <verify>
    <automated>cd "d:/My Projects/grovio" && pnpm --filter @grovio/contracts test blocks 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - blocks.ts contains `export const MerchandisingBlockSchema` using `z.discriminatedUnion("type"`
    - attribute-definition.ts contains `z.enum(["text", "textarea", "number", "boolean", "enum", "multi_select"])` (exactly 6 values, no more)
    - tree.ts contains `z.lazy(` for the recursive CategoryTreeNodeSchema
    - tree.ts CategoryTreeNode type includes exactly: id, name, slug, parentId, sortOrder, depth, hasChildren, childCount, children (no attribute/metadata fields — D-04)
    - `pnpm --filter @grovio/contracts test blocks` exits 0 with all 5 assertions passing
  </acceptance_criteria>
  <done>blocks.ts, tree.ts, attribute-definition.ts, and blocks.test.ts exist; block validation tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create filter-schema + product-template + vendor-restriction + metadata contracts</name>
  <files>packages/contracts/src/category/filter-schema.ts, packages/contracts/src/category/product-template.ts, packages/contracts/src/category/vendor-restriction.ts, packages/contracts/src/category/metadata.ts</files>
  <read_first>
    - packages/contracts/src/category/attribute-definition.ts (created in Task 1 — filter-schema.ts imports and .pick()s from AttributeDefinitionSchema)
    - packages/contracts/src/category/blocks.ts (created in Task 1 — metadata.ts imports MerchandisingBlockSchema)
    - .planning/phases/02-category-engine/02-PATTERNS.md (sections for filter-schema.ts, product-template.ts, vendor-restriction.ts, metadata.ts)
    - .planning/phases/02-category-engine/02-RESEARCH.md (§API Contract Shapes, Pattern 6 display_type mapping)
  </read_first>
  <action>
    Create filter-schema.ts: DisplayTypeSchema = z.enum ["checkbox","radio","range_slider","toggle"] (Claude's Discretion choice per Pattern 6); FilterSchemaDefSchema (id, categoryId, attributeDefId, attribute = AttributeDefinitionSchema.pick key/label/attrType/options, displayType, sortOrder); UpsertFilterSchemaInputSchema { filters: array of {attributeDefId, displayType, sortOrder} }. Import AttributeDefinitionSchema from "./attribute-definition.js". This addresses CAT-04.
    Create product-template.ts: TemplateFieldSchema (key, optional default union string|number|boolean, optional hint) — template_fields reference attribute_definition keys by `key` text, not by id (Claude's Discretion / Open Question 2 recommendation); ProductTemplateSchema (id, categoryId, templateFields array); UpsertTemplateInputSchema { templateFields }. This addresses CAT-05.
    Create vendor-restriction.ts: VendorCategoryRestrictionSchema (id, categoryId, vendorId uuid, createdAt datetime string, createdByAdminId uuid) per D-10. This addresses CAT-06.
    Create metadata.ts: CategoryMetadataSchema (id, categoryId, seoTitle/seoDescription/seoKeywords nullable, canonicalUrl nullable, blocks array of MerchandisingBlockSchema, description nullable, imageUrl nullable url) per D-12/D-13; UpsertMetadataInputSchema with all fields optional. Import MerchandisingBlockSchema from "./blocks.js". This addresses CAT-07.
    Export inferred types for every schema via z.infer.
  </action>
  <verify>
    <automated>cd "d:/My Projects/grovio" && pnpm --filter @grovio/contracts typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - filter-schema.ts contains `z.enum(["checkbox", "radio", "range_slider", "toggle"])`
    - filter-schema.ts imports from "./attribute-definition.js" and uses `.pick(`
    - product-template.ts TemplateFieldSchema references `key` (string), not an id-based reference (per D-04 Open Question 2)
    - metadata.ts imports MerchandisingBlockSchema from "./blocks.js" and includes flat SEO columns seoTitle/seoDescription/seoKeywords/canonicalUrl (D-13)
    - vendor-restriction.ts VendorCategoryRestrictionSchema has fields id, categoryId, vendorId, createdAt, createdByAdminId (D-10)
    - `pnpm --filter @grovio/contracts typecheck` exits 0
  </acceptance_criteria>
  <done>All four contract files exist and typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire category barrel and root export</name>
  <files>packages/contracts/src/category/index.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - packages/contracts/src/index.ts (current root barrel — append, do not replace existing exports)
    - .planning/phases/02-category-engine/02-PATTERNS.md (category/index.ts and root index.ts barrel sections)
  </read_first>
  <action>
    Create packages/contracts/src/category/index.ts that re-exports all 7 category files: `export * from "./blocks.js"`, `./tree.js`, `./attribute-definition.js`, `./filter-schema.js`, `./product-template.js`, `./vendor-restriction.js`, `./metadata.js`.
    Update packages/contracts/src/index.ts by appending `export * from "./category/index.js";` after the existing marketplace-config.js export. Do not remove any existing export.
  </action>
  <verify>
    <automated>cd "d:/My Projects/grovio" && pnpm --filter @grovio/contracts typecheck 2>&1 | tail -10 && pnpm --filter @grovio/contracts build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/contracts/src/index.ts contains `export * from "./category/index.js";`
    - packages/contracts/src/index.ts still contains the original 5 exports (health, feature-flags, auth, envelope, marketplace-config)
    - category/index.ts re-exports all 7 category source files
    - `pnpm --filter @grovio/contracts build` exits 0
    - A consumer can `import { MerchandisingBlock, AttrType, CategoryTreeNode } from "@grovio/contracts"` (verified by build)
  </acceptance_criteria>
  <done>Barrel wiring complete; contracts package builds and exports all category types from the root entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin panel / API → contracts | Schemas in this package are the validation gate reused server-side; if a schema is too permissive, malformed data reaches the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | MerchandisingBlockSchema | mitigate | z.discriminatedUnion rejects unknown block types and structurally-invalid blocks; blocks.test.ts asserts rejection of malformed/unknown blocks before the server-side parse in CategoryMetadataService consumes it (plan 02-05) |
| T-02-02 | Tampering | AttrTypeSchema | mitigate | z.enum of exactly 6 values forbids free-form type strings (D-06), preventing arbitrary attr_type values from being accepted upstream |
| T-02-SC | Tampering | npm installs | accept | No package installs in this plan — contracts uses only the already-installed zod; legitimacy gate is handled in plan 02-07 (web-admin) |
</threat_model>

<verification>
- `pnpm --filter @grovio/contracts test` — full contracts suite green (includes blocks.test.ts)
- `pnpm --filter @grovio/contracts build` — package builds, all category types exported from root
</verification>

<success_criteria>
- 8 category contract files + blocks.test.ts created; root barrel updated
- Block discriminated union validates 3 valid types and rejects unknown/malformed (CAT-07)
- AttrType enum has exactly 6 values (CAT-03 / D-06)
- CategoryTreeNode is nav-fields-only (D-04)
- `@grovio/contracts` builds and re-exports all category types
</success_criteria>

<output>
Create `.planning/phases/02-category-engine/02-01-SUMMARY.md` when done
</output>
