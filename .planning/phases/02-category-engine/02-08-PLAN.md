---
phase: 02-category-engine
plan: 08
type: execute
wave: 6
depends_on: [02-07]
files_modified:
  - apps/web-admin/src/pages/categories/AttributeBuilderPage.tsx
  - apps/web-admin/src/pages/categories/FilterSchemaPage.tsx
  - apps/web-admin/src/pages/categories/ProductTemplatePage.tsx
  - apps/web-admin/src/pages/categories/VendorRestrictionsPage.tsx
  - apps/web-admin/src/pages/categories/CategoryMetadataPage.tsx
  - apps/web-admin/src/components/categories/AttributeRow.tsx
  - apps/web-admin/src/components/categories/BlockEditor.tsx
  - apps/web-admin/src/pages/categories/CategoryDetailPage.tsx
autonomous: false
requirements: [CAT-03, CAT-04, CAT-05, CAT-06, CAT-07]
must_haves:
  truths:
    - "Admin can define typed attributes per category (6 types, options for enum/multi_select) via a reorderable form list (D-08)"
    - "Admin can mark which attributes become filter facets and pick a display type per facet (CAT-04)"
    - "Admin can edit the product template, manage restricted-vendor lists, and edit SEO + merchandising blocks (CAT-05, CAT-06, CAT-07)"
    - "Block editor produces blocks validated by the server-side MerchandisingBlockSchema"
  artifacts:
    - path: "apps/web-admin/src/pages/categories/AttributeBuilderPage.tsx"
      provides: "Attribute schema builder (CAT-03)"
      contains: "AttributeRow"
    - path: "apps/web-admin/src/components/categories/BlockEditor.tsx"
      provides: "Merchandising block editor (CAT-07)"
      contains: "banner"
  key_links:
    - from: "apps/web-admin/src/pages/categories/AttributeBuilderPage.tsx"
      to: "POST /admin/categories/:id/attributes"
      via: "save mutation"
      pattern: "attributes"
    - from: "apps/web-admin/src/pages/categories/CategoryMetadataPage.tsx"
      to: "PUT /admin/categories/:id/metadata"
      via: "save mutation"
      pattern: "metadata"
---

<objective>
Complete the admin category UI: the attribute schema builder (simple reorderable form list per D-08, with conditional options editor for enum/multi_select), the filter schema editor (mark filterable attributes as facets with a display type), the product template editor, the vendor restriction manager, and the category metadata editor (SEO fields + a typed merchandising block editor). All wire into the CategoryDetailPage tab container scaffolded in plan 02-07.

Purpose: Delivers CAT-03, CAT-04, CAT-05, CAT-06, CAT-07 success criteria through the admin UI — the configuration surfaces that make the platform vertical-agnostic.
Output: 5 editor pages + AttributeRow + BlockEditor + the wired tab container.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-category-engine/02-CONTEXT.md
@.planning/phases/02-category-engine/02-PATTERNS.md
@.planning/phases/02-category-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attribute builder + filter schema editors (CAT-03, CAT-04)</name>
  <files>apps/web-admin/src/pages/categories/AttributeBuilderPage.tsx, apps/web-admin/src/components/categories/AttributeRow.tsx, apps/web-admin/src/pages/categories/FilterSchemaPage.tsx</files>
  <read_first>
    - apps/web-admin/src/App.tsx (motion + grovio token classes)
    - apps/web-admin/src/lib/apiClient.ts (query/mutation helpers from plan 02-07)
    - apps/web-admin/src/components/categories/CategoryTree.tsx (dnd-kit useSortable usage example from 02-07)
    - .planning/phases/02-category-engine/02-PATTERNS.md (AttributeRow.tsx + Attribute Builder + Block Editor sections; useSortable drag handle; conditional options editor)
    - .planning/phases/02-category-engine/02-RESEARCH.md (§Admin UI Component Strategy — attribute builder D-08 form list; filter display_type mapping Pattern 6)
    - packages/contracts/src/category/attribute-definition.ts (CreateAttributeInput, AttrType, AttributeOption), packages/contracts/src/category/filter-schema.ts (DisplayType, UpsertFilterSchemaInput)
  </read_first>
  <action>
    Create AttributeRow.tsx: a single attribute form row using dnd-kit useSortable for a drag handle; fields key (auto-derived from label, editable), label, type dropdown (the 6 AttrType values), required checkbox, is_filterable checkbox, is_searchable checkbox, and up/down reorder buttons. When attrType is enum or multi_select, render a conditional options editor: a dynamic list of {value,label} input pairs with Add/Remove. Per D-08 this is a form list, NOT drag-from-palette.
    Create AttributeBuilderPage.tsx: useQuery(GET /categories/:id/attributes) to load existing attributes into local form-list state; allow add/remove/reorder rows; a single Save persists changes via the admin attribute routes (POST/PATCH/DELETE /admin/categories/:id/attributes and reorder) and invalidates the attributes query. Covers CAT-03.
    Create FilterSchemaPage.tsx: load the category's attributes (only those with is_filterable=true are eligible) and the current filter schema (GET /categories/:id/filters); let the admin add a filterable attribute as a facet, choose its displayType (checkbox/radio/range_slider/toggle, defaulting per the attrType→display mapping in Pattern 6), and reorder facets; Save via PUT /admin/categories/:id/filters (UpsertFilterSchemaInput). Covers CAT-04.
    Use grovio token Tailwind classes and the motion.div page-enter pattern.
  </action>
  <verify>
    <automated>cd "d:/My Projects/grovio" && pnpm --filter @grovio/web-admin typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - AttributeRow.tsx renders a type dropdown with the 6 AttrType values and conditionally renders the options editor only for enum/multi_select
    - AttributeBuilderPage.tsx loads attributes via useQuery and saves via the attribute admin routes, invalidating the attributes query key
    - FilterSchemaPage.tsx only offers is_filterable attributes as facets and lets the admin choose a DisplayType, saving via PUT .../filters
    - `pnpm --filter @grovio/web-admin typecheck` exits 0
  </acceptance_criteria>
  <done>Attribute builder and filter schema editors built and wired to the API.</done>
</task>

<task type="auto">
  <name>Task 2: Product template + vendor restrictions + metadata/blocks editors (CAT-05, CAT-06, CAT-07)</name>
  <files>apps/web-admin/src/pages/categories/ProductTemplatePage.tsx, apps/web-admin/src/pages/categories/VendorRestrictionsPage.tsx, apps/web-admin/src/pages/categories/CategoryMetadataPage.tsx, apps/web-admin/src/components/categories/BlockEditor.tsx, apps/web-admin/src/pages/categories/CategoryDetailPage.tsx</files>
  <read_first>
    - apps/web-admin/src/lib/apiClient.ts (query/mutation helpers)
    - apps/web-admin/src/pages/categories/CategoryDetailPage.tsx (tab container scaffolded in plan 02-07 — wire the 5 editors into it)
    - apps/web-admin/src/pages/categories/AttributeBuilderPage.tsx (created in Task 1 — sibling editor pattern; template references attribute keys)
    - .planning/phases/02-category-engine/02-PATTERNS.md (BlockEditor.tsx section — +button, per-type form fields, dnd-kit reorder, PUT metadata)
    - .planning/phases/02-category-engine/02-RESEARCH.md (§Admin UI Component Strategy — block editor, no WYSIWYG in v1; §API Contract Shapes admin PUT routes)
    - packages/contracts/src/category/product-template.ts (UpsertTemplateInput, TemplateField), packages/contracts/src/category/metadata.ts (UpsertMetadataInput), packages/contracts/src/category/blocks.ts (MerchandisingBlock types: banner/product_grid/text_block)
  </read_first>
  <action>
    Create ProductTemplatePage.tsx: load the category's attributes and the current template (GET /categories/:id/template); let the admin set per-field default and hint, keyed by attribute `key` (TemplateField references key, not id); Save via PUT /admin/categories/:id/template (UpsertTemplateInput). Covers CAT-05.
    Create VendorRestrictionsPage.tsx: show the category's is_restricted state and the approved vendor list (GET /categories/:id/restrictions); let the admin toggle is_restricted (PATCH /admin/categories/:id) and add/remove vendor IDs (POST/DELETE /admin/categories/:id/restrictions/vendors). Document in UI copy that enforcement happens in Phase 3 (D-11). Covers CAT-06.
    Create BlockEditor.tsx: a block list with a "+ add block" type selector (banner / product_grid / text_block); each block type renders its own form fields (banner: imageUrl/title/subtitle/ctaText/ctaUrl; product_grid: title/productIds/layout grid|carousel; text_block: title/content) matching the MerchandisingBlock union; blocks reorderable via dnd-kit useSortable. No WYSIWYG in v1.
    Create CategoryMetadataPage.tsx: load metadata (GET /categories/:id/metadata; may be null/lazy), edit flat SEO fields (seoTitle/seoDescription/seoKeywords/canonicalUrl), description, imageUrl, and embed BlockEditor for blocks; Save sends the full payload to PUT /admin/categories/:id/metadata. The server-side MerchandisingBlockSchema is the authority — surface 400 validation errors returned from the API. Covers CAT-07 (D-12, D-13).
    Update CategoryDetailPage.tsx: wire the tab container (scaffolded in 02-07) to render AttributeBuilderPage, FilterSchemaPage, ProductTemplatePage, VendorRestrictionsPage, and CategoryMetadataPage as tabs for the selected category.
  </action>
  <verify>
    <automated>cd "d:/My Projects/grovio" && pnpm --filter @grovio/web-admin typecheck 2>&1 | tail -10 && pnpm --filter @grovio/web-admin build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ProductTemplatePage.tsx saves template fields keyed by attribute `key` via PUT .../template
    - VendorRestrictionsPage.tsx toggles is_restricted and adds/removes vendor IDs, with UI copy noting Phase 3 enforcement
    - BlockEditor.tsx supports all 3 block types (banner, product_grid, text_block) with type-specific fields and reorder
    - CategoryMetadataPage.tsx edits flat SEO fields + blocks and surfaces server 400 block-validation errors
    - CategoryDetailPage.tsx renders all 5 editors as tabs
    - `pnpm --filter @grovio/web-admin build` exits 0
  </acceptance_criteria>
  <done>Template, vendor restriction, and metadata/block editors built; all 5 editors wired into the detail tabs; web-admin builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify category configuration editors end-to-end</name>
  <what-built>
    The full per-category configuration UI in web-admin: attribute builder (6 types + options), filter schema editor, product template editor, vendor restriction manager, and metadata editor (SEO + merchandising blocks) — all tabs on the category detail page, persisting through the admin API.
  </what-built>
  <how-to-verify>
    1. With the API and admin app running (see plan 02-07 Task 4 setup), open a category detail page at http://localhost:5175/categories/:id.
    2. Attributes tab: add a text attribute and an enum attribute (add 2+ options), mark the enum filterable, save; reload and confirm persistence.
    3. Filters tab: add the enum attribute as a facet with display type "checkbox", save; confirm a non-filterable attribute is NOT offered.
    4. Template tab: set a default + hint for an attribute key, save, reload, confirm.
    5. Vendor restrictions tab: toggle is_restricted on, add a vendor UUID, confirm it appears; remove it.
    6. Metadata tab: fill SEO fields, add a banner block and a product_grid block, save; then attempt an invalid product_grid (no productIds) and confirm the server rejects it with a 400 validation message.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → API admin routes | Schema/metadata mutations cross here; the server is authoritative for validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | malformed merchandising blocks from UI | mitigate | BlockEditor produces typed blocks; the authoritative gate is the server-side MerchandisingBlockSchema.array().parse() in CategoryMetadataService (plan 02-05) which returns 400 — the UI surfaces it but never bypasses it (Pitfall 5) |
| T-02-21 | Tampering | filter facet over non-filterable attribute | mitigate | UI only offers is_filterable attributes; FilterSchemaService re-validates server-side (plan 02-04) so a crafted client request is still rejected |
| T-02-22 | Tampering | options omitted/extra from client | mitigate | AttributeDefinitionService enforces the options rule server-side (plan 02-04); UI conditional editor is convenience, not the authority |
| T-02-SC | Tampering | npm installs | accept | No new package installs in this plan — uses packages installed and verified in plan 02-07 |
</threat_model>

<verification>
- `pnpm --filter @grovio/web-admin build` — admin app builds with all editors
- Human checkpoint (Task 3) — all 5 editor tabs verified end-to-end including server-side block validation
</verification>

<success_criteria>
- Attribute builder: 6 types, conditional options, reorder (CAT-03 / D-08)
- Filter schema editor: filterable-only facets with display type (CAT-04)
- Product template editor keyed by attribute key (CAT-05)
- Vendor restriction manager (schema/CRUD only, Phase 3 enforces) (CAT-06 / D-11)
- Metadata editor: SEO + typed blocks; server-side validation surfaced (CAT-07 / D-12 / D-13)
- web-admin builds; human checkpoint passes
</success_criteria>

<output>
Create `.planning/phases/02-category-engine/02-08-SUMMARY.md` when done
</output>
