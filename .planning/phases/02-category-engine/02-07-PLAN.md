---
phase: 02-category-engine
plan: 07
type: execute
wave: 5
depends_on: [02-06]
files_modified:
  - apps/web-admin/package.json
  - apps/web-admin/src/main.tsx
  - apps/web-admin/src/App.tsx
  - apps/web-admin/src/lib/apiClient.ts
  - apps/web-admin/src/stores/categoryUiStore.ts
  - apps/web-admin/src/pages/categories/CategoryListPage.tsx
  - apps/web-admin/src/pages/categories/CategoryDetailPage.tsx
  - apps/web-admin/src/components/categories/CategoryTree.tsx
  - apps/web-admin/src/components/categories/CategoryTreeNode.tsx
autonomous: false
requirements: [CAT-01, CAT-02]
user_setup: []
must_haves:
  truths:
    - "web-admin has @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, react-router-dom installed (after legitimacy verification)"
    - "Admin can view the category tree, create/edit/archive categories, and reorder via drag-and-drop"
    - "Reorder and mutations call the admin API and invalidate the React Query categories cache"
  artifacts:
    - path: "apps/web-admin/src/components/categories/CategoryTree.tsx"
      provides: "dnd-kit sortable tree (CAT-01, CAT-02)"
      contains: "DndContext"
    - path: "apps/web-admin/src/pages/categories/CategoryListPage.tsx"
      provides: "Category list/tree page"
      contains: "useQuery"
  key_links:
    - from: "apps/web-admin/src/components/categories/CategoryTree.tsx"
      to: "POST /admin/categories/:id/reorder"
      via: "onDragEnd mutation"
      pattern: "reorder"
    - from: "apps/web-admin/src/main.tsx"
      to: "react-router-dom + QueryClientProvider"
      via: "router + query provider setup"
      pattern: "QueryClientProvider"
---

<objective>
Stand up the web-admin foundation for category management: install the (currently [ASSUMED]) dnd-kit + react-router-dom packages behind a blocking legitimacy checkpoint, wire React Router + a React Query provider + a typed API client, and build the category tree CRUD UI (list/detail pages, drag-and-drop tree with depth indentation, create/edit/archive). Reorder persists via the admin API.

Purpose: Delivers the CAT-01 (create/edit/archive/reorder) and CAT-02 (nested tree) success criteria through the admin tree UI — the primary DnD surface per D-08. Sets the routing/data-fetching scaffold that plan 02-08 builds the remaining editors on.
Output: Installed packages + router/query scaffold + API client + Zustand UI store + tree CRUD pages and components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-category-engine/02-CONTEXT.md
@.planning/phases/02-category-engine/02-PATTERNS.md
@.planning/phases/02-category-engine/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before install ([ASSUMED] packages)</name>
  <what-built>
    Plan 02-07 needs four packages not yet in web-admin: @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, react-router-dom. The RESEARCH.md Package Legitimacy Audit marked all four [ASSUMED] because slopcheck was unavailable. Per the package legitimacy gate, these MUST be human-verified before install. None may be auto-approved.
  </what-built>
  <how-to-verify>
    1. Confirm each package exists and is the genuine, widely-used library on the official registry:
       - https://www.npmjs.com/package/@dnd-kit/core  (repo should be github.com/clauderic/dnd-kit)
       - https://www.npmjs.com/package/@dnd-kit/sortable  (same repo)
       - https://www.npmjs.com/package/@dnd-kit/utilities  (same repo)
       - https://www.npmjs.com/package/react-router-dom  (repo should be github.com/remix-run/react-router)
    2. Confirm the latest versions and React 19 compatibility. Capture exact versions to pin:
       run `npm view @dnd-kit/core version`, `npm view @dnd-kit/sortable version`, `npm view @dnd-kit/utilities version`, `npm view react-router-dom version`.
    3. Confirm none are typosquats and weekly download counts are in the millions (dnd-kit) / tens of millions (react-router-dom).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name any package to remove/replace.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install packages + wire router, React Query provider, and API client</name>
  <files>apps/web-admin/package.json, apps/web-admin/src/main.tsx, apps/web-admin/src/App.tsx, apps/web-admin/src/lib/apiClient.ts, apps/web-admin/src/stores/categoryUiStore.ts</files>
  <read_first>
    - apps/web-admin/package.json (current deps — add the 4 verified packages with pinned versions from Task 1)
    - apps/web-admin/src/main.tsx (current React mount — wrap with providers)
    - apps/web-admin/src/App.tsx (current shell — convert to a routed layout)
    - .planning/phases/02-category-engine/02-PATTERNS.md (Admin UI files section — React Query, Zustand store, motion/Tailwind token patterns)
    - .planning/phases/02-category-engine/02-RESEARCH.md (§Admin UI Component Strategy — routing, expand/collapse store, offset pagination)
  </read_first>
  <action>
    Install the four human-verified packages in web-admin only: run `pnpm --filter @grovio/web-admin add @dnd-kit/core@<v> @dnd-kit/sortable@<v> @dnd-kit/utilities@<v> react-router-dom@<v>` using the exact versions captured in Task 1.
    Update main.tsx to wrap the app in a QueryClientProvider (new QueryClient) and a BrowserRouter. Update App.tsx into a routed layout with React Router Routes: a redirect/landing plus routes for /categories (CategoryListPage) and /categories/:id (CategoryDetailPage). Keep the existing motion.div page-enter animation pattern and grovio design-token Tailwind classes.
    Create lib/apiClient.ts: a thin typed fetch wrapper that targets the API base URL, sets JSON headers, includes the X-Internal-Admin-Token header for admin mutations (read from an env var via import.meta.env), and unwraps the { success, data } | { success, error } envelope (throwing on error). Export helpers used by category queries/mutations.
    Create stores/categoryUiStore.ts: a Zustand store (with persist middleware, name "category-ui-state") holding expandedIds (Set<string>) and toggleExpand(id) for tree expand/collapse persisted to localStorage.
  </action>
  <verify>
    <automated>cd "d:/My Projects/grovio" && pnpm --filter @grovio/web-admin typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, and react-router-dom with pinned versions
    - main.tsx wraps the tree in `QueryClientProvider` and a router provider
    - App.tsx defines routes for /categories and /categories/:id
    - apiClient.ts unwraps the { success, data } envelope and throws on { success:false }
    - categoryUiStore.ts uses Zustand `persist` with name "category-ui-state"
    - `pnpm --filter @grovio/web-admin typecheck` exits 0
  </acceptance_criteria>
  <done>Packages installed; router + query provider + API client + UI store wired; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Category tree CRUD UI (list/detail pages + dnd-kit tree)</name>
  <files>apps/web-admin/src/pages/categories/CategoryListPage.tsx, apps/web-admin/src/pages/categories/CategoryDetailPage.tsx, apps/web-admin/src/components/categories/CategoryTree.tsx, apps/web-admin/src/components/categories/CategoryTreeNode.tsx</files>
  <read_first>
    - apps/web-admin/src/App.tsx (motion.div page-enter pattern; grovio token classes)
    - apps/web-admin/src/lib/apiClient.ts (created in Task 2 — query/mutation helpers)
    - apps/web-admin/src/stores/categoryUiStore.ts (created in Task 2 — expand state)
    - .planning/phases/02-category-engine/02-PATTERNS.md (CategoryTree.tsx dnd-kit SortableContext + flat-list-with-indentation pattern; depth padding pl-0/pl-8/pl-16; CategoryListPage; CategoryTreeNode)
    - .planning/phases/02-category-engine/02-RESEARCH.md (§Admin UI Component Strategy — tree DnD, slide-over create, optimistic update)
    - packages/contracts/src/category/tree.ts (CategoryTreeNode type)
  </read_first>
  <action>
    Create CategoryTree.tsx: flatten the nested tree (from GET /categories) into a flat list carrying depth, wrap in dnd-kit DndContext (closestCenter) + SortableContext (verticalListSortingStrategy) keyed by node ids, render each row as CategoryTreeNode. On onDragEnd, compute the new sibling order and call the reorder mutation (POST /admin/categories/:id/reorder with orderedIds) using React Query useMutation with optimistic onMutate and onSuccess invalidation of the ['categories'] query key. Indent rows by depth via Tailwind pl-0 / pl-8 / pl-16 (D-08: tree DnD is the primary DnD surface).
    Create CategoryTreeNode.tsx: a single draggable row using useSortable; shows name, an expand/collapse toggle (wired to categoryUiStore) when hasChildren, and inline actions (edit → navigate to /categories/:id, archive → archive mutation). Use grovio token classes.
    Create CategoryListPage.tsx: useQuery(['categories','tree']) to fetch the tree, render CategoryTree, and a "New Category" action that opens a create slide-over (Framer Motion AnimatePresence + motion.div) posting to POST /admin/categories (with optional parentId for subcategories) and invalidating ['categories'] on success. Wrap the page in the motion.div enter animation.
    Create CategoryDetailPage.tsx: useQuery for the single category (GET /categories/:id) and an edit form (PATCH /admin/categories/:id for name/slug/sortOrder/isRestricted). This page hosts the tabbed editors that plan 02-08 fills in (attributes/filters/template/metadata/restrictions) — scaffold the tab container now and leave the per-tab editors to 02-08.
    Surface the 422 CategoryDepthError message to the user when creating a subcategory beyond depth 3 (CAT-02).
  </action>
  <verify>
    <automated>cd "d:/My Projects/grovio" && pnpm --filter @grovio/web-admin typecheck 2>&1 | tail -10 && pnpm --filter @grovio/web-admin build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CategoryTree.tsx contains `DndContext` and `SortableContext` and renders rows with depth-based padding (pl-0/pl-8/pl-16)
    - onDragEnd triggers a reorder mutation that invalidates the ['categories'] query key
    - CategoryListPage.tsx uses `useQuery` for the tree and provides create (with parentId support) + the create slide-over
    - CategoryDetailPage.tsx provides an edit form (PATCH) and an archive action, and scaffolds a tab container for 02-08
    - the depth-exceeded 422 error message is shown to the user on subcategory create
    - `pnpm --filter @grovio/web-admin build` exits 0
  </acceptance_criteria>
  <done>Tree CRUD UI built; drag reorder, create, edit, archive all wired to the admin API; web-admin builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify category tree management UI end-to-end</name>
  <what-built>
    A category tree management UI in web-admin: list/tree view with drag-and-drop reorder, create (root + subcategory via slide-over), edit, and archive — all persisting through the admin API and invalidating the React Query cache.
  </what-built>
  <how-to-verify>
    1. Ensure the API is running (docker-compose infra up; `pnpm --filter @grovio/api dev`) and the admin app is running (`pnpm --filter @grovio/web-admin dev`, port 5175). Set the X-Internal-Admin-Token env var to match the API's expected value (or run the API with NODE_ENV=development so the guard allows it).
    2. Open http://localhost:5175/categories. Create a root category, then a subcategory under it, then a third-level leaf. Confirm a 4th-level create attempt shows "Cannot create subcategory: maximum depth of 3 levels reached."
    3. Drag a category to reorder siblings; refresh the page and confirm the new order persisted.
    4. Edit a category name and archive a category; confirm the archived one disappears from the tree.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → API admin routes | Admin UI sends mutations with the placeholder admin token; the token is a dev-only stand-in until Phase 4 JWT |
| package registry → web-admin build | New third-party packages enter the build; supply-chain integrity must be verified before install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | @dnd-kit/*, react-router-dom installs | mitigate | Blocking human-verify legitimacy checkpoint (Task 1) confirms each [ASSUMED] package on the official registry before install; pinned exact versions; not auto-approvable |
| T-02-18 | Elevation of Privilege | admin token exposure in client bundle | accept | Phase 2 placeholder X-Internal-Admin-Token is a dev-only guard documented for Phase 4 JWT replacement; not a production secret (Pitfall 8, A5) |
| T-02-19 | Tampering | depth bypass from UI | mitigate | UI surfaces the server-side 422; the authoritative depth guard remains in CategoryService (plan 02-03), never trusted from the client |
</threat_model>

<verification>
- `pnpm --filter @grovio/web-admin build` — admin app builds with new packages
- Human checkpoint (Task 4) — tree create/edit/archive/reorder + depth limit verified in the browser
</verification>

<success_criteria>
- dnd-kit + react-router-dom installed after legitimacy verification (pinned)
- Router + React Query + API client + Zustand UI store scaffolded
- Category tree CRUD UI: create (root+sub), edit, archive, drag reorder — all persisting (CAT-01, CAT-02)
- Depth-limit error surfaced from the server
- web-admin builds; human checkpoint passes
</success_criteria>

<output>
Create `.planning/phases/02-category-engine/02-07-SUMMARY.md` when done
</output>
