/**
 * @grovio/contracts — wallet domain barrel export
 *
 * Re-exports all wallet contract types and Zod schemas.
 * Consumers import from "@grovio/contracts" (via root index.ts) or directly
 * from "@grovio/contracts/wallet" if tree-shaking is a concern.
 */

export * from "./types.js";
