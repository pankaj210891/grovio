/**
 * @grovio/contracts — orders domain barrel export
 *
 * Re-exports all order contract types and Zod schemas.
 * Consumers import from "@grovio/contracts" (via root index.ts) or directly
 * from "@grovio/contracts/orders" if tree-shaking is a concern.
 */

export * from "./types.js";
