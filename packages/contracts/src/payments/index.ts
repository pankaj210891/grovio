/**
 * @grovio/contracts — payments domain barrel export
 *
 * Re-exports all payment contract types and Zod schemas.
 * Consumers import from "@grovio/contracts" (via root index.ts) or directly
 * from "@grovio/contracts/payments" if tree-shaking is a concern.
 */

export * from "./types.js";
