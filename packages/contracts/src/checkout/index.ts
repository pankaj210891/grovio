/**
 * @grovio/contracts — checkout domain barrel export
 *
 * Re-exports all checkout contract types and Zod schemas.
 * Consumers import from "@grovio/contracts" (via root index.ts) or directly
 * from "@grovio/contracts/checkout" if tree-shaking is a concern.
 */

export * from "./types.js";
