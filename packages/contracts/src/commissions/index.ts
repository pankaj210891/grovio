/**
 * @grovio/contracts — commissions domain barrel export
 *
 * Re-exports all commission contract types and Zod schemas.
 * Consumers import from "@grovio/contracts" (via root index.ts) or directly
 * from "@grovio/contracts/commissions" if tree-shaking is a concern.
 */

export * from "./types.js";
