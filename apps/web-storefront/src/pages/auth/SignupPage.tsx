import { PageTransition } from '../../components/layout/PageTransition.js';

/**
 * Signup page stub — overwritten by Wave 6 (04-08).
 */
export default function SignupPage() {
  return (
    <PageTransition>
      <div className="flex min-h-[calc(100vh-128px)] items-center justify-center px-4">
        <h1 className="text-xl font-semibold text-grovio-text">Create Account</h1>
      </div>
    </PageTransition>
  );
}
